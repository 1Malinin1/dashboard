# WB · РНП Дашборд — памятка для Claude

Читай этот файл в начале **любой** новой сессии в этом репозитории — он описывает
архитектуру, где что лежит и как безопасно продолжать работу без рисков всё
переделать заново. Пользовательские фичи и формулы — в `README.md`, здесь —
только то, что нужно ИИ-агенту для работы с кодовой базой.

## Что это

Управленческий дашборд Wildberries для продавца — один файл `index.html`
(вся вёрстка/стили/логика инлайн, без сборки), задеплоен на GitHub Pages
(`1malinin1.github.io/dashboard/`), ветка `claude/wb-dashboard-html-0kxavb`.
Пользователь общается по-русски, комментарии в коде — по-русски.

## Модель безопасности — САМОЕ ВАЖНОЕ

Реальные данные (каталог товаров, история заказов, финансы, реклама) хранятся
**зашифрованными** в `wb-secure.js` (AES-256-GCM, ключ — PBKDF2-SHA256 от
**кода доступа**). Это единственный файл с данными в репозитории — он публичный
(репозиторий публичный), поэтому шифрование — не формальность, а реальная защита.

- **Код доступа НИКОГДА не хранится в репозитории.** Его знает только
  пользователь и передаёт в чате. Если пользователь не дал код в начале
  сессии — **спроси**, не пытайся угадать и не бери из старых сообщений
  этого файла (тут его и не должно быть).
- Плейнтекст-версии данных (`wb-data.js`, `wb-reports.js`) **не коммитятся**
  (см. `.gitignore`) — они существуют только временно в `decrypted/`, которая
  тоже игнорируется гитом. Рабочая директория контейнера эфемерна: между
  сессиями `decrypted/` не переживёт — это ожидаемо, просто расшифруй заново.
- Единственное, что переживает переход в новую сессию, — то, что закоммичено
  в git. Поэтому: **закончил работу с данными → зашифровал → закоммитил
  wb-secure.js.** Не оставляй сессию с несинхронизированным decrypted/.
- Если пользователь просит сменить код доступа — расшифруй старым кодом,
  зашифруй новым, сообщи явно, что старый код перестанет работать у всех.

## Файлы репозитория

| Файл | Что это |
|---|---|
| `index.html` | Весь сайт: стили, разметка, вся JS-логика. Снимок грузится ДИНАМИЧЕСКИ (`loadSnapshot` в конце файла): сначала `data-version.json` без кеша, потом `wb-secure.js?v=<оттуда>`. Руками версию бампить НЕ надо. |
| `data-version.json` | Версия снимка + отпечаток разметки (`{v,period,builtAt,build,stampedAt}`). `encrypt.cjs` **дописывает** сюда `v` (полная перезапись сносила `build`, и самообновление молча выключалось после обычной заливки данных). Крошечный, грузится `cache:"no-store"` — поэтому обновление данных доезжает до браузера даже с закешированным `index.html`. Раньше версия жила в query внутри index.html и залипала вместе с ним: продавец видел «заказы по 3 авг», хотя в репозитории лежало 5-е. Не возвращай статический `<script src="wb-secure.js?v=...">`.<br>**`build`** — sha1 самого `index.html`, ставит `scripts/stamp-build.cjs`. Страница сверяет его со своим адресом `?b=…` и при расхождении ОДИН раз перезагружается по новому адресу — иначе после правок кода продавец ещё минут десять видит старую страницу из кеша Pages. Зациклиться нельзя: после перехода адрес уже содержит нужный build. Плюс `watchBuild` следит за обновлениями У ОТКРЫТОЙ вкладки: сверяет отпечаток при возврате на вкладку (`visibilitychange`/`focus`) и раз в 10 минут — дашборд у продавца сутками висит открытым и сам по себе не перезагружается. |
| `wb-secure.js` | Единственный файл с данными в git. Зашифрован. Формат: `const WB_SECURE = {v,alg,kdf,hash,iter,zip,salt,iv,ct,period}`. Внутри (после распаковки и расшифровки): `REAL_DATA` + `BAKED_FINANCE` + `BAKED_ADS` + `BAKED_FUNNEL`.<br>**`zip:"gzip"` — снимок СЖАТ до шифрования** (`zlib.gzipSync` в `encrypt.cjs`, `DecompressionStream("gzip")` в `wbDecrypt`). Зачем: JSON внутри ~9,5 МБ, base64 раздувал файл до **12,7 МБ**, и на слабой связи браузер его не догружал — дашборд открывался пустым (реальный инцидент 24.08). Со сжатием — **1,2 МБ** (9% от исходного). Поле появилось позже, поэтому снимок БЕЗ `zip` читается по-старому, как обычный текст — не удаляй эту ветку. |
| `favicon.svg` | Фавикон (градиентный квадрат «WB», тот же стиль, что логотип в шапке). |
| `README.md` | Пользовательская документация: что считается, формулы, как грузить отчёты. Обновляй при пользовательских фичах. |
| `scripts/*.cjs` | Офлайн-инструменты обслуживания данных (ниже). Не используются самим сайтом — сайт всё делает в браузере. |
| `decrypted/` | Плейнтекст (gitignored, эфемерно). Создаётся `scripts/decrypt.cjs`. |

## Офлайн-скрипты (`scripts/`)

Перед первым запуском: `cd scripts && npm install` (нужен только пакет `xlsx`).

Типовой цикл обслуживания данных:

```
node scripts/decrypt.cjs <код>              # → decrypted/wb-data.js + wb-reports.js
# ...правите decrypted/ через один из скриптов ниже, или руками...
node scripts/encrypt.cjs <код>              # decrypted/*.js → wb-secure.js
node scripts/stamp-build.cjs                # ОБЯЗАТЕЛЬНО, если менял index.html
git add wb-secure.js && git commit && git push
```

- **`stamp-build.cjs`** — пишет в `data-version.json` отпечаток `index.html`. Без него правка кода
  доезжает до браузера продавца только когда протухнет кеш GitHub Pages (до 10 минут), и он пишет
  «не вижу изменений». **Руками запускать не нужно — это делает git-хук** `.githooks/pre-commit`
  (срабатывает, когда в коммит попадает `index.html`, и сам добавляет `data-version.json`).
  **В НОВОЙ СЕССИИ включи хук одной командой:** `git config core.hooksPath .githooks` — конфиг
  git'а не переживает пересоздание контейнера, а сам хук лежит в репозитории.
- **`decrypt.cjs <код>`** — расшифровывает `wb-secure.js` → `decrypted/wb-data.js` + `wb-reports.js`.
- **`encrypt.cjs <код>`** — обратно, `decrypted/*.js` → `wb-secure.js` (с самопроверкой roundtrip).
- **`ingest-finance.cjs файл1.xlsx файл2.xlsx ...`** — разбирает отчёты «О реализации» и/или
  «История затрат» (авто-определение типа), копит построчные записи в
  `decrypted/acc-finance.json` / `acc-ads.json` с дедупом по отпечатку строки
  (`rowHash`, идентичен воркеру на сайте). Можно вызывать много раз — накопитель
  не обнуляется. Требует уже расшифрованный `decrypted/wb-data.js` (список артикулов).
- **`bake-snapshot.cjs "новая подпись периода"`** — сливает накопитель в
  `decrypted/wb-reports.js`: новые даты перекрывают старые (без задвоения),
  остальные даты старого снимка сохраняются (недели складываются, не затираются).
  **Печатает контрольные суммы — сверяй их с личным кабинетом WB перед encrypt/push.**
  **Переносит `BAKED_FUNNEL` из старого снимка как есть** (её печёт `bake-funnel.cjs`) —
  без этого пересборка молча стирала воронку и ломала вкладку «Товар». Отбрасывает
  полностью пустые агрегаты (ни штук, ни денег) — иначе нулевая строка со старой датой
  создавала фантомную дату и «история с …» съезжала на месяцы назад.
- **Строки без даты продажи** (прежде всего «Возмещение издержек»): WB не привязывает их
  к продаже — пусты ВСЕ колонки с датами. `aggregateFinanceRows` **размазывает** такие суммы
  по дням того же артикула пропорционально его выручке за день (остаток отдаётся последнему
  дню, чтобы сумма сошлась до копейки). Раньше они падали целиком на последний день периода
  и он проваливался на графике «Прибыль по дням» (26.07: 404 487 ₽ вместо ~792 000 ₽) —
  итог за период при этом был верным. Не возвращай `r.date || maxDate`.
- **ЛЮБОЙ СКРИПТ, ПЕРЕПИСЫВАЮЩИЙ `decrypted/wb-reports.js`, ОБЯЗАН ПЕРЕНОСИТЬ `MODELED_FINANCE`**
  (инцидент 01.09.2026). `bake-funnel.cjs`, `bake-snapshot.cjs` и `wb-drop-day.cjs` пишут файл
  целиком и переносили только `BAKED_FUNNEL` — заливка ОДНОЙ воронки молча стирала оценку
  финансов за недели после факта, и «Главная» схлопывалась с 9 недель до 5 (84 → 67 млн ₽).
  Восстанавливается прогоном `model-finance.cjs` (он пересобирает оценку из заказов), но
  заметить пропажу можно только по числу недель. Во всех трёх скриптах перенос добавлен.
- **`bake-funnel.cjs <день1.xlsx> …`** — запекает **воронку продаж** (детальный
  подённый отчёт WB «Воронка продаж») в `BAKED_FUNNEL` внутри `decrypted/wb-reports.js`.
  Источник — те же подённые файлы, что и `wb-extend-orders`/`ingest`, но берутся ВСЕ
  ступени: показы/переходы/корзина/заказы/выкуп + суммы + остатки. Колонки читаются
  **по имени** через `indexOf` (формат отчёта менялся — индексы «плывут»: в старом
  экспорте был лишний столбец «Артикул продавца» перед «Артикул WB», воркер в index.html
  до сих пор рассчитан на старый формат по фикс-индексам). Даты из файлов перекрывают те
  же дни снимка; печатает контрольные суммы. Дальше `encrypt.cjs`. Формат записи:
  `{id:дата_sku, date, sku, name, category, impressions, cardViews, addCart, addFav,
  ordersQty, buyoutQty, cancelQty, ordersSum, buyoutSum, cancelSum, wbStock, ownStock}`.
- **`add-catalog-cards.cjs <файл.xlsx> [лист] [--apply]`** — ДОБАВЛЯЕТ новые карточки в каталог ВБ.
  `update-catalog-meta.cjs` только обновляет уже существующие, а неизвестные артикулы молча
  кладёт в «нет в каталоге» — завести новую линейку было нечем (21.09.2026 продавец завёл
  25 рулей и 20 позиций бокса, и они висели в выгрузках XWAY как «чужие», 45 карточек).
  · **КОД 1С ОБЯЗАТЕЛЕН, строка без него НЕ ДОБАВЛЯЕТСЯ.** `supplierCode` — единственная связка
    со всем остальным: артикул Озона = код 1С, склад (`warehouse.bySup`), паллеты, «в пути»,
    режим «Всего», а в ПОДСОРТЕ он вообще ключ строки. Карточки с пустым кодом схлопнулись бы
    там в ОДИН пустой ключ и перемешали товары между собой. Лучше не добавить, чем добавить
    сломанным — скрипт печатает такие строки списком.
  · По умолчанию СУХОЙ ПРОГОН, запись только с `--apply`.
  · Колонки по имени: «Артикул ВБ» + «Код 1С» (обязательные), «Наименование», «Категория»,
    «Код фабрики», «Статус», «Контейнер», «Себестоимость». Статус по умолчанию «Новинка»
    (не «На вывод» — иначе новая карточка сразу спрячется по флагу `showOut`).
  · `buyoutPct14d` ставится `null`, а НЕ 0: ноль читался бы как «0% выкупа» и обнулил расчёты.
    `costHistory` заводится только вместе с ценой, первая запись всегда `from:null`.
  · **НОЛЬ В СЕБЕСТОИМОСТИ И КРАТНОСТИ ЧИТАЕТСЯ КАК «НЕ ЗАПОЛНЕНО»** (`numPos`, поймано
    21.09.2026 на первом же возвращённом файле). Продавец заполняет файл частями и в пустых
    клетках оставляет 0: из 45 карточек цена была у 9, у остальных стоял ноль. Записать его
    как цену значит сказать «товар достался бесплатно» — себестоимость проданного 0, и карточка
    выглядит бесконечно прибыльной на «Главной», причём молча. Кратность 0 так же сломала бы
    округление дозаказа до контейнера.
  · Дальше: `update-ad-tags.cjs` (проставит склейки новым карточкам) → `encrypt.cjs`.
- **`update-catalog-meta.cjs справочник.xlsx [лист]`** — заливает справочные поля каталога ВБ из
  рабочего файла продавца (колонки по имени: «Артикул Wb» · «код поставщика» · «код фабрики» · «Статус»):
  `factoryCode` и `productionStatus` (регистр нормализуется: «на вывод» → «На вывод»).
  `supplierCode` **не перезаписывается**, только сверяется и проставляется если пуст — он связывает
  товар с Озоном (арт. Озона = supplierCode), тихая правка порвала бы связку. Печатает, что появилось
  впервые / что изменилось / чего нет в файле. Дальше `encrypt.cjs`.
- **ОСТАТКИ ВБ — ТРИ ФОРМАТА, и ВЕРЕН ТОЛЬКО ОДИН.** `update-wb-stock.cjs` читает «Всего находится
  на складах» (полный отчёт ВБ), «Склад WB РФ» (короткий отчёт) и «Текущий остаток товара»
  (выгрузка сервиса XWAY, продавец шлёт её, когда ВБ виснет). **Годится только полный отчёт
  с колонкой «Всего находится на складах»** — проси именно его.
  · **XWAY = FBO + FBS**: замер 26.08 —
  XWAY 76 719 шт против 52 745 в отчёте самого ВБ на следующий день; разница 23 974 совпала
  с 80% новосибирского склада КОД В КОД (487199: 3 464 против 3 466; 493369: 1 919 против 1 919).
  FBS — это товар с нашего склада, он уже посчитан в `REAL_DATA.warehouse`, поэтому заливка XWAY
  как `wbStock` ЗАДВАИВАЕТ товар: покрытие ВБ раздувается, подсорт занижается (24.08 подсорт на ВБ
  вышел 112 шт вместо 617). Скрипт печатает предупреждение при XWAY-формате.
  · **СКЛАДЫ ВБ СГОРЕЛИ — БЕРЁМ ТОЛЬКО «Склад WB РФ», «Всего находится» НЕ БЕРЁМ** (27.08.2026).
  Арифметика колонок: `«Всего находится» = «Склад WB РФ» + «Склад WB СГТ РФ» + именные склады`
  (Коледино, Электросталь, Котовск, Краснодар, Самара …) — равенство держится построчно на всех
  4 141 строке, колонки не пересекаются. **Но именные склады сгорели**, ВБ скрыл по ним реальные
  остатки, и цифры в этих колонках висят фантомом — товара за ними нет. Продавец подтвердил:
  живой остаток — «Склад WB РФ». Замер 27.08: 41 228 шт против 107 680 по «Всего находится».
  Прирост «Всего» с 24.08 (54 138 шт, ровно `catalog.pending` = 54 298) целиком лёг в сгоревшие
  склады — то есть это НЕ приёмка партии, а фантом; не принимай такое совпадение за доказательство
  прихода. В `update-wb-stock.cjs` приоритет колонок жёсткий: `iRF → iAll → iXway`; при обоих
  колонках печатается, сколько вышло бы по «Всего» и почему пропущено (`skippedAll`), при отсутствии
  «Склад WB РФ» — предупреждение (`onlyAll`). **Не возвращай приоритет «Всего находится».**
  · **`catalog.pending` после этого под вопросом**: 54 298 шт числятся «в пути на ВБ» по файлу
  «на оптовых», а в остатках их нет. На подсорт/дозаказ не влияет (покрытие ВБ = только `wbStock`),
  но в ЗАКУПЕ «есть сейчас» = остаток + в пути + склад + Китай + заказано — и они там учтены.
  Проси свежий файл «на оптовых» и перезаливай `update-transit.cjs`.
- **`update-cost.cjs себестоимость.xlsx [дата-с]`** — обновляет себестоимость. Ключ ищется по
  имени колонки («Код»/«код поставщика»/«артикул wb») и сопоставляется сначала с `supplierCode`
  (код 1С), потом с артикулом ВБ; один код 1С может стоять у нескольких артикулов — цену получают все.
  Числа читаются и в «озоновском» формате («1,015.09» = 1015.09). **Второй аргумент — дата, с которой
  действует новая цена**: тогда старая не пропадает, а уезжает в `costHistory` (см. ниже). Без даты —
  цена перезаписывается на всю историю. Печатает что заполнено впервые / что изменилось.
- **`set-prod-status.cjs "<В работе|Новинка|На вывод>" <коды|--file файл> [--except коды]`** —
  проставляет `productionStatus` пачкой (продавец решает судьбу ассортимента списками:
  «всё отсюда, кроме этих шести, — на вывод»). `list` — сводка по статусам. Код 1С
  индексируется и с пробелами, и без них («WY730A-WB белый синий» приходит без пробелов).
- **`missing-pallets-report.cjs [--all]`** — файл «паллеты-заполнить.xlsx»: товары без
  вместимости паллеты, отсортированные по важности (сверху то, что надо везти сейчас и есть
  на складе). Товары «На вывод» пропускаются — просить у них вместимость незачем (`--all`
  вернёт). Нужен запущенный локальный сервер: цифры подсорта берутся из самого дашборда.
- **`missing-cost-report.cjs`** — генерит `decrypted/себестоимость-заполнить.xlsx`
  (товары без цены, сортировка по продажам) — отдать пользователю на заполнение.
- **ДАТА ДНЯ У ПОДЁННЫХ ФАЙЛОВ — ДВА ИСТОЧНИКА** (`wb-extend-orders.cjs` и `bake-funnel.cjs`,
  держи синхронно). Обычная подённая выгрузка несёт дату на листе «Общая информация». Но продавец
  иногда шлёт ТОТ ЖЕ день в раскладке СВОДНОГО отчёта (единственный лист «Товары», колонки
  «… (предыдущий период)») — этого листа там нет вовсе, и скрипты падали с «нет даты периода».
  Теперь дата берётся из имени файла (в нём начало и конец периода, ДДММГГГГ) и принимается
  ТОЛЬКО если они совпадают — иначе это отчёт за диапазон и раскидать его по дням нельзя;
  тогда нужен явный `--date ГГГГ-ММ-ДД`. Сам разбор одинаков: колонки в обеих раскладках
  ищутся ПО ИМЕНИ («Заказали товаров, шт», «Заказали на сумму, ₽» …), а не по индексам.
- **`wb-extend-orders.cjs [--date ГГГГ-ММ-ДД] <день1.xlsx> …`** — продлевает `REAL_DATA.orderSeries` заказами ВБ из
  подённой аналитики (лист «Товары» → «Заказали товаров, шт» кол.19, ключ «Артикул WB»; дата из «Общей
  информации»). Заодно копит **₽ по дням** в `orderSeries.money` (кол.31 «Заказали на сумму, ₽»,
  кол.34 «Выкупили на сумму, ₽»). Даты из файлов перезаписывают те же дни ряда, прочие сохраняются;
  ряды всех sku держатся одной длины с `dates`. Дальше `encrypt.cjs`.
- **`wb-drop-day.cjs <ГГГГ-ММ-ДД>`** — вырезает ОДИН день из данных ВБ: `orderSeries`
  (dates + все ряды `bySku` + `money`) и `BAKED_FUNNEL`. Нужен, когда продавец присылает
  отчёт за СЕГОДНЯ: день неполный (выгружен утром), а окно «последние 7 дней» в подсорте
  из-за него проседает — на реальном примере 268 зак/дн вместо 324. Индекс вырезается из
  ВСЕХ рядов сразу и проверяется длина; при расхождении снимок не пишется.
  **Файл отчётов переписывается целиком — в ТОЙ ЖЕ форме, что у `bake-funnel.cjs`**
  (со служебными `BAKED_AT`, `*_ROWS`): без них `encrypt.cjs` падает с ReferenceError.
- **`xlsx-xml-reader.cjs`** — резервный читатель .xlsx напрямую по XML (см. ниже,
  «когда SheetJS виснет»).
- **`lib/parse-wb.cjs`** — общая логика (num/rowHash/парсеры/агрегация). **Если
  меняешь формулу «Итого к оплате» или набор колонок в воркере `index.html` —
  обновляй и здесь**, иначе отпечатки строк расходятся и офлайн-дедуп сломается.

## Как передать пользователю недостающие товары / кастомные отчёты

Смотри `missing-cost-report.cjs` как образец: генерируешь `.xlsx` в `decrypted/`
через `XLSX.writeFile`, отдаёшь через инструмент отправки файлов пользователю
(SendUserFile или аналог в актуальном тулинге), просишь заполнить и прислать
обратно — дальше `update-cost.cjs` на результате.

## Ключевые данные/форматы (чтобы не удивляться при отладке)

- **`REAL_DATA.catalog[]`**: `{sku, factoryCode, supplierCode, name, category,
  wbStock, inTransitToWB, ownWarehouseStock, buyoutPct14d, containerQty,
  productionStatus, pending, costPrice, costHistory}`. `costPrice: null` = себестоимость не задана
  (UI показывает предупреждение и позволяет вписать вручную/через файл).
- **`costHistory` — история себестоимости** `[{from:null|iso, cost}]` по возрастанию даты.
  Первая запись ВСЕГДА с `from:null` (цена «с начала времён») — это гарантирует `update-cost.cjs`,
  иначе `costAt()` не найдёт цену для прошлых дат и свалится на текущую, то есть молча пересчитает
  прошлые недели. Зачем: новая поставка дороже/дешевле старой, и продавец просил считать новую цену
  **только с даты поставки**, а прошлые периоды оставить как были. В UI:
  `costAt(sku,date)` — цена на дату (ручной `costOverrides` перекрывает всё), `costOf(sku)` — текущая.
  **Себестоимость проданного считается ПОСТРОЧНО по дате строки** — в `finAggregate`,
  `finSeriesByDate` и `finAggBySku` (там `cogs` копится сразу, `productPnL` его только берёт).
  Форвард-расчёты (дозаказ, закуп, безубыточность) используют `costOf` — текущую цену.
  Проверка при заливке: cogs за прошлый период до и после должен совпасть до рубля.
- **`REAL_DATA.orderSeries`**: `{dates:[iso...], bySku:{sku:[qty по дням, той же длины что dates]}},
  money:{iso:{sku:[заказано₽,выкуплено₽]}}}`. Ряды `bySku` должны быть строго одной длины с `dates` —
  при добавлении дней продлевай ВСЕ существующие ряды (нулями, если для sku заказов не было), иначе
  графики съедут. `money` — разреженная карта по дням (не по индексам), питает график «Сумма заказов
  и выкупа по дням» (₽) на «Динамике». Аналогично у Озона: `ozon.orderSeries.money` (см. ниже).
  В UI карту отдаёт MP-aware хелпер `mpMoney()`. **На «Всего» деньги СВОДЯТСЯ по коду товара**
  (`combinedMoney()`: у ВБ карта по sku → переводим через `supplierCode`, у Озона артикул и есть код;
  возвращает `{all,wb,oz}`). Раньше на «Всего» денег не было вовсе, и «общая» вкладка общей не была.
- **`BAKED_FINANCE[]`** — свод по (дата+артикул): `{id,date,sku,name,category,
  qty,returnsQty,revenue,payout,logistics,penalty,storage,reimb,deduction,
  priyomka,loyalty,loyaltyPts}`. `revenue`/`qty`/`returnsQty` — только из строк
  Продажа/Возврат; остальные денежные поля — сырые суммы (логистика/хранение/
  штрафы и т.д. — расходы, не привязаны к конкретной продаже).
- **`BAKED_ADS[]`** — `{id,date,sku,spend}`, свод по (дата+артикул) из «Истории затрат»,
  только кампании `18+...`, артикул = последние цифры в названии кампании.
- **`BAKED_FUNNEL[]`** — зашитая воронка по (дата+sku): `{id,date,sku,name,category,
  impressions,cardViews,addCart,addFav,ordersQty,buyoutQty,cancelQty,ordersSum,buyoutSum,
  cancelSum,wbStock,ownStock}`. Питает вкладку «Товар» (разбор воронки). Живёт в
  `wb-reports.js` рядом с `BAKED_FINANCE`/`BAKED_ADS`; `encrypt/decrypt.cjs` его переносят
  (с обратной совместимостью: старый снимок без воронки → `[]`). В UI `fdbRefreshCache`
  кладёт `BAKED_FUNNEL` базовым слоем, браузерные загрузки IndexedDB — поверх (даты
  загрузок перекрывают). `funnelIsBaked`/`funnelHasBaked` — как у финансов.
  **Суммы `ordersSum`/`buyoutSum` воронки сходятся с `orderSeries.money` до рубля** (один
  источник — подённые файлы), это хороший чек консистентности.
- **Формула «Итого к оплате»** (сверена на суммах всего магазина, точность 99.99%):
  `К перечислению − логистика − штрафы − удержания − хранение − операции приёмки
  − стоимость лояльности − баллы лояльности − возмещение издержек`.
  «Возмещение издержек» — это РАСХОД (вычитается), не доход. См. `finHoldings`/`finNet` в index.html.
- **Прибыль** = Итого к оплате − Себестоимость проданного − Реклама.
- **Главная vs Финансы — разные вкладки, не дубль**: «Главная» = ПРИБЫЛЬ (выручка − удержания WB
  − себестоимость − реклама, маржа). «Финансы» = СВЕРКА С WB: «сколько придёт на счёт и за что
  удержано» — герой-KPI «Итого к оплате», разложение `выручка = комиссия(revenue−payout) + логистика
  + хранение/приёмка + штрафы/удержания + возмещение + итого к оплате` (карточка «Куда уходит выручка»),
  по товарам все удержания раздельно + **% каждого раздела от выручки товара** (доля «съедания») и колонка
  «Забирает WB» (сумма+% всех удержаний). Себестоимости/рекламы на «Финансах» НЕТ (WB про них не знает).
- **Вкладка «Инструкция» удалена** (была под браузерную загрузку, теперь данные — через диалог).
  `TABS` = home/dynamics/turnover/stockrate/product/finance/ads/analytics. CSS-классы `.guide-*` остались, но не
  используются (мёртвые) — не восстанавливай вкладку без явной просьбы.
- WB отдаёт **два отчёта в неделю** — «по заказам» и «по выкупам» — их нужно
  оба грузить, они не пересекаются по отпечаткам строк и складываются.
- **Категория товара**: в финансовых отчётах WB поле «Предмет» может не совпадать
  с категорией каталога (напр. «Груши боксерские» в отчёте vs «Набор для бокса»
  в каталоге). Все таблицы/фильтры используют `normFinCategory()` — категорию
  каталога по SKU, а не то, что написано в отчёте. Не откатывай этот фикс.
- **Правило `hasActivity`**: товар попадает в таблицы по товарам (Главная/Финансы/
  Аналитика), только если были продажи, возвраты или реклама за период — иначе
  число товаров расходится между вкладками (это было багом, исправлено).
- **Доп. аналитические блоки по вкладкам** (карточки-разрезы, дополняют основные таблицы):
  Главная — «Что двигает прибыль» (`renderProfitDrivers`: топ-3 в плюсе / топ-3 в минусе под KPI),
  «Прибыль по дням» (`profitDayChart`: прибыль на своей шкале — столбцы зелёный+/красный−, плюс
  накопительная линия; ноль линии совмещён с нулём столбцов; конец линии = итог периода = KPI прибыли)
  и «Разбивка прибыли по категориям» (`renderHomeCatProfit`, бары +/−); Товар —
  «Топ узких мест по всем товарам» (`renderBottlenecks`: где товар конвертит хуже среднего
  по магазину; потери пересчитаны в **заказы** через средние конверсии; только `impressions>=300`);
  Аналитика — «Разбор возвратов» (`renderReturns`) и «Цена безубыточности» (`renderBreakeven`);
  Финансы — «Логистика и хранение по категориям» (`renderFinLogByCat`). Таблицы возвратов и
  безубыточности **сортируемые** (`anRetSortState`/`anBeSortState` + `wireSortable`).
- **Проверенные инварианты (аудит 06.08.2026)** — если правишь расчёты, эти равенства должны сохраниться:
  выручка = комиссия + логистика + хранение/приёмка + штрафы/удержания + возмещение + лояльность + «итого к оплате»;
  Σ прибыли по дням = итог периода; Σ по товарам = KPI (кроме служебных строк без продаж, см. ниже);
  Σ воронки `ordersSum` = `orderSeries.money`; ряды заказов длиной с осью дат; в подсорте
  отгрузка ≤ склада, кратна паллете, приоритет Озону; «Всего» = ВБ + Озон без задвоения.
  Скрипты проверок одноразовые (в scratchpad), но воспроизводятся за минуты через Playwright.
- **KPI «Прибыль» на Главной больше суммы колонки таблицы** на служебные строки без продаж
  (обычно «возмещение издержек» по непроданному артикулу): правило `hasActivity` не пускает их
  в таблицу, чтобы число товаров совпадало между вкладками, но в KPI они учтены. Разница
  печатается в подписи над таблицей (`homeTblNote`) — не «чини» её удалением правила.
- **Последняя ступень воронки ДОЗРЕВАЕТ**: и ВБ, и Озон проставляют выкуп/доставку задним числом,
  поэтому у свежих дней она нулевая (у ВБ в снимке — нули за все последние дни каждой выгрузки).
  `renderFunnelDiag` в этом случае ведёт сквозную конверсию до «Заказов» и пишет, что выкуп не
  дозрел, — раньше выводилось «сквозная конверсия 0.00%», что читалось как «ничего не выкупают».
- **Цена безубыточности** (`renderBreakeven`) = юнит-экономика: `(себестоимость + логистика/ед) ÷
  (1 − доля комиссии WB от цены)`. **Реклама, хранение, штрафы НЕ включаются** — это расходы периода,
  деление их на штуки завышало безубыток у малотиражных товаров (была цифра 8000₽ у товара за 1400₽).
  Не возвращай `finHoldings(r)` в формулу — это осознанный выбор ради адекватных цифр.

## Загрузка данных — только через диалог (браузерные дропзоны убраны)

Пользователь грузит все данные **через диалог** (ИИ запекает в снимок), а не через
браузер. Дропзоны на вкладках «Товар» (себестоимость, воронка) и «Финансы» (реализация,
реклама) **удалены** — браузерная загрузка жила только в IndexedDB локально до чистки
кэша и вводила в заблуждение. Функции-обработчики `handleFunnelFiles`/`handleCostFiles`/
`handleFinanceFiles`/`handleAdsFiles` и воркер парсинга **остались в коде, но НЕ
подключены к UI** (мёртвый код) — если понадобится снова, достаточно вернуть дропзону и
обвязку. `renderCostInfo`/`renderAdsHistoryInfo` — с null-guard, безопасны без своих
элементов. **Разные цифры в разных браузерах** = в одном остались локальные данные
(старые IndexedDB-загрузки финансов/рекламы/воронки или ручные правки себестоимости
`costOverrides` в localStorage `wb_cost_overrides_v1`). В строке свежести показывается
ссылка «сбросить локальные данные» (`localDataResetLink()` → `resetLocalData()`: чистит
три стора IndexedDB + costOverrides + reload) — только когда локальные данные есть
(флаги `*HasBaked && !*IsBaked`). После сброса везде — единый зашитый снимок.
**Воронка теперь запекается в снимок** — см. `BAKED_FUNNEL` ниже
(`FUNNEL_CACHE` = зашитый снимок + браузерные загрузки поверх, как у финансов).

## Вкладка «Товар» — разбор воронки (`renderProductPeriods` и др.)

Блок «Разбор воронки»: область `prodScope` (`sku`/`cat`/`all` — сегмент `#prodScopeSeg`),
выбор товара/категории, два периода A/B. `prodScopeSkus()` даёт набор sku (null = все);
`aggregateFunnel(skuSet,from,to)` сводит воронку по набору (остаток = последняя дата в
периоде у каждого sku, суммарно). Три вывода:
- **Визуальная воронка** (`renderFunnelVis`, `FUNNEL_STAGES` = Показы→Переходы→Корзина→
  Заказы→Выкуп): полосы шириной «доля от показов», конвертеры между ступенями (`connectorConv`)
  с Δ п.п. к периоду B; шаг с наибольшей просадкой помечается «узкое место» (`weakStepIndex`).
- **Авто-диагностика** (`renderFunnelDiag`, `weakestDeclineIndex`): сквозная конверсия +
  самый просевший и самый выросший шаг vs B (п.п.). Без B — подсказка заполнить B.
- **График по дням** (`renderProdChart`, `CHART_METRICS`, `funnelDailyAgg`, `metricBars`):
  выбранная метрика (заказы/₽/CTR/конверсия/%выкупа) по всем дням воронки.
Ниже — полная таблица метрик A/B (`FUNNEL_METRICS`). Всё показывается только когда есть
`FUNNEL_CACHE` и область «готова» (`prodScopeReady`: для `sku` — выбран товар). Перерисовка —
`refreshProductView()` (на смене области/товара/периода и на входе во вкладку).

## Финансы Озона (`scripts/ozon-finance.cjs`, ИУ 2026)

Прибыль Озона до 04.09.2026 не считалась вообще — не хватало одного: сколько площадка
забирает себе. Продавец прислал свои индивидуальные условия («Сравнение условий», Ozon)
и подтвердил, что колонка **«Новое ИУ 2026» — это его траты**.

**Тариф, % от выручки в выкупе** (лежит в `ozon.meta.terms`): эквайринг 1,3 · комиссия
категории 30 · логистика 0 · возвраты 0 · кроссдокинг 0 · платное размещение 0 ·
подписка Premium и прочее 1,4 · **реклама 10** → **итого 42,7%**. Реклама в самой таблице 8%, но продавец
попросил считать 10% — она отдельным полем (`terms.ads`, меняется `--ads`), `meta.adRate`
держится синхронным, чтобы не было двух разных цифр.

**ВТОРАЯ КОЛОНКА ТАБЛИЦЫ («Базовая оферта», итог 111,1%) К ПРОДАВЦУ НЕ ОТНОСИТСЯ** — он работает
по ИУ (04.09.2026). В расчётах не участвует, в ответах не приводится, сравнений «сколько бы
стоило по оферте» не делаем.

**СОИНВЕСТ 22% НЕ ВЫЧИТАЕТСЯ, И ЭТО ОКОНЧАТЕЛЬНО.** В таблице есть «Соинвест 22%» и «Net тариф
… New ИУ 18,7%» (= 40,7 − 22), но это НЕ скидка продавцу: «соинвест значит, что для клиента
просто будет дешевле, по факту все расчёты идут с той цены, по которой я продаю, а Озон даёт
соинвест за свой счёт» (04.09.2026). Покупателю Озон срезает цену из своего кармана, выручка
продавца не меняется. Вычесть 22% = раздуть прибыль на десятки миллионов из воздуха.

**БАЗА — ВЫРУЧКА В ВЫКУПЕ** (заказано ₽ × % выкупа), как в `model-finance.cjs` у ВБ. Брать
«выкуплено ₽» из `orderSeries.money` напрямую нельзя: у свежих дней доставка не дозрела.

**ПЕРИОДЫ У ПЛОЩАДОК РАЗНЫЕ — НЕ ПУТАЙ И НЕ СКЛАДЫВАЙ МЕСЯЦЫ В ЛОБ** (правило продавца
04.09.2026): **Ozon — КАЛЕНДАРНЫЕ месяцы** (август = 01.08–31.08), **ВБ — месяц по неделям ВБ**
(август = 03.08–30.08, см. `wbMonthOfWeek`). Месячные итоги двух площадок относятся к разным
отрезкам; для сводных цифр по бизнесу бери одинаковый диапазон дат явно.

Замер 01.08–03.09: Ozon выручка 38 401 056 ₽, забирает Озон 16 397 251 ₽, себестоимость
16 525 558 ₽, прибыль 5 478 247 ₽, маржа 14,3%. Календарный август: выручка 34 960 385 ₽,
прибыль 4 952 008 ₽, маржа 14,2%.

### Озон на «Главной» (`renderHomeOzon`, 04.09.2026)
`MP_TABS` пополнился `home`, и переключатель площадки появился на «Главной». Реализовано
ОТДЕЛЬНЫМ рендером, а не ветками внутри ВБ-шного: у площадок разные источники (у ВБ отчёт
о реализации, у Озона расчёт по тарифу ИУ), разный состав расходов и разный календарь месяцев —
смешать их в одной функции значит рано или поздно сломать ВБ, а его цифры продавец сверяет
с кабинетом. Контейнеры те же (`homeKpis`, `ch-home-*`, `homeTable`, `homeWeeksCard` …),
наполнение своё.
· **Считается В БРАУЗЕРЕ, не запекается** (`ozFinRows`/`ozFinAggregate`/`ozFinBySku`): тариф
  задаёт человек и меняет при пересмотре условий, запечённые строки разъехались бы с ним молча.
· **Себестоимость берётся своей картой `ozCostMap()` по коду 1С, а НЕ через `costAt()`** — тот
  смотрит в карты активной площадки, и на «ВБ»/«Всего» цена Озон-артикула из них не достанется.
· **Реклама внутри «Забирает Ozon»**, отдельная KPI-плитка есть, но подписана «уже входит» —
  иначе продавец сложит два числа и вычтет рекламу дважды.
· **В блоке «Каждая неделя отдельно» у Озона — МЕСЯЦЫ, А НЕ НЕДЕЛИ** (просьба продавца
  04.09.2026: «собери периоды так, чтобы видно было по Озону месяца, а не недели»); заголовок
  и подпись меняются на «Каждый месяц отдельно», у ВБ возвращаются обратно — контейнер общий.
  Месяцы КАЛЕНДАРНЫЕ (`ozMonthly`), неполные помечены бейджем, «лучший/худший» считается только
  по полным. Первая версия показывала недели, сгруппированные по календарным месяцам, и неделя
  на стыке попадала в ОБА месяца (31.08–03.09 висела и в сентябре, и в августе) — фильтр
  сравнивал месяц начала ИЛИ конца. Недель тут больше нет, задваиваться нечему; `ozWeekly`
  оставлена для ежедневного отчёта. Инвариант: сумма месяцев = «Весь период» (проверено:
  выручка 68 107 576 ₽, прибыль 8 421 190 ₽, 22 937 шт — сходится ровно).
· **«ВСЕГО» НА «ГЛАВНОЙ»: МЕСЯЦЫ ДВУХ ПЛОЩАДОК РЯДОМ** (`combinedMonths`/`renderHomeAll`).
  Границы месяца у площадок РАЗНЫЕ, и это учёт продавца, а не ошибка: Ozon — календарный
  месяц, ВБ — целые недели по правилу «неделя относится к месяцу своего ЧЕТВЕРГА». Границы
  сверены с тем, что назвал продавец 04.09.2026, и совпали все три: июль 29.06–02.08,
  август 03.08–30.08, сентябрь 31.08–27.09. Поэтому строка месяца — сумма ДВУХ РАЗНЫХ
  отрезков, и период каждой площадки печатается прямо в строке. Разрезы по товарам и графики
  на «Всего» скрыты (`homeCards(false)`) — у площадок разные артикулы и категории; обязательно
  возвращаются на ВБ/Озоне, иначе останутся скрытыми.
· **ЛОВУШКА, СТОИВШАЯ 7,3 МЛН: `costAt`/`costOf` СМОТРЯТ В КАРТЫ АКТИВНОЙ ПЛОЩАДКИ.** На «Всего»
  `catalogCostBySku` ключуется кодом 1С, а строки финансов ВБ — артикулом ВБ, и себестоимость
  ВБ падала в НОЛЬ: август показывал прибыль 10 385 777 ₽ вместо 3 078 374 ₽ и маржу 59,8%
  вместо 17,7%. Добавлен фолбэк `wbCostBySku()` — если в картах активной площадки пусто, цена
  берётся прямо из каталога ВБ по артикулу. Кеш сбрасывается рядом с остальными в `hydrateData`.
· **«Финансы» и «Аналитика» остаются только ВБ** и в `MP_TABS` не входят: «Финансы» — сверка
  с кабинетом ВБ (у Озона такого источника нет), «Аналитика» построена на строках отчёта.
· Сверка при внедрении: браузер и `ozon-finance.cjs` совпали ДО РУБЛЯ на трёх периодах
  (01.08–03.09, календарный август, вся история 68 769 911 ₽ выручки и 8 481 869 ₽ прибыли).

## Ozon — мультиплощадочность (в разработке, Фаза 1)

Дашборд расширяется на второй маркетплейс (Ozon) рядом с WB. Сделано на данный момент:

- **Модель данных**: `REAL_DATA.ozon = {catalog[], orderSeries:{dates[], byArt{}, money{}}, funnel[], ordersMeta}`.
  `ozon.funnel[]` — воронка Озона (см. `ozon-funnel-build.cjs`), питает вкладку «Товар» на площадке Озон.
  `ozon.catalog[].ozonSku` — числовой SKU Озона (из отчёта воронки, кол. «SKU»), нужен для прямой
  ссылки на карточку `ozon.ru/product/{ozonSku}/`; проставляет `ozon-funnel-build.cjs`.
- **Кликабельные артикулы**: `skuLink(sku)`/`productUrl(sku)` — артикул в таблицах ведёт на карточку
  товара: ВБ → `wildberries.ru/catalog/{sku}/detail.aspx`; Озон → `ozon.ru/product/{ozonSku}/` (или
  поиск по артикулу, если числового SKU нет); «Всего» — без ссылки. Площадка берётся из `_appliedMp`.
  `ozon.orderSeries.money` = `{iso:{арт:[заказано₽,выкуплено₽]}}` (заказано = «Ваша цена» всех строк,
  выкуплено = строки со статусом «Доставлен»; собирается в `ozon-build.cjs` из `byDateArtRub/byDateArtBuyRub`).
  Лежит в том же зашифрованном `wb-secure.js` (encrypt.cjs сериализует весь `REAL_DATA`).
  `ozon.catalog[]`: `{sku, name, category, wbSku}` — здесь **`sku` = артикул Озона**.
- **Связка ВБ↔Озон по арт. поставщика**: артикул Озона = `supplierCode` из WB-каталога
  (проверено: совпало 135/137). Отдельной таблицы соответствий НЕ нужно. В WB-каталоге
  `supplierCode` уникален (нужен для режима «Всего» — товары сливаются по нему).
- **ДВЕ КАТЕГОРИИ У ОЗОН-ТОВАРА, И ЭТО НАМЕРЕННО** (27.08.2026; до этого категория Озона
  ЗАМЕНЯЛАСЬ ВБ-шной ради общего фильтра — продавец попросил вернуть родные названия, потому что
  одинаковые подписи на двух площадках мешали работать):
  · `ozon.catalog[].category` — **родное название Озона** («Каталка», «Игрушка для песочницы»,
    «Сюжетно-ролевые игрушки», «Музыкальная игрушка»). Ставит **`ozon-category.cjs`** из отчёта
    «Аналитика → По товарам» (колонки «Категория 1/2/3 уровня», по умолчанию 3-й — самый детальный).
  · `ozon.catalog[].wbCategory` — категория из ВБ-каталога, **КЛЮЧ СВЯЗКИ для режима «Всего»**.
    Ставит `ozon-build.cjs` по связке артикул Озона = `supplierCode` ВБ (совпадает у 143/143).
  **В режиме «Всего» категория ВСЕГДА ВБ-шная** (`combinedDataset` берёт `c.wbCategory||c.category`):
  Озон-категории крупнее (одна «Каталка» = ВБ «Пушкары»+«Толокары»), и если бы сюда попадали они,
  один товар оказывался бы в двух категориях и фильтр делил бы выборку. Проверка после правок:
  список категорий на «Всего» обязан совпадать со списком ВБ, «чужих» категорий — 0.
  **Подсорт и «Запас и темп» тоже используют ВБ-категорию** — это сквозные виды по коду 1С, и
  переименование категории при переключении площадки там сбивало бы с толку. Родные названия
  Озона видны на «Динамике» и «Товаре» — там расчёт идёт по площадке.
  `applyMpData` при смене площадки **сбрасывает выбор категорий** (`dynCat`/`turnCat`/… + `srColFilters`) —
  иначе выбранные на ВБ «Пушкары» на Озоне не совпадут ни с чем и таблица окажется пустой.
  **Товар, которого нет в отчёте аналитики, добивается ПО СВЯЗКЕ** (`ozon-category.cjs fill`):
  строим карту «категория ВБ → категория Озона» по тем, кого отчёт покрыл (Пушкары→Каталка
  на 269 товарах, Парковки→Сюжетно-ролевые игрушки и т.д.) и применяем к остальным. Раньше
  тут был фолбэк на ВБ-название, и продавец видел в фильтре Озона «Пушкары» рядом с «Каталкой» —
  таких категорий на площадке нет. Если по ВБ-категории связки нет вообще (у «Машинок» и
  «Копилок» ни один товар не попадал в отчёт), ставится **«Без категории»**: честнее, чем
  показывать несуществующее название. Лечится любым отчётом аналитики, где эти товары есть.
- **`ozon-prune.cjs [--apply]`** — убирает из каталога Озона карточки БЕЗ ЕДИНОГО СЛЕДА
  на площадке: нулевые остаток, заявки, заказы за всю историю, строки воронки И отсутствует
  числовой `ozonSku`. Каталог собирается по связке с ВБ, поэтому в него попадают товары,
  которых продавец на Озоне не заводил, — они висят в фильтрах и лезут в подсорт «стартовой
  партией». **Товар с `ozonSku` или строками воронки НЕ удаляется**, даже если сейчас остаток
  и продажи нулевые: карточка существует, просто товар кончился, и удаление стёрло бы историю
  показов. Замер 31.08: из 143 удалено 6, оставлено 39 «пусто сейчас, но карточка есть».
  По умолчанию сухой прогон.
- **Переключатель площадки** `#mpSwitch` (ВБ / Озон / Всего): глобальный `activeMp`,
  `applyMpData(mp)` «перенаводит» `RCAT/RSERIES/RDATES/RLAST` на выбранную площадку
  (для «Всего» — `combinedDataset()`, объединение по коду товара + сумма рядов на общей
  оси дат). Приём: у Озон-товаров `sku`=артикул, поэтому существующий код «Динамики»
  (оперирует `sku`) работает без переписывания.
- **Воронка Озона (вкладка «Товар»)**: `REAL_DATA.ozon.funnel` (записи как FUNNEL_CACHE, sku=арт.продавца).
  Товар-таб теперь MP-aware: `activeFunnel()` отдаёт `FUNNEL_CACHE` (ВБ) или `OZ_FUNNEL` (Озон, из снимка),
  для «Всего» — пусто + подсказка «смотрите по площадке». Все функции воронки (`funnelDates`,
  `aggregateFunnel`, `funnelDailyAgg`) ходят через `activeFunnel()`. Последняя ступень: у Озона «Доставлено»
  (`stLabel()`), у ВБ «Выкуп». При смене площадки `applyMpData` сбрасывает `selectedSku`/`_prodPeriodsMp`
  (выбор товара и даты — свои на каждой площадке). `buyoutSum` у Озона отчёт не отдаёт → **ОЦЕНКА**
  (доставлено × средний чек `ordersSum/ordersQty`), в графике помечается «≈ оценка». В таблице метрик
  на Озоне свои подписи (Доставлено/Доля доставки) и скрыты поля, которых нет (addFav/cancelSum/остатки WB).
- **`MP_TABS`** = список вкладок с поддержкой выбора площадки. **Сейчас `dynamics` + `turnover` + `product`.**
  На остальных вкладках `switchTab` принудительно ставит эффективную площадку «ВБ»
  (данные ВБ не ломаются), а переключатель скрыт (`OZDATA` есть → показан только на «Динамике»).
  Когда появятся финансы/реклама Озона — добавляй вкладки в `MP_TABS` и расширяй датасеты.
- **Заказы Озона по дням**: gross «Заказано» (все статусы, включая отмены — так решил
  пользователь; выкупы/процент выкупа — отдельная будущая механика). Денежные KPI/колонки
  на «Динамике» для Озона/Всего скрыты (нет данных воронки).
- **Нюанс покрытия дат**: заказы ВБ и Озона могут кончаться на разные даты (напр. ВБ по 14.07,
  Озон по 20.07). В «Всего» после более ранней даты суммарный ряд отражает только более
  свежую площадку — это честно помечается в шапке «Динамики» (не баг, а разное покрытие).
- **История заказов Озона начинается 03.05.2026 — это ВЫХОД товара на площадку, не пропуск данных.**
  Продавец подтвердил: более ранних выгрузок не существует, просить их не нужно. 1–14 мая идут
  0–3 заказа в день, с 15 мая разгон (15 → 105 → ~155). Следствие для закупа: в базе «по среднему»
  май участвует как полноценный месяц со средним темпом ~68/день против реальных ~155 в конце месяца,
  то есть занижает расчёт (31 440 шт против 33 421 без мая). «По пику» это не затрагивает — там берётся
  лучший месяц, а пиковым май вышел лишь у 12 товаров из 143. Если продавец попросит — май исключается
  из базы так же, как декабрь (условие в `monthlyOrders`, рядом с `m.key.endsWith("-12")`).

### OZON ПЕРЕСТРОИЛ ЦЕНОВУЮ ЛОГИКУ (31.08–07.09.2026) — ГЛАВНОЕ ПРО ВЫРУЧКУ

Ozon переименовал ценовые показатели И сменил методику расчёта продаж в аналитике.
Раскатка началась 31.08.2026, 07.09 старый интерфейс убрали. Новая терминология:

| Было | Стало | Смысл |
|---|---|---|
| Ваша цена | **Предельная цена без акций** | что настроил продавец, без акций |
| Ваша цена с учётом акций | **Предельная цена** | ПОТОЛОК: выше цена на сайте не поднимется |
| Цена для покупателя | **Цена на сайте** | что видит покупатель после акций и скидок |
| Минимальная цена | **Ограничение для акций и стратегий** | нижняя граница для автомеханик |
| Цена до скидки | **Зачёркнутая цена** | база для показа скидки |

**ГЛАВНОЕ: «Предельная цена» — ЭТО ПОТОЛОК, А НЕ ЦЕНА ПРОДАЖИ.** Аналитика Ozon теперь
считает по **цене реализации** (фактическая продажа с региональными коэффициентами), поэтому
суммы на графиках у продавца упали — это смена методики, а не падение продаж.

**ЧТО ЭТО СДЕЛАЛО С НАШИМИ ДАННЫМИ (замеры 08.09.2026):**
· Выгрузка «Заказы»: колонка `Ваша цена` → `Предельная цена`. Парсер искал одно имя и молча
  писал НОЛЬ — за 07.09 залилось 339 шт при 0 ₽. Теперь имя ищется по `PRICE_COLS`, при
  промахе скрипт ПАДАЕТ (молчаливый ноль опаснее падения: штуки заливаются, день выглядит
  правдоподобно).
· **Воронка («Заказано на сумму») с 31.08 считает по НОВОЙ базе.** Перелом ровно 31.08:
  до 30.08 воронка/наш ряд = 88–104% (в среднем 96%), с 31.08 — 70,7–75,2%. **ШТУКИ при
  этом совпадают день в день** (31.08: 410 против 406; 03.09: 394 против 395) — расходится
  ТОЛЬКО цена. Это единственное, что реально изменилось 31.08.

**«ОПЛАЧЕНО ПОКУПАТЕЛЕМ» — НЕ ВЫРУЧКА ПРОДАВЦА. НЕ БЕРИ ЕЁ ЗА БАЗУ** (проверено 08.09.2026,
после того как я сам чуть не сделал наоборот). Соблазн очевиден: колонка даёт ~72% от потолка,
ровно как новая воронка. Три факта закрывают вопрос:
· **Разрыв НЕ НОВЫЙ.** По нашим товарам «оплачено ÷ предельная» = 71,0% (24.08), 70,0% (25.08),
  70,7% (26.08), 71,2% (27.08), 68,7% (28.08), 71,4% (29.08), 71,0% (30.08) — то есть ДО
  перелома он такой же, как после (72,6% на 07.09). Значит воронка и эта колонка — НЕ два
  независимых подтверждения падения выручки: просто воронка перешла с потолка на покупательскую
  базу, а сама колонка всё это время лежала рядом неизменной.
· **Это сумма ОТПРАВЛЕНИЯ, а не цена строки.** В смешанном отправлении она стоит на каждой
  строке целиком: 07.09 арт. 487176 — предельная 6 332 ₽, «оплачено» 26 813 ₽; 04.09 арт. 487164 —
  4 515 ₽ против 446 720 ₽. Построчная сумма задваивает. (Нашим игрушкам повезло: их отправления
  однострочные — 339 из 339 на 07.09, — поэтому дневной итог по ним всё же читается.)
· **Она НЕ включает оплату баллами Ozon.** 07.09: 17 строк из 339 ниже 20% от цены товара
  (24.08 арт. 487192 — толокар за 1 857 ₽ при «оплачено» 52,64 ₽). Продавец такой заказ
  получает целиком; в колонке — только живые деньги покупателя.
· В «Акциях» у этих строк стоит **«Системная виртуальная скидка селлера»** — та самая механика,
  про которую продавец говорил 04.09 («соинвест за счёт Озона»). Плюс «Цена товара до скидок»
  (зачёркнутая) втрое выше потолка: 501432 — 15 600 ₽ зачёркнутая, 5 446 ₽ предельная,
  3 955 ₽ оплачено. Три уровня цены, а не два.
· Воронка стабильно на 0,3–7,4% ВЫШЕ «оплачено» — похоже, «цена реализации» = оплачено + баллы.
  Точной формулы у нас нет.

**ЧЕГО МЫ НЕ ЗНАЕМ И ЧТО РЕШАЕТ ВОПРОС.** Получает ли продавец потолок (а разницу Ozon
компенсирует, как он говорил про соинвест 04.09) или цену реализации — из выгрузки «Заказы»
не следует НИКАК: там нет ни одной колонки «к начислению продавцу». Ответ даёт ТОЛЬКО
финансовый документ Ozon: «Начисления» / «Отчёт о реализации товаров» с суммой к начислению
по SKU. **Пока его нет, прибыль Озона считается по-старому (по «Предельной цене»), и это
надо проговаривать в каждом ответе про деньги Озона.**

**ЦЕНА ВОПРОСА** (замер 08.09.2026, вся история 03.05–07.09, `ozon-finance.cjs`):
заказано 81 998 661 ₽ · выручка в выкупе 75 512 567 ₽ · забирает Озон 32 243 866 ₽ ·
себестоимость 33 803 609 ₽ · **прибыль 9 465 092 ₽, маржа 12,5%**.
**Точка безубытка — 78,1% от потолка** (при тарифе 42,7% и этой себестоимости). Любая база
ниже неё делает всю историю Озона убыточной: при 72,6% прибыль −2 390 532 ₽, при 70,7%
−3 212 637 ₽. Разброс прибыли — 11,9–12,7 млн ₽ и смена знака, поэтому решать без финотчёта
нельзя ни в какую сторону.

**ЧТО УЖЕ СДЕЛАНО:** `ozon-ingest-orders.cjs` копит обе цены (`byDateArtPaid` рядом
с `byDateArtRub`), `ozon-build.cjs` кладёт третье число в карту:
`money[дата][арт] = [предельная, выкуплено, оплачено покупателем]`. Это НЕ заготовка для
переключения выручки (см. выше — база не годится), а материал для сверки, когда придёт
финотчёт. Колонка «Оплачено покупателем» есть и в старых выгрузках.

### ФИНОТЧЁТ OZON ПРИШЁЛ 17.09.2026 — ОБА ВОПРОСА ЗАКРЫТЫ (`scripts/ozon-unit-report.cjs`)
Продавец прислал отчёт **«Юнит-экономика»** за 01–31.08.2026 (8 236 строк по всему кабинету,
наших — 92). Это ЕДИНСТВЕННЫЙ документ Ozon, где написано, сколько площадка реально начислила
продавцу и сколько удержала. Он ответил на оба вопроса, висевших с 08.09.

**1) БАЗА ВЫРУЧКИ — «ПРЕДЕЛЬНАЯ ЦЕНА», ВОПРОС ЗАКРЫТ ФАКТОМ.** Отчёт по нашим товарам:
| строка отчёта | ₽ | ₽/шт | от нашего потолка |
|---|---|---|---|
| «Выручка» (заплатил покупатель) | 21 561 526 | 2 384 | 72,3% |
| + «Баллы за скидки» (соинвест) | 7 251 808 | | |
| + «Программы партнёров» | 197 316 | | |
| **= ЗАСЧИТАНО ПРОДАВЦУ** | **29 010 649** | **3 208** | **97,2%** |

Соинвест приходит продавцу **отдельной строкой живыми деньгами** — ровно как он говорил
04.09 («Озон даёт соинвест за свой счёт»), и его возражение 08.09 («по факту ничего
не изменилось в плане экономики») подтвердилось. **`revBase.applied=false` подтверждён,
включать НЕЛЬЗЯ**: 72,3% — это цена для покупателя, а не выручка продавца. Замер k=73,06%
остаётся историческим фактом о витрине аналитики, а не правилом расчёта.

**2) ТАРИФ ПОДТВЕРЖДЁН ПОСТРОЧНО, НО РЕКЛАМА БЫЛА ЗАВЫШЕНА ВДВОЕ.** Комиссия в отчёте
**30,02%** при 30% в ИУ, эквайринг **1,30%** при 1,3%, логистика 0 — совпадение до сотых.
А реклама: в модели стояли жёсткие 10%, факт августа — **4,23%**. Вместе с Premium это давало
42,7% против фактических 35,6%, и прибыль августа занижалась на **1 528 870 ₽ (25,6%)**.

**РЕШЕНИЯ ПРОДАВЦА 17.09.2026 (дословно):**
· реклама — «опирайся на то, что я тебе буду скидывать с отчетов, раньше просто у нас было 10%,
  сейчас выбрали другой процент оплаты за заказ, в целом я еще сейчас буду другие рекламные
  компании запускать. То есть то что шло и будет идти в отчетах то и считаешь»;
· Premium 1,4% — «вот ее нужно считать по умолчанию, там ее не будет».

**КАК УСТРОЕНО СЕЙЧАС.** Тариф разделён на две части:
· **фиксированная 32,7%** = эквайринг 1,3 + комиссия 30 + логистика 0 + возвраты 0 +
  кроссдокинг 0 + платное размещение 0 + **Premium и прочее 1,4** (в отчёте её нет ни строкой —
  и не ищи, продавец подтвердил, что её там не будет);
· **реклама — ПО ДАТЕ ДНЯ из `ozon.meta.unitReports[]`**: день внутри периода отчёта → ставка
  этого отчёта; день после последнего → ставка последнего (несём вперёд); день раньше первого →
  ставка первого. Хелперы: **`ozAdRateFor()` + `ozFixedRate()` в index.html, `adRateFor()` +
  `FIXED` в `scripts/ozon-finance.cjs` — ДЕРЖИ СИНХРОННО.**
· **ЭТО НЕ «переписывание прошлого», как с % выкупа.** Отчёт закрытого месяца не дозревает —
  он окончательный, поэтому факт августа законно применяется к августу. Не «чини» на правило
  «с даты замера и вперёд»: там причина была в дозревании выкупа, здесь её нет.
· Ручной `--ads` в `ozon-finance.cjs` перекрывает отчёты — только для прикидок.
· `terms.ads` остался как ЗАПАСНАЯ ставка для дат, когда отчётов нет вовсе.

**`scripts/ozon-unit-report.cjs`** (`<файл.xlsx>` / `list` / `drop <от> <до>`): читает лист
«Юнит-экономика», берёт только наши артикулы (∈ supplierCode ВБ), считает ставки и кладёт
запись в `meta.unitReports[]`. **Период берётся ИЗ ИМЕНИ ФАЙЛА** («…01.08.2026-31.08.2026.xlsx») —
внутри листа его нет ни строкой; при промахе скрипт падает, потому что без периода непонятно,
к каким дням относится ставка. Не переименовывай присланный файл, теряя даты. Повторная заливка
того же периода ЗАМЕНЯЕТ запись. Скрипт сам печатает сверку базы выручки (засчитано ₽/шт против
нашего потолка) и предупреждает, если засчитано заметно ниже потолка.
**`ozon-build.cjs` переносит `unitReports`** — ЧЕТВЁРТЫЙ капкан рядом с `terms`/`revBase`/`buyoutWin`:
потерять их значит молча вернуть 10% и занизить прибыль.

**ОТЧЁТ ЗА НЕЗАКРЫТЫЙ ПЕРИОД НЕПРИГОДЕН ДЛЯ СТАВКИ — ПРОВЕРКА ЗРЕЛОСТИ ОБЯЗАТЕЛЬНА**
(поймано 17.09.2026 на втором же файле). Продавец прислал отчёт за 01–16.09, и в нём
**комиссия 0,72% вместо 30%, эквайринг 0,04% вместо 1,3%, реклама 0,15% вместо 4,23%**,
доставлено/заказано 57,2% против 79,8% в августе. Причина: Ozon показывает выручку сразу,
а удержания начисляет позже — у открытого периода их просто нет. Если пустить такую ставку
в расчёт, модель скажет, что площадка почти ничего не забирает, и прибыль раздуется.
· `ozon-unit-report.cjs` ставит `mature = commissionRate >= terms.commission×0,8` и при
  `mature:false` печатает громкое предупреждение. Запись СОХРАНЯЕТСЯ (это факт на момент
  выгрузки), но **из выбора ставки исключается** — фильтр `r.mature!==false` в `ozUnitReports()`
  (index.html) и в `UR` (`ozon-finance.cjs`), **держи синхронно**.
· Перезалить тот же период, когда он закроется, — запись заменится сама.
· Признак надёжный именно по комиссии: у зрелого отчёта она совпадает с тарифом ИУ
  (август 30,02% при 30%), у незрелого близка к нулю. Не заменяй на «прошло N дней».

**ФОРМА ОТЧЁТА ПЛАВАЕТ — ЧИТАЕМ ЕГО `xlsx-xml-reader.cjs`, А НЕ SheetJS** (17.09.2026).
Августовский файл SheetJS читает нормально, сентябрьский — нет: там вся кириллица записана
числовыми сущностями (`&#x410;…`) в ячейках `<c t="str"><v>…</v></c>`, и **SheetJS теряет
старший байт** — «Артикул» приходит как «\u0010@B8:C;», колонки не находятся, скрипт падает.
`codepage` не помогает: искажение не в кодировке, а в разборе. Поэтому отчёт читается своим
XML-читателем, и в него пришлось добавить три вещи (все нужны, не убирай):
· разворот числовых сущностей `&#x41D;` / `&#1053;` (иначе имя листа и колонки — мусор);
· ячейки БЕЗ атрибута `r=` (сентябрьская форма адреса не пишет; прежний regex требовал `r="A1"`
  и не находил ни одной ячейки — лист читался пустым);
· разбор `workbook.xml.rels` НЕЗАВИСИМО ОТ ПОРЯДКА АТРИБУТОВ (там `Target` идёт перед `Id`,
  а regex требовал `Id` первым — лист не находился вовсе).
Оба файла после этого читаются одним путём. Шапка ищется по строке с «SKU» и «Артикул»
в первых 15 строках (в августе это строка 1, в сентябре 4 — перед ней «Период: …»).
Период берётся из строки «Период: …» внутри файла, если она есть, иначе из имени файла;
явные `--from/--to` перекрывают оба.

**ЧТО ИЗМЕНИЛОСЬ В ЦИФРАХ** (после правки, сверено браузер vs скрипт ДО РУБЛЯ, 0 ошибок консоли):
| период | было (42,7%) | стало (32,7% + реклама из отчёта) |
|---|---|---|
| август кал. | 4 443 457 ₽ / 14,2% | **6 261 261 ₽ / 19,9%** |
| сентябрь 01–16 | — | **3 340 427 ₽ / 20,8%** |
| вся история | — | **13 874 744 ₽ / 18,6%** |

**СВЕРКА МОДЕЛИ С ФАКТОМ (август):** модель 6 261 261 ₽ против факта отчёта 5 972 326 ₽ —
расхождение +288 935 ₽ (4,8%) против −1 528 870 ₽ (−25,6%) до правки. Остаток объясняется
двумя вещами и это нормально: модель берёт заказы×выкуп (9 516 шт по 3 300 ₽/шт), отчёт —
ДОСТАВЛЕННЫЕ в августе (9 044 шт по 3 208 ₽/шт), это разные когорты; плюс модель считает
Premium 1,4%, которой в отчёте нет. **Проси этот отчёт раз в месяц — это лучшая проверка
модели, которая у нас есть.**

### РЕКЛАМА ОЗОНА: СТАВКА ПО УМОЛЧАНИЮ + ФАКТ XWAY ПОВЕРХ, ПОТОВАРНО (`scripts/ozon-ads-spend.cjs`)
**Решение продавца 17.09.2026, два сообщения подряд.** Первое: «по умолчанию затраты на
продвижение по всем товарам ты берёшь согласно нашей проверки юнитки за август, но
дополнительно я тебе ещё буду скидывать затраты на рекламу с xway … я запустил выборочно
товары. То есть по умолчанию у тебя есть модель по расчётам, но если в отчёте с xway будет
другая цифра — ты и берёшь её». Второе (оно и задаёт ГРАНУЛЯРНОСТЬ): «ближайшие 2-3 недели
изменения будет только у 4095529612, 4095524788, 4095524257, 4317503881, 3521243052,
315176520, 4095525293, 3521243327 — в этих товарах будет другая экономика в плане трат
на рекламу (ДРР), так как я запустил дополнительно рекламу, а у всех других (на озоне)
будет фикса как и идёт по августу».

**ПРАВИЛО ПОТОВАРНОЕ, А НЕ НА ВСЮ ПЛОЩАДКУ.** Товар есть в выгрузке за эти даты → его
реклама = ФАКТ из выгрузки, ставка к нему не применяется. Товара нет → ставка из отчёта
юнит-экономики (сейчас август, 4,23%). Не заменяй на «общий процент по площадке»: расход
размажется по всем 143 товарам, рекламируемые получат заниженный расход, остальные — чужой.
· Хранилище — `ozon.meta.adSpend = {parts:[{from,to,bySup:{код1С:₽},src}], bySup, watch}`.
  Ключ ВСЕГДА код 1С (= артикул продавца); скрипт принимает и числовой SKU Озона, и код 1С.
· **Разнесение по дням — пропорционально выручке дня** (выгрузка даёт итог за период, как
  у ВБ в `adRowsFromPerf`). Внутри периода день приблизителен, на неделю/месяц это не влияет.
· **Расход по товару БЕЗ продаж в периоде не теряется**: ему заводится строка с нулевой
  выручкой на последний день части (`ozApplyAdFact` в index.html, симметричный блок в `calc()`
  в `ozon-finance.cjs`). Иначе деньги молча выпали бы из прибыли. Проверено: 5 000 ₽ на
  артикул без заказов дали строку с `profit:-5000`.
· **Инвариант:** Σ `ad` по строкам с `adFact` = сумма из выгрузки, до копейки. И
  `mpFee = выручка×фиксированная ставка + реклама` для ЛЮБОЙ строки (проверено, расхождение 7e-12).
· `mpFee` при факте считается как `rev*FIXED/100 + факт`, а не по суммарному проценту —
  иначе реклама учлась бы дважды.
· **ДУБЛЬ ЛОГИКИ В ДВУХ МЕСТАХ, ДЕРЖИ СИНХРОННО**: `ozApplyAdFact()` + `ozAdSpendParts()`
  в index.html и `AP`/`adFact()`/`partRev` в `scripts/ozon-finance.cjs`. Ручной `--ads`
  отключает и факт, и отчёты (только для прикидок).
· `ozon-build.cjs` переносит `adSpend` — **ПЯТЫЙ капкан** рядом с
  `terms`/`revBase`/`buyoutWin`/`unitReports`.
· **Команды:** `list` · `set <с> <по> "4095529612=12345, 493370=6789"` (когда цифры приходят
  текстом) · `<выгрузка.xlsx> [с] [по]` (шапка ищется по «Артикул/SKU» + «Расход»; при промахе
  ПАДАЕТ и печатает шапку файла — молчаливый ноль опаснее падения) · `drop <с> <по>` ·
  `watch "<ключи>"|off`. Повторная заливка пересекающегося периода ЗАМЕНЯЕТ часть.
· **`watch` — список «реклама запущена, выгрузки ещё нет».** Арифметику не меняет, но строка
  свежести «Главной» на Озоне пишет: «по N товарам реклама запущена, но выгрузки XWAY ещё нет —
  пока считаются по ставке». Без этого расход новых кампаний молча сидел бы в модели по
  августовским 4,23%, а продавец считал бы, что видит факт. Сейчас в списке те самые 8 SKU
  (коды 1С 493373 · 493370 · 487180 · 500175 · 487168 · 487199 · 487190 · 487195).
· Строка свежести печатает ПРИМЕНЁННУЮ ставку (`ozFixedRate()+ozAdRateFor(последний день)`),
  а не запасную `terms.ads` — раньше там висели старые 10%.
· Сверка при внедрении (браузер vs `ozon-finance.cjs`, ДО РУБЛЯ, 0 ошибок консоли): без частей
  цифры не изменились ни на рубль (сентябрь 3 340 427 ₽, август 6 261 261 ₽, вся история
  13 874 744 ₽); с тестовой частью 117 000 ₽ на 8 товаров за 10–16.09 обе стороны дали
  рекламу 401 293 ₽ и прибыль 1 333 107 ₽ за неделю, август при этом не тронут.
· **СТРОКА С НУЛЕВЫМ РАСХОДОМ В ВЫГРУЗКЕ — ЭТО «КАМПАНИИ НЕ БЫЛО», А НЕ «РЕКЛАМА СТОИЛА 0»**
  (первая живая выгрузка 18.09.2026). В файл попадают все карточки рекламного кабинета,
  в том числе те, по которым ставка не крутилась (493374). Записать им 0 значит сказать, что
  площадка не взяла за продвижение ничего, хотя средняя ставка из юнит-экономики никуда
  не делась, — такие строки пропускаются, товар остаётся на ставке. Скрипт печатает, кого пропустил.
· **ИМЯ ФАЙЛА ПРИХОДИТ С «_» ВМЕСТО ПРОБЕЛОВ** (`XWAY_RICHFAMILY_2026-09-17_2026-09-17.xlsx`):
  граница слова `\b` между «_» и цифрой НЕ срабатывает, и период не определялся. Дата ищется
  по соседним ЦИФРАМ (`(?<![0-9])…(?![0-9])`). Тот же фикс в `update-ads-perf.cjs` — правь оба.
· **РАСХОД ЗА ДНИ БЕЗ ЗАКАЗОВ МОЛЧА ПРОПАДАЕТ — `warnGaps()` в `ozon-ads-spend.cjs`**
  (поймано 21.09.2026). Расход разносится по дням пропорционально выручке дня, а товару без
  продаж строка кладётся на ПОСЛЕДНИЙ ДЕНЬ ЧАСТИ, ВЗЯТЫЙ ИЗ РЯДА ЗАКАЗОВ (`partLast` в
  `ozon-finance.cjs`, `days[days.length-1]` в `ozApplyAdFact`). Если в окне части НЕТ НИ ОДНОГО
  дня заказов, такого дня не существует: скрипт пропускает часть целиком (`if(!partLast[pi]…) return`),
  а дашборд кладёт строку на дату вне ряда, и её отсекает фильтр периода. **Ни ошибки, ни пустой
  таблицы — деньги просто исчезают из прибыли.** Реальный случай: продавец прислал расход Озона
  за 18–20.09 (105 568 ₽), когда заказы были залиты только по 17.09, — пропали бы все 105 568 ₽.
  · Арифметикой не чинится: без выручки дня расход не на что разнести. Поэтому при заливке и в
    `list` печатается громкое предупреждение с суммой и последним днём заказов.
  · **Лечится заливкой заказов** (`ozon-ingest-orders.cjs` → `ozon-build.cjs`) — расход
    разнесётся сам, часть пересобирать или удалять не нужно.
  · Порядок заливки поэтому такой: СНАЧАЛА заказы Озона, ПОТОМ расход. Обратный порядок
    работает, но до заливки заказов цифры прибыли занижены на весь расход этих дней.

### ЦЕПОЧКА ДРР НА ОЗОНЕ: КАБИНЕТ → КАТЕГОРИЯ → СКЛЕЙКА → ТОВАР (`renderAdsOzon`, 18.09.2026)
Просьба продавца в день первой выгрузки: «нужно иметь полную цепочку, категория склейка товар,
общий ДРР. Просто может быть были куплены другие товары со склеек помимо рекламных, и тогда
ситуация будет другая». **Он оказался прав, и разрыв огромный:** 17.09 личный ДРР восьми
рекламируемых товаров по ФАКТУ XWAY — **28,0%** (от 14,5% у 500175 до 79,4% у 487199),
а по их склейкам (с учётом обоих источников расхода) — **5,6–14,7%** при цели ≤7%;
по всему кабинету Ozon — **6,1%**. Судить по личному ДРР значит выключать «лошадку»,
которая тянет заказы соседям по склейке.

- **СКЛЕЙКИ БЕРУТСЯ ВБ-ШНЫЕ, ПО КОДУ 1С** — он же артикул Озона. Продавец подтвердил дважды:
  «склейки на озоне у нас точно такие же как и на вб» и «у тебя есть и на вб и на озоне
  шестизначный код, по нему ты можешь посмотреть как сделаны склейки на вб и сделать такие же
  на озоне». Отдельного источника склеек для Озона НЕ НУЖНО. Покрытие: 113 товаров Озона
  из 137 имеют склейку ВБ, остальные идут «(без склейки)» отдельной строкой — не прячем.
  Карта — `ozAdMap()` (кеш `_ozAdMapCache`, сбрасывается в `hydrateData`).
- **ЗНАМЕНАТЕЛЬ — ВЫРУЧКА В ВЫКУПЕ ВСЕХ КАРТОЧЕК УРОВНЯ**, включая нерекламируемые: то же
  правило, что на ВБ («лошадка» тянет продажи соседям, они законно в зачёте).
- **В ЧИСЛИТЕЛЕ — РЕКЛАМА У КАЖДОГО ТОВАРА, А НЕ ТОЛЬКО ФАКТ XWAY** (правка продавца
  18.09.2026, он поймал это в первой же версии: «помимо тех товаров, что я скинул, есть
  рекламный расход, там работает другой канал продвижения, и по всем остальным ты берёшь
  фиксированный расход по юнитке за август»). Первая версия брала только `adSpend` — ДРР
  кабинета вышел **2,2% вместо реальных 6,1%**, потому что модельные 4,23% по остальным
  137 товарам просто выпадали (35 330 ₽ из 55 370 ₽ за 17.09).
  **ПОЭТОМУ `ozAdRows()` СТРОИТСЯ ИЗ `ozFinRows()`** — того же источника, что «Главная»:
  там у каждой строки `ad` уже правильный (факт XWAY у своих товаров, ставка у остальных).
  Реклама в цепочке ДРР и реклама в прибыли — ОДНО И ТО ЖЕ ЧИСЛО; считать их двумя разными
  способами нельзя. Проверка после правок (она встроена в тест): Σ `ad` по `ozFinRows` за окно
  = расход кабинета в пирамиде, Σ `revenue` = выручка кабинета.
  · В строке товара и в подсказке KPI расход разложен на «факт XWAY» и «ставка N%» —
    иначе обе цифры читаются одинаково, а решения по ним разные.
  · Склейка БЕЗ своей кампании даёт ДРР ровно 4,2% (это и есть ставка) — хороший чек.
- **ПЕРИОД — ДНИ ВЫГРУЗОК РАСХОДА** (`ozAdWindow()` = границы `adSpend.parts`). Расход и выручка
  обязаны считаться по ОДНИМ И ТЕМ ЖЕ дням, иначе ДРР бессмыслен. На ВБ это не так (там период
  отбора кандидатов и период выгрузки разные) — не переноси то правило сюда.
- **ОТДЕЛЬНЫЙ РЕНДЕР, а не ветки внутри ВБ-шного** (тот же приём, что у «Главной»): на ВБ вкладка
  про ОТБОР кандидатов (Парето, запас в Москве, потолок CPO), на Озоне — про цепочку ДРР по факту.
  `MP_TABS` пополнился `ads`; `renderAdsTab()` при `_appliedMp==="ozon"` уходит в `renderAdsOzon()`,
  на «Всего» пишет «реклама считается по площадке». ВБ-шные органы управления (`adGroupSel`,
  `adViewSeg`, `adDateRange`, `adExportBtn`) на Озоне прячутся и ОБЯЗАТЕЛЬНО возвращаются
  на ВБ — иначе после переключения вкладка остаётся без сегмента видов.
- Клик по склейке в пирамиде фильтрует таблицу (`_ozAdTagFilter`, ветка `oztag` в обработчике
  `#adDrrLevels`). В таблице у каждого товара ДВА ДРР рядом: свой и своей склейки.
- Сверка при внедрении: браузер дал расход кабинета 55 370 ₽ (факт 20 040 + ставка 35 330)
  при выручке 907 802 ₽ → ДРР 6,1%, и это ровно та реклама, что уходит в прибыль Озона
  за тот же день; 0 ошибок консоли, возврат на ВБ вкладку не ломает.
- **ОДИН ДЕНЬ — НЕ ВЕРДИКТ.** Замер 17.09 (день включения): выручка восьми склеек 694 880 ₽/дн
  против 762 469 ₽/дн за 10–16.09 (−9%) при фоне всей площадки −11%. То есть эффекта пока
  не видно, и это нормально: реклама даёт заказы и в следующие дни. Решения по ставкам —
  не раньше недели накопленных выгрузок.
- **НА ОЗОНЕ 10% — ЭТО МИНИМУМ, КОТОРЫЙ НАДО ДЕРЖАТЬ, А НЕ ПОТОЛОК. СМЫСЛ ОБРАТНЫЙ ВБ-шному.**
  Правило продавца 18.09.2026, дословно: **«на озоне у меня иу и там задача держать минимум
  10% дрр для поддержания соинвеста»**. То есть провал НИЖЕ 10% — это риск потерять соинвест,
  а не сэкономленные деньги. На ВБ ровно наоборот: 7% — потолок, и чем ниже, тем лучше.
  · Константа одна — `OZ_DRR_GOAL=10`, бейдж `ozDrrBadge()`: `v>=10` → зелёный, ниже → жёлтый,
    расход без продаж → красный. **Не подставляй сюда `AD_DRR_GOAL`** (на ВБ цель 7% и она
    потолок) и **не сравнивай через `<=`** — площадки судят по разным линейкам И В РАЗНЫЕ СТОРОНЫ.
  · **ВТОРОГО ПОРОГА НЕТ, И ЗАВОДИТЬ ЕГО НЕЛЬЗЯ.** Я сам придумал «хорошо ≤8%» по аналогии
    с ВБ — продавец снял в тот же день: «8 — это не говорит о том, что всё хорошо». Верхней
    границы он тоже не задавал; пока её нет, не изобретай.
  · Подпись над пирамидой (`#adDrrCap`) своя у каждой площадки — её ставит рендер, а не разметка.
  · Замер 17.09 по этому правилу: кабинет Ozon **6,1% — НИЖЕ минимума**; держат его только
    «Бюджетные 3» (14,7%) и «Land Rover» (10,6%), все остальные склейки от 4,2% до 8,0%
    (4,2% = склейка вообще без своей кампании, там работает только ставка из юнитки).
    То есть рекламу на Озоне надо ДОБАВЛЯТЬ, а не резать.

### БАЗА ВЫРУЧКИ ОЗОНА: МЕХАНИЗМ ЕСТЬ, НО ОН ВЫКЛЮЧЕН (`applied:false`)
**ИТОГ 08.09.2026: расчёт Озона идёт по «Предельной цене» на ВСЕЙ истории. Не включай
обратно без финотчёта Ozon.** Продавец сначала попросил считать с 31.08 по-новому
(«не нужно пересчитывать всю историю, давай только поменяем с выхода обновления»), я включил —
и он же поймал ошибку: **«фактически у меня не поднялась комиссия, я не скинул цену и т.д.,
по факту ничего не изменилось в плане экономики, но по твоим цифрам я в ноль вышел либо
в минусе»**. Возражение оказалось верным, проверка это подтвердила:
· **Скидка покупателю была и ДО перелома**: 24.08 — 71,0%, 25.08 — 70,0%, 26.08 — 70,7%,
  27.08 — 71,2%, 28.08 — 68,7%, 29.08 — 71,4%, 30.08 — 71,0% от цены продавца.
· **А воронка Ozon в те же дни считала выручку по ПОЛНОЙ цене**: июль — 99,8% от нашего ряда
  по среднему чеку, 01–30.08 — 98,7%. С 31.08 — 73,1%.
· Значит покупатель платил ~72%, а Ozon засчитывал продавцу 100% — **ровно механика соинвеста**
  («Озон даёт соинвест за свой счёт», 04.09.2026), и 31.08 в ней изменилась только ВИТРИНА
  аналитики, а не деньги.
· **Проверка здравым смыслом, которая всё решает**: если бы продавец реально получал 72%,
  июль был бы −1 043 225 ₽ (−9,7%), август −459 617 ₽ (−1,7%), вся история −2 191 496 ₽ (−4,0%).
  Это не прошло бы мимо расчётного счёта и 1С — а бизнес работал.
· Урок на будущее: **смена цифры в отчёте площадки без единого реального изменения
  (комиссия, цена, тариф) — повод перепроверить МОДЕЛЬ, а не пересчитывать бизнес.**

Механизм оставлен рабочим и выключенным — включится одной командой, когда придёт финотчёт:
· `ozon.meta.revBase.applied` — **применять или нет**. `false` (сейчас) = вся история по
  «Предельной цене»; `history[]` при этом хранится как ЗАМЕР, а не как правило.
  Переключатели: `node scripts/ozon-revbase.cjs off` / `on`.
· Если включить — правило работает как у процента выкупа ВБ: новая база действует
  С ДАТЫ ЗАПИСИ И ВПЕРЁД, прошлые дни не пересчитываются.
· `ozon.meta.revBase.history[]` — записи `{from, k, kMoney, measuredFrom, measuredTo, days,
  funnelRub, funnelQty, seriesRub, seriesQty, builtAt}`, ведёт **`scripts/ozon-revbase.cjs`**
  (`list` / `measure [--from дата] [--win от до]` / `set <k> --from дата`). **Запись с той же
  датой НЕ перезаписывается** — иначе прошлое поехало бы; новый замер кладётся своей датой.
· **k считается ПО СРЕДНЕМУ ЧЕКУ**, а не делением сумм: `k = (воронка ₽ / воронка шт) ÷
  (ряд ₽ / ряд шт)`. У воронки и ряда чуть разное число штук (разные срезы выгрузок), и
  деление сумм смешало бы разницу в цене с разницей в штуках. Замер 31.08–06.09 (7 дн.):
  **k = 73,06%** по чеку, 73,35% по суммам — сходятся.
· **ПЕРЕСЧЁТ ДЕЛАЕТСЯ ОДИН РАЗ ПРИ ЗАГРУЗКЕ, ПРЯМО В `orderSeries.money`** (`applyOzRevBase()`
  в `hydrateData`, ДО `buildMpDatasets`), а не внутри `ozFinRows`: ту же карту читают
  «Динамика» (`mpMoney`) и «Всего» (`combinedMoney`), и пересчёт только финансов развёл бы
  график заказов в рублях с прибылью и с воронкой. Заодно **восстановлен инвариант
  «Σ ordersSum воронки = orderSeries.money»**, сломавшийся 31.08: было 31.08 — ряд 1 317 824
  против воронки 985 254 (−33%), стало 962 805 против 985 254 (2,3%); 03.09 — 1,0%.
  Элемент `[2]` («оплачено покупателем») НЕ трогаем. Флаг `moneyRebased` даёт идемпотентность.
· **Снимок хранит СЫРЫЕ суммы по «Предельной цене»**, коэффициент лежит отдельно — поэтому
  правило можно поменять без перезаливки заказов. `ozon-build.cjs` переносит `revBase`
  в meta (тот же капкан, что с `terms`) и предупреждает, если правила нет.
· Дубль `revK()` — в `scripts/ozon-finance.cjs`, **держи синхронно с `ozRevK()`** в index.html.
· В строке свежести «Главной» на Озоне печатается, с какой даты и по какой базе считается —
  цифры до и после несопоставимы, молча их смешивать нельзя.
· **ЧТО БУДЕТ, ЕСЛИ ВКЛЮЧИТЬ** (замер 08.09.2026, k = 73,06%): май/июнь/июль без изменений,
  в августе меняется только 31.08 (5 118 186 → 4 930 852 ₽), сентябрь 01–07.09
  (1 242 488 ₽, маржа 15,5%) → 8 946 ₽, маржа 0,1%, вся история 9 465 092 → 8 044 212 ₽,
  маржа 12,5% → 11,0%. **Текущие (действующие) цифры — по «Предельной цене»:** вся история
  9 465 092 ₽ / 12,5%, август 5 118 186 ₽ / 14,2%, сентябрь 01–07.09 1 242 488 ₽ / 15,5%.
· ТОЧКА БЕЗУБЫТКА по k: сентябрь 72,9% · август 75,3% · июль 85,5% · вся история 78,1%.
  То есть при k = 73% в минус уходит всё, кроме сентября, который садится ровно на границу.
· **Расчёт берёт `money[...][0]`**; при включённом правиле рескейл применяется к самой карте
  в `applyOzRevBase()`, поэтому потребители (финансы, «Динамика», «Всего») не правятся.
· Коэффициент описывает, КАК OZON ПОКАЗЫВАЕТ продажи, и НЕ доказывает, сколько продавец
  получает на счёт. Решает только финотчёт Ozon. **Проверка по нему — по ЦЕНЕ ЗА ШТУКУ**
  (у отчёта и у нашего ряда разные границы периода): август даёт 3 322 ₽/шт по «Предельной
  цене» против 2 427 ₽/шт по новой базе — какая из двух в колонке «Цена реализации», та
  и верна.

### Офлайн-инструменты Озона (`scripts/`)
- **ТРИ КАПКАНА `ozon-build.cjs` (закрыты, не открывай обратно):**
  · **`meta.terms` — ТАРИФ ИУ 2026 — терялся при каждой пересборке** (инцидент 07.09.2026).
    Блок переписывает `RD.ozon` целиком, а `terms` кладёт другой скрипт (`ozon-finance.cjs`),
    и в списке переносимых полей его не было: заливка ОДНИХ ОСТАТКОВ стёрла тариф, залитый
    04.09. Пропажа не даёт ни ошибки, ни пустой таблицы — `ozTerms()` возвращает null, и
    прибыль Озона на «Главной» просто исчезает. Теперь переносится (`terms: prevMeta.terms`),
    а при его отсутствии скрипт печатает предупреждение со ссылкой на `ozon-finance.cjs`.
    **Любое новое поле в `ozon.meta`, которое кладёт не этот скрипт, добавляй в перенос.**
  · **Накопитель `ozon-orders.json` эфемерный** (gitignored) и не переживает пересоздание
    контейнера. После потери он содержит ТОЛЬКО дни свежей партии, и пересборка ряда стирала
    бы всю прежнюю историю заказов Озона (реальный риск: в накопителе 07–23.08, в снимке
    03.05–06.08). Теперь дни, которых в накопителе НЕТ, переносятся из снимка (`backfilled`),
    дни, которые есть, всегда берутся из накопителя — он свежее. Проверка длины рядов, иначе
    снимок не пишется. **% выкупа считается только по дням накопителя** — за старые дни
    `byDateArtNet` недоступен, и окно по ним дало бы 0% выкупа.
  · **`--reuse` без файла остатков обнулял остатки Озона.** Обнуление `ozStock`/`ozTransit`
    теперь только при переданном отчёте остатков (он полный снимок, товар вне него = 0).
    Без отчёта остатки остаются как были — иначе заливка одних ЗАКАЗОВ молча стирала
    остатки и «в пути» (27 337 и 3 160 шт → нули).
- **`ozon-build.cjs <файл_Июль.xlsx> [остатки.xlsx] [дата]`** — собирает `REAL_DATA.ozon`
  (каталог из листа «Июль»: Артикул+Наименование+Вид товара; заказы из `decrypted/ozon-orders.json`;
  остатки из отчёта «Остатки», лист «Товары») и вписывает в `decrypted/wb-data.js`. Дальше `encrypt.cjs`.
- **Остатки Озона**: лист «Товары» отчёта «Остатки на складах» (двухэтажная шапка, данные с 5-й строки).
  Колонки ищутся **по имени** (индексы плывут между версиями), с фолбэком на индексы:
  0 Артикул · «Доступно к продаже» (→ `ozStock`) · 6 «Дней до конца остатка» · 7 «Среднесут. продажи 28д».
  **`ozTransit` = «В заявках на поставку» + «В поставках в пути»** — это две непересекающиеся стадии
  под общей шапкой «В пути на склад Ozon»: заявка создана / партия физически едет. Продавец считает
  созданную заявку уже «направленным на Озон» товаром, поэтому суммируем. НЕ путать с `ozIncoming`.
  **Один артикул может идти НЕСКОЛЬКИМИ строками** — разные карточки (SKU) под одним арт. поставщика:
  «Маркируемый» и «Уценка, Маркируемый». Остатки по строкам **складываются** (`+=`), а не присваиваются:
  пустая строка уценки шла последней и затирала реальный остаток нулём (арт. 493370: 25 шт и 936 в
  заявках → 0). Не возвращай присваивание. `ozon.meta.stockDate` — дата снимка,
  `ozon.meta.buyoutAll` — общий % выкупа. Расчёт оборачиваемости Озона — ниже (`renderTurnoverOzon`).
- **`ozon-funnel-build.cjs <отчёт.xlsx>`** — запекает **воронку Озона** в `REAL_DATA.ozon.funnel`.
  Источник — отчёт «Аналитика → По товарам, подённо» (лист «По товарам», многоуровневая шапка,
  секция «Воронка продаж»). Колонки по имени (в под-строке шапки): «Показы всего»→impressions,
  «Посещения карточки»→cardViews, «Добавления в корзину всего»→addCart, «Заказано товаров»→ordersQty,
  «Доставлено товаров»→**buyoutQty** (аналог «Выкупили» ВБ — «доставлено=получил», дозревает),
  «Отменено товаров»→cancelQty, «Заказано на сумму»→ordersSum. Ключ — «Артикул продавца» (=supplierCode);
  берём только наши (∈ supplierCode ВБ). **Формат чисел Озона: запятая=разделитель тысяч, точка=десятичная.**
  Записи — та же форма, что FUNNEL_CACHE. `buyoutSum` не даётся отчётом (=0). Дальше `encrypt.cjs`.
  В UI воронку Озона отдаёт `activeFunnel()` (см. ниже), последняя ступень подписана «Доставлено».
  **Мерж — по ключу «дата + артикул», НЕ по дню целиком.** Продавец дробит один большой отчёт
  ПО СТРОКАМ (шапка та же, дни те же, товары разные), и замена дня затирала бы уже загруженную
  часть. С таким ключом части складываются, повторная заливка ничего не двоит, исправленная
  выгрузка перекрывает старые цифры. Скрипт печатает «новых / перезаписано». При переходе на этот
  мерж схлопнулись 16 задвоенных строк, оставшихся от прежней логики (01–20.07: показы
  2 552 672 → 2 537 224) — это исправление двойного счёта, а не потеря.
- **`ozon-ingest-orders.cjs <файлы «Заказы»…>`** — разбирает отчёты Озона «Заказы» →
  `decrypted/ozon-orders.json` (`byDateArt/byDate/byDateNet/perArt/perArtNet/statuses`).
  Колонки: «Принят в обработку» (дата), «Артикул» (=арт.поставщика), «SKU» (озон), «Количество», «Статус».
  **Форматы дат РАЗНЫЕ по файлам**: csv — Д/М, xlsx — М/Д (определять пофайлово, иначе даты съедут).
  Дедуп по «Номер отправления + SKU» (не по дате). «Наши» — артикул ∈ supplierCode. Заказано = все
  статусы (gross); net = без «Отменён». Для окна выкупа нужен `byDateArtNet` (net по дата+артикул).
- **% ВЫКУПА ОЗОНА В ФИНАНСАХ — «ОТ ЗАКРЫТЫХ ЗАКАЗОВ», КАК НА ВБ** (`scripts/ozon-buyout-window.cjs`,
  решение продавца 08.09.2026: «давай обновим логику на озоне и сделаем её как на вб»).
  `% выкупа = ДОСТАВЛЕНО ÷ (ДОСТАВЛЕНО + ОТМЕНЕНО)`. **Не путать с `meta.buyoutAll`** —
  та считается в `ozon-build.cjs` как `не отменён ÷ все заказы` по статусам выгрузки «Заказы»
  и даёт **92,1%**, потому что засчитывает как проданный весь едущий товар: на 08.09 из 12 765
  заказов накопителя 4 761 «Доставляется», 1 213 «Ожидает отгрузки», 972 «Ожидает сборки» —
  больше половины ещё не закрылись. Правильная цифра ~80% и она устойчива на независимых окнах:
  июль 80,3% · 01–14.08 79,3% · 15–28.08 80,7% · 01–28.08 **80,0%** (первый замер).
  · **ПРАВИЛО ПРИМЕНЕНИЯ ТО ЖЕ, ЧТО У ВБ**: `meta.buyoutWin.history[]`, замер действует
    **с даты замера (`builtAt`) и вперёд**, прошлое не пересчитывается; дни раньше ПЕРВОГО
    замера считаются по нему же (другого источника для них нет — поэтому первый замер накрывает
    всю историю). Повторный замер того же окна НЕ перезаписывает запись.
    Хелперы: `boFor()` в `ozon-finance.cjs`, `ozBuyoutFor()` в index.html — **держи синхронно**.
  · **НА МАРЖУ % ВЫКУПА НЕ ВЛИЯЕТ ВООБЩЕ** — он умножает и выручку, и себестоимость
    (`rev=ordRub*bo`, `cogs=qty*bo*cost`), поэтому `cogs/rev` не меняется. Меняется масштаб
    в рублях: вся история 9 465 092 → **8 225 554 ₽** (−1,24 млн), маржа как была 12,5%;
    август 14,2%, июль 8,3%, сентябрь 15,5% — все до десятой доли те же.
  · Источник замера — отчёт «Аналитика → По товарам» **за период** (подённый тоже читается,
    дни суммируются); свой % у товара при **10+ ЗАКРЫТЫХ** заказах, иначе общий.
    Есть режим `--from-funnel` — замер по воронке, уже лежащей в снимке.
  · `ozon-build.cjs` переносит `buyoutWin` в meta (тот же капкан, что с `terms` и `revBase`).
- **`meta.buyoutAll` (старая формула) остаётся фолбэком и питает ОБОРАЧИВАЕМОСТЬ** —
  считается в `ozon-build.cjs`: `net/gross` за **14 дней, заканчивающихся за 7 дней
  до последней даты** (HOLD=7, WIN=14). Последнюю неделю пропускаем — заказы ещё в пути («Доставляется»),
  выкуп не «дозрел» и завышается. По товару (при <10 заказов в окне — общий по магазину). Окно и общий %
  лежат в `ozon.meta.buyoutWindow`/`buyoutAll`; при малой истории (<8 дней) берётся весь период.
  **Финансы её больше НЕ используют** (см. пункт выше). `catalog[].ozBuyout` остался ТОЛЬКО
  запасным значением — с 21.09.2026 он не применяется ни к одному товару (см. следующий пункт).
- **ВЫКУП ОЗОНА ВЕЗДЕ БЕРЁТСЯ ИЗ ЗАМЕРА, КАК У ВБ** (решение продавца 21.09.2026: «процент
  выкупа давай по озону брать так же как и на вб по точно такой же логике»).
  **Что было не так.** Формула, окно и правило «с даты замера и вперёд» на площадках и так
  совпадали, но ИСТОЧНИК различался: `buyoutInfo("ozon")` падал сразу в `buyoutFromFunnel` —
  окно по ПОДЁННОЙ воронке. А воронка заморожена на момент выгрузки: доставка дозревает задним
  числом, и день, выгруженный сразу, навсегда остаётся с заниженным выкупом. Отсюда разъезд:
  финансы Озона шли по замеру **79,9%**, а оборачиваемость и подсорт — по воронке **75,3%**,
  при ОДНОМ И ТОМ ЖЕ окне 31.08–13.09 и одной формуле. Подсорт из-за этого занижал потребность.
  · Теперь в `buyoutInfo` у Озона своя ветка ПЕРЕД фолбэком на воронку: берётся ПОСЛЕДНИЙ замер
    из `ozon.meta.buyoutWin.history` (вместе с его `bySku` — свой % у товара). Последний, а не
    по дате: оборачиваемость и подсорт смотрят ВПЕРЁД, им нужен действующий процент. В финансах
    по-прежнему `ozBuyoutFor(дата)` — там правило «с даты замера и вперёд», прошлое не трогаем.
  · **Структура у площадок разная, не перепутай**: у ВБ `meta.buyoutWin` держит последний замер
    ПЛЮС `history`, у Озона — ТОЛЬКО `history` (верхнего уровня `all`/`bySku` нет вовсе).
  · Воронка осталась фолбэком: нет замеров — работает как работала.
  · **Дубль в `scripts/resupply-report.cjs`** (`ozMeasured()` рядом с `wbMeasured()`) — держи
    синхронно, иначе скрипт разойдётся с дашбордом.
  · Замер после правки: обе вкладки Озона дают 79,9%, свой % у 81 товара, общий у 56;
    подсорт на Озон 1 539 → 1 610 шт, «нечем закрыть» 400 → 242 шт. 0 ошибок консоли.
- **Простая оборачиваемость — ЕДИНАЯ для ВБ и Озона** (`simpleTurnList` + `renderTurnoverSimple`).
  Старую сложную ВБ-модель (`computeTurnover` с редакт. сроком/буфером/«Прибудет») **заменили** по просьбе
  пользователя (сам `computeTurnover`/`turnoverList`/`exportTurnoverCSV` остались в коде, но НЕ вызываются).
  Отличаются только источники полей: **ВБ** — `wbStock` · `pending.qty` (в пути, дату игнорируем) ·
  `buyoutPct14d` · `containerQty` · `productionStatus`(«На вывод»); **Озон** — `ozStock` · `ozTransit` ·
  `ozBuyout`, размер контейнера берётся из связанного ВБ-товара (кратность общая ВБ/Озон).
- **% ВЫКУПА ДЕЙСТВУЕТ С ДАТЫ ЗАМЕРА И ВПЕРЁД — ПРОШЛОЕ НЕ ТРОГАЕМ** (правило продавца
  07.09.2026, дословно: «сегодня я тебе скинул файл и мы обновили процент выкупа, и до
  следующего такого файла ты просто берёшь этот процент и всё; как только я скидываю новый
  и мы его обновляем, старые расчёты ты не трогаешь, а просто конвертируешь заказы в выкуп
  все последующие, пока мы опять не пересчитаем»).
  До этого в снимке жило ТОЛЬКО последнее окно, и `model-finance.cjs` применял его ко ВСЕМУ
  периоду оценки — каждый еженедельный отчёт задним числом переписывал уже посчитанные недели
  (замер 17–30.08 поднял весь август с 75,3% до 79,5%: +1 385 368 ₽ выручки и +327 969 ₽
  прибыли из воздуха). Продавец поймал это сам.
  **Механика, которую он объяснил:** выкупы доезжают и заносятся ВБ тем днём, когда был ЗАКАЗ,
  поэтому один и тот же период, измеренный позже, показывает процент выше. Подставлять свежий
  процент в старые дни = завышать прошлое, тем более что фин. отчёты приходят раз в неделю.
  · **КЛЮЧ — ДАТА ЗАМЕРА (`builtAt`), А НЕ ДАТЫ ОКНА ВНУТРИ ОТЧЁТА.** Окно 17–30.08, замеренное
    07.09, начинает действовать с 07.09, а не задним числом с 17.08. Моя первая версия резала
    историю по датам окна — продавец поправил: это его учёт, а не деталь реализации. Не «чини»
    обратно на разбивку по окнам.
  · `meta.buyoutWin.history[]` — все замеры `{from,to,all,moneyAll,bySku,builtAt}`, ведёт
    `wb-buyout-window.cjs`. **Повторный замер того же окна НЕ перезаписывает запись** — первая
    остаётся, иначе прошлое опять поехало бы.
  · `model-finance.cjs`: `winFor(d)` берёт ПОСЛЕДНИЙ замер с `builtAt <= d`; дни раньше самого
    первого замера считаются по нему же (другого источника для них нет). `mbFor`/`boFor` — то же
    для денежного и штучного процента. Печатается разбивка «с какой даты по какую какой процент».
  · Ручной `--buyout` перекрывает историю и применяется ко всем дням — только для прикидок.
  · Проверка после правок (прогон 07.09): замеры 01.09 → 75,3% и 07.09 → 79,5% дают
    03.08–06.09 → 75,3%, а 07.09 и позже → 79,5%. Весь текущий период оценки идёт на 75,3%;
    новый процент подхватится на данных, которые придут после 07.09.
- **ИСТОЧНИК % ВЫКУПА ВБ — `scripts/wb-buyout-window.cjs`, а НЕ подённая воронка.** Скрипт читает
  **сводный** отчёт аналитики за период и пишет `REAL_DATA.meta.buyoutWin`.
  **ПРАВИЛО ПРОДАВЦА (01.09.2026, файл приходит КАЖДЫЙ ПОНЕДЕЛЬНИК):**
  · окно = **14 дней, пропустив последние 7** (01.09 → берём 10–23.08, свежую неделю не трогаем:
    товар ещё едет);
  · **% выкупа = ВЫКУПЛЕНО ÷ (ВЫКУПЛЕНО + ОТМЕНЕНО)** — «от ЗАКРЫТЫХ заказов». Ровно так
    считает сам ВБ в колонке «Процент выкупа», и эту цифру продавец видит в кабинете.
    Сверка на файле 10–23.08: `1829 ÷ (1829+577) = 76.0%` при «Процент выкупа = 76» в листе
    «Фильтры»; предыдущий период `1942 ÷ (1942+1202) = 61.8%` при 62 в файле.
  **ПОЧЕМУ НЕ «÷ ВСЕ ЗАКАЗЫ» (прежняя формула).** Заказ уходит в одно из двух состояний —
  выкуп или отмена; пока он «в пути», исход неизвестен. Старая формула считала все «в пути»
  невыкупленными и занижала цифру тем сильнее, чем свежее окно: на том же файле 62.9%
  против 76.0% — 13 п.п. **На зрелом окне обе формулы совпадают** (при «в пути» → 0 имеем
  заказано = выкуп + отмена), так что это снятие смещения, а не смена смысла. Именно из-за
  старого смещения приходилось городить проверку зрелости и порог `MATURE_OPEN_PCT`;
  теперь окно берётся то, что в файле, а «в пути» печатается справочно.
  **Не путать** с «от неотменённых» (`b ÷ (заказано − отменено)` = 78.5% — там в знаменателе
  ещё не доехавшие) и с `buyoutPct14d` из каталога (88.4%).
  **ДЕНЕЖНЫЙ выкуп — на той же базе**: `выкуплено ₽ ÷ (выкуплено ₽ + отменено ₽)` (75.3%),
  идёт в `model-finance.cjs`. Прежние `rb/ro` давали 61.9% и занижали оценку выручки.
  **МЕРЖ, А НЕ ЗАМЕНА `bySku`**: отчёт бывает отфильтрован по одному предмету (лист называется
  «Пушкары», а не «Товары» — скрипт ищет лист по содержимому). Полная перезапись стёрла бы
  свой % выкупа у остальных категорий, а он теперь идёт в ДРР по уровням и в потолок CPO.
  Новые значения перекрывают старые, ненайденные сохраняются; счётчики `bySkuFresh`/`bySkuKept`.
  Свой % у товара — при **10+ ЗАКРЫТЫХ** заказах (выкуп+отмена), иначе общий.
- **РАСХОД НА РЕКЛАМУ — ТРИ ИСТОЧНИКА, И ВСЕ ТРИ ИДУТ В ПРИБЫЛЬ** (`fdbRefreshAdsCache`).
  1) `BAKED_ADS` — «История затрат» ВБ (29.06–02.08, дальше отчёты продавцу закрыли);
  2) браузерные загрузки IndexedDB (легаси);
  3) **`adPerf.parts` — выгрузки XWAY** (`adRowsFromPerf`).
  Третий слой добавлен 01.09.2026 по вопросу продавца «я гружу рекламу — ты её считаешь?».
  Не считал, и это была дыра: «История затрат» кончилась 02.08, `model-finance.cjs` шёл
  с рекламой = 0, а по XWAY за 26–31.08 реально потрачено 503 249 ₽ — прибыль августа была
  завышена ровно на эту сумму. Приоритет: факт «Истории затрат» > загрузки > XWAY, XWAY
  берётся ТОЛЬКО за даты, которых нет в первых двух (иначе двойной счёт).
  **Сумма за период фактическая, разнесение по дням РАВНОМЕРНОЕ** — выгрузка даёт итог за
  период, а не по дням; на неделю/месяц это не влияет, внутри периода день приблизителен.
  Двойного счёта с оценкой нет: в `MODELED_FINANCE` поля `ads` нет вовсе, а `FIN_MONEY`
  рекламу не содержит — она всегда приходит из `adAggregate`.
  **ЭФФЕКТ ВИДЕН СРАЗУ:** неделя 24–30.08 с рекламой 410 542 ₽ даёт прибыль −12 689 ₽
  вместо +398 тыс, то есть недели без загруженной рекламы (03–23.08, маржа ~19%) завышены.
  Проси у продавца выгрузки XWAY за пропущенные дни — без них август несопоставим.
- **`MODELED_FINANCE` — ОЦЕНКА финансов за даты после факта** (`scripts/model-finance.cjs`).
  Отчёты «О реализации» продавцу закрыли, поэтому свежие недели считаются из заказов:
  `заказано ₽ × денежный выкуп` → выручка, `× payoutRate` → «итого к оплате», минус
  себестоимость (штуки = заказы × выкуп по sku, цена — `costAt` на дату) и минус реклама
  (аргумент `--ads`, разносится пропорционально выручке дня). Живёт в `wb-reports.js` рядом
  с `BAKED_FINANCE`, переносится encrypt/decrypt, в `unlock()` кладётся в `window.MODELED_FINANCE`.
  **Каждая строка помечена `est:true`, и дашборд обязан об этом говорить:** `fdbRefreshFinanceCache`
  подмешивает оценку только за даты, которых НЕТ в факте (факт всегда сильнее), выставляет
  `financeEstFrom/To`; строка свежести Главной пишет «факт по X, дальше ОЦЕНКА», недели в
  «Каждая неделя отдельно» помечаются «оценка» (`r.est`). Не давай оценке молча слиться с фактом.
  Денежный выкуп берётся из `meta.buyoutWin.moneyAll` (выкуплено ₽ ÷ заказано ₽), а НЕ штучный:
  чек выкупленного отличается от чека заказанного, на реальных данных 66.7% против 75.0% —
  это 12% выручки. `wb-buyout-window.cjs` кладёт его в снимок, чтобы еженедельный прогон
  не зависел от ручного аргумента `--buyout`.
  Когда придёт настоящий отчёт за этот период — `node scripts/model-finance.cjs --clear`.
  **Проверка на реальных данных 24.08:** оценка за 03–16.08 дала 8 134 016 ₽ против 6 650 913 ₽
  в недозревшем сводном отчёте — разница 22% при 22.1% заказов «ещё в пути», средний чек
  2 277 ₽ против 2 221 ₽. То есть модель попадает в то, что отчёт покажет после дозревания.
- **`REAL_DATA.meta.model` — финансовые коэффициенты, снятые с ФАКТА** (`scripts/calibrate-model.cjs`).
  **Продавцу закрыли доступ к финансовым отчётам 24.08.2026 — новых данных не будет, пересчитать
  коэффициенты не из чего.** Поэтому они сняты один раз с 5 недель (29.06–02.08) и живут в снимке.
  Что надёжно: `payoutRate` = «итого к оплате» ÷ выручка = **62.0%** (по неделям 61.9–62.6%,
  ошибка обратного теста 0.3%) — структурная величина; `avgTicket` 1326 ₽/шт (1280–1391).
  Что НЕ надёжно: `soldPerOrdered` 91.5% ±8.5 п.п. — зависит от тренда (продажи недели закрываются
  заказами прошлых недель, на падении задран, на росте занижен; в длинном окне должен сходиться
  к % выкупа 75%) — годится на месяц, не на неделю. `adRate` 1.1–8.2% — это решение продавца,
  а НЕ коэффициент: любой прогноз обязан брать рекламу вводом.
- **% ВЫКУПА — ОКНО «ПРОПУСТИТЬ НЕДЕЛЮ, ВЗЯТЬ ДВЕ»** (просьба продавца 24.08, одно правило на
  обе площадки). `BUYOUT_HOLD=7`, `BUYOUT_WIN=14`, `BUYOUT_MIN_ORD=10`: окно = 14 дней, кончающихся
  за 7 дней до последней даты воронки (последняя 23.08 → окно 03.08–16.08). Свежая неделя
  выброшена сознательно — товар ещё в пути, выкуп по ней занижен. Источник — воронка:
  `ordersQty`→`buyoutQty` (у Озона последняя ступень «Доставлено»); свой % у товара с 10+ заказами
  в окне, иначе общий по магазину. Хелперы `buyoutFromFunnel`/`buyoutInfo`/`buyoutOf` в index.html,
  дубль в `scripts/resupply-report.cjs` — **держи синхронно**. Кеш `_buyoutWin` сбрасывается
  в `hydrateData` и `fdbRefreshCache`.
  **ПРОВЕРКА ЗРЕЛОСТИ обязательна, и она по ФИЗИКЕ, а не по тренду.** Выкуп в снимке заморожен
  на момент выгрузки и задним числом НЕ обновляется: день, выгруженный в тот же день, навсегда
  остаётся с нулевым выкупом. Признак — день с 20+ заказами и выкупом <15% (такого не бывает).
  Хоть один такой день в окне → окну не верим, берём прежний источник (`buyoutPct14d` у ВБ,
  `ozBuyout` из статусов заказов у Озона), и `turnHowTo` пишет, что применилось РЕАЛЬНО.
  Первая версия проверки сравнивала начало окна с хвостом — **не работает**: у ВБ мёртвые дни
  оказались в НАЧАЛЕ окна, тренд выглядел растущим, и заниженные 40% проходили как здоровые.
  **Состояние на 24.08:** Озон — окно зрелое, применено (70.5%); ВБ — НЕ зрело (03–06.08 без
  выкупа, окно даёт 40% против 88% из каталога), работает `buyoutPct14d`. Чтобы окно заработало
  на ВБ, нужна ОДНА свежая выгрузка аналитики за период окна: выгруженная сегодня, она содержит
  дозревший выкуп (именно поэтому у Озона всё сошлось — там был один файл на весь диапазон).
- **Паллеты округляются ВВЕРХ** (`takePallets` в index.html, `take` в resupply-report.cjs — оба
  `Math.ceil`). Раньше обычный подсорт округлялся ВНИЗ («неполную паллету не везём»), и товар
  систематически не дотягивал до цели: при паллете 108 шт и потребности 163 шт уезжала одна
  паллета, покрытие выходило 24 дня вместо 30. Флаг `roundUp` у стартовых партий сохранён для
  пометки «не менее LAUNCH_QTY», но на арифметику больше не влияет. Не возвращай `Math.floor`.
- **Себестоимость ОБЩАЯ для ВБ и Озона.** Ведётся только в `RD.catalog` (ВБ); на Озоне цена
  подтягивается в `rebuildCatalogMaps` через `wbBySup[артикул Озона]` — вместе с `costHistory`,
  поэтому `costAt()` на Озоне тоже знает историю. Проверено: у всех 143 Озон-товаров цена есть,
  `costOf`/`costAt` на обеих площадках дают одно и то же. `update-cost.cjs` заливает один файл —
  расчёты на двух площадках обновляются сразу, отдельного шага для Озона НЕ нужно.
  Формула: ср.заказов **7 дн** × выкуп = продаж/день; **«хватит дней» = (остаток + в пути) ÷ продаж/день**
  (в пути складывается с остатком, показан акцентом: «354 **+1000 в пути**», «18 **→ 67**»).
- **ПОКРЫТИЕ ВБ = ОСТАТОК НА СКЛАДАХ WB + СВОЙ FBS-ЗАПАС** (решение продавца 27.08.2026,
  `wbFbsBySup()` в index.html, дубль `wbFbsFree` в `resupply-report.cjs`). Wildberries продаётся
  и по FBO (пока на складах WB что-то лежит — «что будет потом, непонятно», поэтому считаем),
  и по FBS — товар уходит покупателю прямо с наших складов. **Ловушка: склад Нск-1 одновременно
  снабжает Ozon**, и один и тот же товар нельзя засчитать дважды. Правило: сначала закрывается
  потребность Озона (сперва с Евросиба, недостача — с Нск-1), и только НЕЗАНЯТЫЙ остаток Нск-1
  плюс вся Москва идут в покрытие ВБ (Москва на Ozon не отгружает, поэтому входит целиком;
  Евросиб в покрытие ВБ НЕ входит — по FBS с него не торгуют). В подсорте то же самое считается
  по фактической раскладке (`whMsk + max(0, whNsk1 − usedNsk1)`), потому что там Озон уже разобран
  паллетами. Замер 27.08: FBO 41 228 + FBS 37 120 = 78 348 шт покрытия при 279 продажах/дн.
  Кеш `_wbFbsCache` сбрасывается рядом с `_buyoutWin`.
- **ПОКРЫТИЕ ОЗОНА = только то, что уже на площадке или официально едет на неё.** По просьбе продавца
  товар на ЕГО складах в покрытие НЕ входит — раньше «хватит дней» считалось от партии, которая
  ещё не отгружена, и это сбивало. Сейчас: **ВБ** — покрытие = остаток на складах WB (поле `transit`
  у ВБ всегда 0); **Озон** — остаток + `ozTransit` (заявки на поставку + партии в пути, которые
  показывает сам Озон). Не возвращай `pending.qty` / `ozIncoming` в `covered`.
- **Данные о своём складе при этом СОХРАНЕНЫ** и обновляются: `catalog[].pending.qty` (ВБ) и
  `ozon.catalog[].ozIncoming` (Озон) пишет **`update-transit.cjs`** из рабочего файла продавца
  «на оптовых» (вкладки `вб` / `озон` / `в пути` / `в пути 2`; формула: ВБ = `вб.Итог` +
  `в пути.Кол-во вб` + `в пути 2.кол-во вб`, Озон — аналогично своими колонками; ключ везде —
  арт. поставщика = WB `supplierCode` = Озон `sku`; файл — полный список едущего, поэтому старое
  `pending` перед заливкой очищается). Они нужны для будущей механики «куда и сколько отгрузить»
  (цель — держать на площадке минимум 30 дней запаса), а не для расчёта дозаказа.
  **Дозаказ** (`TURN_MIN_DAYS=130`, `TURN_COVER_DAYS=180`): если покрытия < 130 дн — партия = **6 мес продаж**
  = `ceil(продаж/день × 180)`, **округляется ВВЕРХ до контейнера** (текущий остаток НЕ вычитаем).
  Статус: ≥130 «Норма»; <130 «Пора дозаказать» (короткий бейдж — «Дозаказ»); продажи есть, покрытия
  нет «Критично»; продаж нет «Нет продаж». Выкуп ВБ — 14-дневный из каталога; Озон — окно (см. выше).
  Колонки: Остаток(ВБ/Ozon) · Заказов/дн(7д) · %выкупа · Продаж/дн · Хватит,дн · Дозаказать(+конт.) · Заказать(через N дн) · Статус тов. · Статус.
  У Озона под остатком строка «+N в пути» (его собственные поставки); у ВБ такой строки нет — покрытие только по складу WB.
  Селектор «Скорость» скрыт (фикс. окно 7 дн); режим «Всего» — подсказка выбрать площадку.
- **Производственный статус — СПРАВОЧНЫЙ, на расчёт НЕ влияет** (`prodStatus(sku)` → колонка
  «Статус тов.», бейдж `pStatusBadge`, фильтр по колонке `pStatus`, ранг `PSTATUS_RANK`).
  Дозаказ и закуп считаются для **всех** товаров, включая «На вывод»: продавец сам решает,
  заказывать ли выводимый товар (увидев расчёт, он может вернуть его в работу и сменить статус).
  Раньше по ним ставился `status="discontinued"`, дозаказ обнулялся и они выкидывались из CSV закупа —
  **не возвращай это**, была отдельная просьба считать по всем и решать самому.
  **ИСКЛЮЧЕНИЕ — «Подсорт со склада»**: товары «На вывод» из подсорта выкинуты — везти на
  площадку выводимый товар незачем. **НО если он лежит на СВОЁМ складе, он остаётся**
  (просьба продавца 26.08: «если где-то есть остаток — пока считай, надо быстрее распродать»;
  распродать можно только на площадке). Условие: `st[s] && !(warehouse.bySup[s].qty>0)`.
  На остальных вкладках («Запас и темп», дозаказ, закуп) статус не влияет на РАСЧЁТ — выводимый
  товар с остатком получает цель по темпу, чтобы его распродали.
  **НО ВО ВСЁМ ДАШБОРДЕ ОН БОЛЬШЕ НЕ ПОКАЗЫВАЕТСЯ И НЕ СЧИТАЕТСЯ** (просьба продавца 02.09.2026:
  «убери вообще из всех расчётов эти товары … очень слабо верю, что из них мы что-то будем
  продавать, чтобы не сбиваться убери их из общих перечней товаров»). Это ОТМЕНА прежнего
  «показываем всех, решает продавец» — и отменил её сам продавец, так что не «чини» обратно,
  увидев абзац выше. Данные не тронуты: скрытие обратимо одним флагом.
  · **ОДИН ФЛАГ НА ВЕСЬ ДАШБОРД — `showOut`** (по умолчанию `false`, при перезагрузке снова
    скрыто). Плашка `#outBar` рядом с `#focusBar` (над вкладками, `renderOutBar()` зовётся из
    `switchTab`) говорит, сколько кодов скрыто, и возвращает их кнопкой. Не заводи вторых
    флагов и локальных плашек: разные вкладки не должны показывать разное состояние.
  · **ОДНО ОПРЕДЕЛЕНИЕ**: `recOutSups()` (коды 1С, у которых ВСЕ карточки «На вывод») →
    `outSkuSet()` (артикулы обеих площадок) → `isOutSku`/`isOutRow`/`dropOut`. Карта
    `outSkuSet` СВОЯ, а не `catalog1cBySku`: та перестраивается под активную площадку, а
    фильтр обязан работать одинаково везде. Обе кеш-переменные (`_recOutCache`, `_outSkuCache`)
    сбрасываются в `hydrateData` и в `fdbRefreshCache`.
  · **ТОЧКИ ВРЕЗКИ** — по одной на вкладку, новых правил в рендерах не заводить:
    `turnFilteredRows` (Оборачиваемость), `srFilteredRows` (Запас и темп), `adRows` +
    `adBaseRows` (Реклама), `finAggregate` + `finSeriesByDate` + `adAggregate` (Главная,
    Финансы, Аналитика, недели, графики), `fillProductSelect` + `categories()` (перечни).
  · **ДЕНЬГИ НЕ ПРОПАДАЮТ, А ВЫНОСЯТСЯ ОТДЕЛЬНО** (выбор продавца из трёх предложенных
    вариантов). «Главная» и «Финансы» — сверка с ВБ, молча выкинуть часть выручки нельзя.
    Поэтому `finAggregate` считает скрытую часть вторым таким же сводом и кладёт в `agg.out`,
    `adAggregate` копит расход скрытых в `outTotal`, а `renderOutSplit()` печатает под KPI
    строку «живой ассортимент · товар на выводе · всего». **Инвариант, проверять после правок:
    живой + на выводе = всего** — на 02.09 прибыль 10 314 610 + (−7 315 863) = 2 998 747 ₽,
    выручка 68 165 701 + 19 535 340 = 87 701 041 ₽, реклама 2 548 350 + 1 852 363 = 4 400 713 ₽.
  · **ПЕРЕЧНИ ЗАПОЛНЯЮТСЯ ОДИН РАЗ** (`if(!sel.options.length)`), поэтому обработчик плашки
    ОЧИЩАЕТ `turnCat/turnProduct/srCat/finCat/homeCat/dynCat/anCat` — иначе после переключения
    в списке остаётся прежний состав. Заодно сбрасываются `turnColFilters/srColFilters/adColFilters`:
    наборы значений в воронках меняются вместе с выборкой.
  · **ДВА ИСКЛЮЧЕНИЯ, СДЕЛАННЫЕ СОЗНАТЕЛЬНО**: «Разбор товара» (туда приходят с конкретным
    кодом в руках — прятать введённый руками товар бессмысленно) и план машин в подсорте
    (`renderTruckPlan` получает `resupplyRows()` целиком, поэтому выводимый товар со склада
    по-прежнему едет распродаваться).
  · Замер 02.09: Главная 300 → 105 строк, Финансы 300 → 105, Аналитика 300 → 105,
    дозаказ 413 → 111, Запас и темп 190 → 101, кандидаты рекламы 108 → 72,
    перечень товаров 413 → 111, категории 13 → 8; машин в подсорте по-прежнему 2.
  Фильтр — `outSup` в `resupplyRows()` (index.html) и такой же в `scripts/resupply-report.cjs`,
  **держи их синхронными**. Считается по ВСЕМ карточкам кода 1С: если хоть одна НЕ «На вывод» —
  товар живой и остаётся (у одного кода 1С бывает несколько карточек ВБ).
  **Выгрузки «Оборачиваемости» — настоящий .xlsx с ДВУМЯ листами**: «Как считается» (текст
  из `turnHowTo()` + параметры выгрузки: площадка, даты данных, активные фильтры) и лист с цифрами.
  Файл уходит руководству без дашборда, поэтому объяснение обязано ехать вместе с цифрами.
  Пишется своим мини-райтером (`xlsxDownload`/`_zipStore`/`_sheetXml`, zip методом STORE +
  минимальный OOXML, inline-строки, 4 стиля) — **SheetJS не подключаем**, он тянется с CDN,
  который в песочнице недоступен. Функции экспорта сохранили старые имена (`exportTurnoverCSV`
  и др.), но отдают xlsx; кнопка называется «Скачать Excel».
  В листах закупа/месяцев показаны **ВСЕ месяцы, включая декабрь** (с пометкой «не в расчёте»),
  плюс `ПИКОВЫЙ МЕСЯЦ (база)` / `Какие месяцы взяты в среднее` — чтобы можно было проверить,
  от какого именно месяца посчитано. Проверка после правок: скачать файлы через Playwright
  и разобрать их пакетом `xlsx` в node (файл должен читаться как обычный Excel).
  **Блок «Как считается»** (`#turnHowTo` + `renderTurnHowTo()`, стиль `.howto`) — над таблицей,
  **текст обязан описывать ТОЛЬКО выбранную площадку** (`turnHowTo(view,oz,mp)`, третий аргумент —
  `wb`/`ozon`/`all`): продавец читает его как инструкцию и путается, если в одном абзаце и ВБ, и Озон.
  В заголовке стоит «— Wildberries»/«— Ozon». Исключения оставлены СОЗНАТЕЛЬНО, там где вторая
  площадка реально влияет на цифры этой: в подсорте на ВБ — оговорка про приоритет Озона, в закупе —
  предупреждение, что склад/Китай/заказанное общие и выгрузки площадок нельзя складывать.
  Выгрузка подсорта тоже под площадку: `exportResupplyXLSX` строит колонки из массива `cols`
  (на ВБ нет колонок Озона и наоборот), имя файла с префиксом площадки.
  **Лист «Машины» в выгрузке**: строка на код (Склад · Площадка · Машина · Код 1С · Название ·
  Остаток на складе · К отгрузке шт · Паллет), под каждой машиной жирный итог (штуки + паллеты +
  сколько кодов), между машинами пустая строка, в конце направления — «ВСЕГО по направлению».
  Штуки = паллеты × вместимость: `truckPlan` кладёт в позицию `cap` и `have`. Лист фильтруется
  по площадке (раньше в файл ВБ попадали машины Озона). `_sheetXml` умеет ЖИРНЫЕ ЧИСЛА —
  значение достаётся до проверки на число, иначе итог уезжал в текст и Excel его не суммировал.
  **KPI «Останется на складе» — тоже под площадку**: склад ОДИН на две площадки, поэтому раньше
  здесь всегда стоял остаток после ОБЕИХ отгрузок и число не менялось при переключении ВБ/Озон —
  продавец принял это за поломку. Сейчас на площадке показывается остаток после ЕЁ отгрузки
  (склад − отгрузка этой площадки), на «Всего» — после обеих; в подсказке разложение и полная
  картина. Инвариант: остаток(ВБ) − отгрузка(Озон) = остаток(Всего) и наоборот.
  свой текст для каждого вида и площадки: цепочка от исходных данных до итоговой цифры плюс
  оговорки (декабрь исключён, покрытие вычитается только в закупе, статус не влияет и т.п.).
  Обновляется в `renderTurnoverSimple`, очищается на «Всего» и при отсутствии данных.
  В таблицах закупа под числами показан сам расчёт: «Нужно» → `продаж/дн × 180 дн`,
  «Есть сейчас» → `остаток + в пути + ожид.`, «К закупу» → `нужно − есть = N → K конт.`
  (`rawBuy` — количество до округления до контейнера).
  **Идентификация товара — во всех видах и выгрузках «Оборачиваемости»**: под названием печатается
  `категория · артикул · 1С <supplierCode> · ф. <factoryCode>` (хелпер `skuMeta(r)`; на Озоне артикул
  И ЕСТЬ код 1С — второй раз не дублируется). В CSV — первыми колонками «Артикул · Код 1С · Код фабрики ·
  Статус товара». Поиск (`matchQ`) ищет по артикулу, коду 1С, коду фабрики и названию.
  Фильтры: поиск · категории · **список товаров** (`#turnProduct`, мультивыбор конкретных sku).
  Таблица компактная (влезает без прокрутки вбок): колонка одна «Товар / категория» (категория+sku 2-й строкой),
  меньше паддинги (`#turnTable`). `enhanceSelect` теперь читает `data-all`/`data-plural` (подпись «Все товары / N товаров»).
- **Вид «Подсорт со склада»** (`turnView="resupply"`, `resupplyRows` + `renderTurnResupply`): что
  отгрузить со СВОЕГО склада на площадки, чтобы держать на каждой минимум `RESUPPLY_DAYS=30` дней.
  **Расчёт** всегда по ОБЕИМ площадкам (`mpStats("wb")` / `mpStats("ozon")` — свой расчёт, без
  подмены глобалей), а **показ — под выбранную площадку**: на «ВБ» видны только ВБ-колонки и
  ВБ-статус (`stW`), на «Озоне» — только Озон (`stO`), на «Всего» — обе (класс `.wide` поджимает
  колонку названия, чтобы влезало). Ключ строки — **арт. поставщика** (код 1С = артикул Озона), потому что склад один.
  В колонке «дней» под числом печатается СОСТАВ покрытия: `остаток +N в пути` (на Озоне это его
  собственные поставки `ozTransit`, на ВБ — незакрытые отгрузки из журнала). Раньше показывалось
  одно суммарное число, и продавец не видел уже созданные поставки. На «Всего» (13 колонок)
  разбивка сворачивается — там только итог, иначе таблица не влезает.
  **Новинки и товары, которых на площадке нет** (`spd<=0 && covered<=0`, но есть на складе):
  расчёт «по продажам» дал бы ноль и товар не поехал бы никогда, поэтому им назначается
  **стартовая партия `LAUNCH_QTY=500` шт** (или весь склад, если меньше). Такие строки помечены
  звёздочкой, паллеты по ним округляются ВВЕРХ («не менее 500»), обычный подсорт — вниз.
  `нужно = ceil(продаж/дн × 30) − покрытие`; если склада не хватает на обе площадки —
  **приоритет у Озона** (решение продавца): сначала закрываем Озон полностью, остатком — ВБ; если в файле склада
  товар уже разделён на «вб»/«озон» (`warehouse.split`), площадки считаются раздельно.
  **Артикул в подсорте — ПОД ПЛОЩАДКУ** (`resupMeta`, не `skuMeta`): ключ строки — код 1С, поэтому
  на Озоне печатается артикул Озона (он же код 1С) со ссылкой на карточку Озона, на ВБ — WB sku
  + «1С …», на «Всего» — оба без ссылок. Раньше везде подставлялся WB sku, и на Озоне ссылка вела
  в никуда. Код фабрики и произв. статус кладутся в саму строку (`r.factory` / `r.pStatus`) из
  ВБ-каталога ПО КОДУ 1С — `factoryCode()`/`prodStatus()` смотрят в карту текущей площадки и на
  Озоне пусты. Выгрузка — `exportResupplyXLSX` (лист «Как считается» + план отгрузки; артикулы ВБ
  и Озона отдельными колонками).
  **ТРИ СКЛАДА И ЖЁСТКАЯ МАРШРУТИЗАЦИЯ (правила продавца 27.08.2026)** — `WHS` + `whKey()`
  в index.html, дубль в `scripts/resupply-report.cjs`, канонизация имён — в `update-warehouse.cjs`:
  · **Москва «СХ Солнечногорск»** — ТОЛЬКО FBS для Wildberries. Не отгружает НИКУДА: ни на склады
    ВБ, ни на Ozon. (Раньше именно Москва кормила Ozon — **не возвращай это**.)
    **ЭТО ВРЕМЕННО, НО МЕНЯТЬ ПРАВИЛО МОЖНО ТОЛЬКО ПО ЕГО КОМАНДЕ** (продавец 11.09.2026:
    «на данный момент мы не возим с Москвы на Озон из-за технических неполадок — позже всё
    изменится и мы начнём это делать, как только будет новая информация поменяем скрипт»).
    Следствие видно в подсорте и выглядит как дыра, но дырой НЕ является: на 11.09 **18 кодов
    на 14 861 шт лежат только в Москве**, и потребность Озона по ним закрыть нечем — ярче всего
    487175 (на Озоне 1 шт при потребности 49, в Москве 4 785 шт). Не «чини» это, не предлагай
    обходных маршрутов и не спрашивай второй раз — продавец сам скажет, когда неполадки уйдут.
  · **Нск-1 «Склад FBS»** — FBS для Wildberries И поставки на Ozon.
  · **Нск-2 «Склад Евросиб»** — поставки на Ozon + ПЕРЕМЕЩЕНИЕ на Нск-1.
  Ассортимент на складах почти не пересекается, поэтому машина (33 паллеты) набирается строго
  в разрезе ОДНОГО склада. Порядок разбора Озона: сначала Евросиб, потом Нск-1 — Нск-1 бережём
  под FBS. Имена складов в выгрузках 1С не различают два новосибирских, поэтому у
  `update-warehouse.cjs` есть **`--wh msk|nsk1|nsk2`** — без него выгрузка второго склада молча
  затирала бы первый.
  **ПОСТАВКИ НА ВБ FBO ВЕРНУЛИСЬ, МОСКВА СНОВА ВОЗИТ НА OZON (21.09.2026) — `WB_FBO_SUPPLY=true`.**
  Продавец: «я могу отправить с Евросиба так же товар на Ozon и на вб Fbo»; на прямой вопрос
  «Москва на Ozon по-прежнему нельзя?» — «уже можно». Это ОТМЕНА двух правил ниже (запрет FBO
  от 27.08 и запрет «Москва → Ozon» от 11.09); отменил их сам продавец, не «чини» обратно,
  увидев старые абзацы. Что стало:
  · **ВСЕ ТРИ склада отгружают на ОБЕ площадки.** Порядок разбора — Евросиб → Нск-1 → Москва
    (Нск-1 и Москву бережём: с них уходит FBS). Сначала закрывается Ozon целиком, остатком — ВБ FBO.
  · **`NSK2_MIN_TAKE=100`: на Евросибе позиции с остатком меньше 100 шт не берём вовсе**
    («если на нем по каким то позициям товара меньше чем 100, то эти позиции не берем»).
    Порог ТОЛЬКО у Евросиба.
  · Остатки при распределении считаются по коду суммарно (товар дублируется на складах),
    но **машина по-прежнему собирается в разрезе ОДНОГО склада** — «машины я буду набивать
    в разрезе определенного склада».
  · `moveNeed` (перемещение Евросиб → Нск-1) по построению обнуляется при `WB_FBO_SUPPLY=true`
    и карточка сама исчезает; код оставлен — если FBO снова закроют, сигнал вернётся без правок.
  · `wbFbsFree` теперь `mskFree + nsk1Free`, где `mskFree = whMsk − usedMsk`: Москва больше
    не входит целиком, иначе её остаток считался бы дважды (и в покрытии, и в отгрузке).
  · Новые поля строки: `oMsk/wNsk1/wNsk2/wMsk/usedMsk`; `topUpTrucks` и `truckPlan` работают
    по ШЕСТИ направлениям (три склада × две площадки). Дубль — `scripts/resupply-report.cjs`.
  · **ОТГРУЗКА НА OZON РАЗВОЗИТСЯ ПО ОКРУГАМ ЦФО / ЮФО / ПФО, НА ВБ — ОДНО НАПРАВЛЕНИЕ FBO**
    («на вб сейчас только одно направление это Fbo и выбрать склад нельзя»). Округ = набор
    кластеров Ozon, карта в `scripts` разовых расчётов; источник спроса и остатка по кластерам —
    лист **«Товар-кластер»** в обычном отчёте Ozon «Остатки на складах» (он же `stocks_report.xlsx`),
    колонки «Среднесуточные продажи за 28 дней», «Доступно к продаже», «В заявках на поставку»,
    «В поставках в пути». Отдельный файл «Ликвидность» просить НЕ нужно — данные те же.
  · **ДОБОР ДО ПОЛНОЙ МАШИНЫ — ТОЛЬКО ТЕМ, ЧТО ПРОДАСТСЯ ЗА 7 МЕСЯЦЕВ** (правило 21.09.2026:
    «доибрать машины можно только теми позициями которые продадуться за 7 месяцев, а так да важно
    что бы машины были исключительно по 33 паллета, не хватает добираешь»). Без этого ограничения
    добор придумывал целые машины из одной паллеты реальной нужды (поймано на Евросибе).
    Если добрать нечем — хвост НЕ едет и остаётся на складе; неполных машин не бывает.

  **ИСТОРИЯ (отменено 21.09.2026, оставлено для понимания кода):**
  **ПОСТАВОК НА СКЛАДЫ WILDBERRIES БОЛЬШЕ НЕТ** (`WB_FBO_SUPPLY=false`, 27.08.2026): остаток FBO,
  который там лежит, — последний, дальше ВБ продаётся только по FBS со своих складов; `pending`
  («в пути на ВБ») очищен, `update-transit.cjs` его больше не пишет. Поэтому в подсорте `needW=0`,
  `shipW=0`, стартовых партий на ВБ нет — иначе каждая строка вечно висела бы «нужно на ВБ, взять
  негде». **Вместо отгрузки на ВБ считается ПЕРЕМЕЩЕНИЕ Евросиб → Нск-1**: FBS уходит покупателю
  со склада Нск-1, и когда там кончается ходовой товар, его надо подвезти со второго склада —
  продавец просил сигнализировать сам, не дожидаясь вопроса. `move = ceil(продаж/дн ВБ ×
  NSK1_MIN_DAYS(30)) − остаток Нск-1`, ограничено свободным остатком Евросиба; `nsk1Days` —
  на сколько дней хватит Нск-1. Статус ВБ (`stW`) теперь про это, а не про отгрузку. На вкладке
  ВБ вместо плана машин показывается карточка «Евросиб → Нск-1».
  **Паллеты и машины** (`takePallets`, `truckPlan`, `PALLETS_PER_TRUCK=33`, блок `#turnTrucks`):
  отгружаем только целыми паллетами (вместимость ВБ ≠ Озон, живёт в `REAL_DATA.pallets.bySup`),
  паллеты собираются в машины, машина — всегда с ОДНОГО склада на ОДНУ площадку.
  **`usedNsk1`/`usedNsk2` — сколько ШТУК фактически взято с каждого склада** (паллетами ИЛИ
  штучно, когда вместимость паллеты неизвестна). Свободный остаток для добора считается
  только по ним. Раньше считалось из паллет (`oNsk*palOz`), и при штучной отгрузке склад
  выглядел нетронутым: добор раздавал уже занятый товар, отгрузка превышала склад
  (код 429597: 385 шт при складе 337). Не возвращай расчёт свободного места из паллет.
  **ДОБОР НЕПОЛНОЙ МАШИНЫ** (`topUpTrucks`, `TRUCK_TOPUP_MIN=15`, `TOPUP_SELL_DAYS=90`):
  если в неполной машине набралось ≥15 паллет — добиваем её до 33 (везти полупустую фуру
  дороже, чем довезти то, что скоро понадобится). Ограничитель против застоя: добираем
  только тем, чьё покрытие после добора не выйдет за 90 дней продаж на ЭТОЙ площадке
  (`cap = floor(spd×90) − (покрытие + уже отгруженное)`), поэтому товар без продаж и
  стартовые партии в добор не попадают. Раздаём по ОДНОЙ паллете разным кодам от самых
  горящих — добор обязан быть разнообразным. **Сухой прогон: если до полной не добить —
  не добираем вообще** (смысл только в полной машине). Леджер остатка склада общий для
  обоих направлений одного склада, Озон разбирается первым. Добор пишется в `topUpO`/`topUpW`,
  показан в ячейке отгрузки («＋N добор машины», на «Всего» сворачивается) и отдельной
  колонкой в выгрузке. Логика продублирована в `scripts/resupply-report.cjs` — правь обе.
  **НЕПОЛНЫХ МАШИН НЕ БЫВАЕТ — правило продавца 09.09.2026** (дословно: «по правилам ты должен
  добить машину до 33 пал, но у тебя местами попадаются и 29 и 32 — такого быть не должно
  добивай другими товарами, даже если они по правилу уходят свыше 8 месяцев»). Полупустая фура
  стоит столько же, сколько полная. Правило дано на РАЗОВОМ расчёте «Москва → Ozon»
  (scratchpad `oz/topup.cjs`): там число машин и раскладка по округам НЕ менялись, добирались
  только неполные машины, горизонт 7 мес / стоп 8 мес в доборе снят, порядок добора — код,
  у которого запас в днях ПОСЛЕ добора наименьший. **В самом дашборде (`topUpTrucks`) это ещё
  НЕ применено**: там держатся `TRUCK_TOPUP_MIN=15`, `TOPUP_SELL_DAYS=90` и сухой прогон.
  Если продавец попросит распространить правило на подсорт — снимать надо все три ограничения
  сразу и в `index.html`, и в `scripts/resupply-report.cjs`.
  **ПЛАН МАШИН СТРОИТСЯ ПО ВСЕМУ ПОДСОРТУ, а не по отфильтрованной таблице** — и в UI
  (`renderTruckPlan(allRows, anyFilter)`), и в выгрузке (`truckPlan(resupplyRows())`).
  Машина — физическая отгрузка, она не перестаёт быть полной оттого, что продавец
  отфильтровал таблицу по категории. Раньше сюда приходили отфильтрованные строки, план
  пересобирался на подмножестве и рождал «остаток 24 палл.», которого в реальности нет —
  продавец резонно принял это за незаполненную машину. При активном фильтре в заголовке
  блока стоит пометка. **Не возвращай фильтрованные строки в `truckPlan`.**
  **Ассортимент размазывается по ВСЕМ машинам**: сначала считаем, сколько машин нужно
  (целые по 33 + остаток), заводим их заранее, и паллеты каждого кода раздаём сразу по всем —
  приоритет машине, где кода ещё нет, при равенстве — где больше свободного места.
  Раньше был round-robin с последовательным набиванием: первая машина забирала весь
  ассортимент (9 кодов), во вторую попадали 3–4 крупных. **Не возвращай это.**
  Логика продублирована в `scripts/resupply-report.cjs` — правишь одно, правь и второе.
- **Свой склад и то, что едет К ПРОДАВЦУ** — скрипт **`update-warehouse.cjs <stock|china|order> <файл.xls> [дата]`**,
  читает выгрузки 1С двух раскладок: «Ведомость по товарам на складах» (шапка `Номенклатура.Код` +
  колонки складов + «Итог»; «Итог» НЕ берём, иначе задвоение) и «Ведомость по заказам поставщикам»
  (строки-заголовки «Заказ поставщику …», под ними код/кол-во; отрицательные остатки — мусор, пропускаем).
  Раскладка опознаётся по наличию строк «Заказ поставщику»; ячейку `Номенклатура.Код` ищем по ТОЧНОМУ
  совпадению — она же встречается в описании отчёта («Группировки строк: …»). Коды печатаются с
  разделителем разрядов («487 160»), числа как «1,234.000».
  · `stock` → `REAL_DATA.warehouse = {date, split:false, byWh:{склад:шт}, bySup:{код:{qty, wh:{склад:шт}}}}`.
    **Колонка кода — «Номенклатура.Код» ИЛИ «Код»** (в рабочем файле-своде продавца она называется
    просто «Код»). Справочные колонки «Паллет Озон/Wb (св-во Номенклатура)» и «Номенклатура»
    складами не считаются — иначе вместимость паллеты попадала бы в остаток как отдельный склад.
    Тот же файл читает `update-pallets.cjs` (вместимости), заливать нужно оба раза.
    Склады: «Склад Евросиб» = Новосибирск (в UI «Нск»), «СХ Солнечногорск» = Москва («Мск»).
    **МЕРЖ ПО СКЛАДАМ, а не замена целиком**: продавец шлёт и полную ведомость, и выгрузку по
    ОДНОМУ складу (шапка «Номенклатура.Код» + один столбец). Склады из шапки перезаписываются
    целиком (нет строки = 0 там), склады, которых в файле нет, сохраняются. Полная замена молча
    обнуляла бы второй склад. Плюс страховка: `taken===0` → ошибка и снимок НЕ пишется.
    **Коды 1С приходят и с пробелом («487 160»), и с ЗАПЯТОЙ («474,092»)** — `code()` срезает оба;
    без этого файл читался как пустой и склад обнулялся.
    **СКЛАДЫ МАРКЕТПЛЕЙСОВ В ФАЙЛЕ — НЕ НАШ СКЛАД** (появились 31.08.2026): в 1С товар,
    лежащий на площадках, учитывается отдельными складами — «Склад ОЗОН ПФО», «Склад ОЗОН СФО»,
    «Склад РВБ ПФО» (РВБ = юрлицо Wildberries). Засчитать их в `REAL_DATA.warehouse` нельзя:
    они задвоятся с `ozStock`/`wbStock`, и подсорт решит, что этот товар можно отгрузить ещё раз.
    Скрипт их пропускает и печатает отдельным блоком. Справочные колонки-свойства
    («Код Ozon (св-во Номенклатура)», «Артикул для WB», «Кратность отгрузки») отсеиваются
    ПО ПРИЗНАКУ «св-во» ДО проверки на маркетплейс — иначе «Код Ozon» попадал в неё как
    «склад Озона» и в отчёт лезли миллиарды штук.
    **КОДЫ НЕ ИЗ КАТАЛОГА — ЭТО РОЗНИЦА, ТАК И ДОЛЖНО БЫТЬ** (подтверждено 28.08.2026):
    в файле склада лежат и товары, которых нет ни на ВБ, ни на Озоне (28.08 — 24 кода,
    16 570 шт: 99775, 139320, 500183, 150462 …). Скрипт их пропускает, и это НОРМА, а не
    потеря данных — связывать их с маркетплейсами не нужно. Не «чини» это и не проси
    у продавца артикулы по ним.
    **ЧЕТВЁРТЫЙ СКЛАД: «РЦ Солнечногорский (Москва)»** (появился 28.08.2026) — держим ОТДЕЛЬНОЙ
    строкой, а не сливаем с «СХ Солнечногорск»: продавец видит обе. На маршрутизацию это не влияет,
    оба московских склада ведут себя одинаково (только FBS для Wildberries, никуда не отгружают),
    и `whKey()` в index.html разбирает их по «солнечногор» → `msk`. В `canonWh` правило РЦ стоит
    ДО общего московского — иначе он схлопнулся бы в «СХ Солнечногорск».
    **СЧИТАЕМ ТОЛЬКО ЧЕТЫРЕ СВОИХ СКЛАДА — БЕЛЫЙ СПИСОК `KNOWN_WH`** (правило продавца 31.08.2026).
    В выгрузке начали появляться колонки «Склад ОЗОН ПФО», «Склад ОЗОН СФО», «Склад РВБ ПФО» —
    продавец подтвердил: этого товара нет ни у него, ни на площадке, это баг выгрузки, считать
    нельзя. Раньше любая незнакомая колонка молча становилась складом и попадала в остаток.
    Незнакомые колонки печатаются списком «не посчитано». Явный `--wh` белый список обходит.
    Справочные колонки-свойства («Код Ozon (св-во Номенклатура)», «Артикул для WB»,
    «Кратность отгрузки») отсеиваются ПО ПРИЗНАКУ «св-во» ДО этой проверки — иначе «Код Ozon»
    попадал в отчёт как склад с миллиардами штук.
    **Имя склада в шапке НОРМАЛИЗУЕТСЯ** (`canonWh`): «Нск»/«Новосибирск» → «Склад Евросиб»,
    «Мск»/«Москва» → «СХ Солнечногорск». Продавец выгружает с разными подписями, и без этого
    мерж добавлял ВТОРОЙ склад рядом со старым (старый «не упомянут в файле» → сохранялся)
    и остаток задваивался. Справочные колонки («плановый запас» и т.п.) складами не считаются.
    **«Склад ответхранения Евросиб (магазины)» — НЕ Евросиб** (07.09.2026): это товар на
    ответственном хранении под розничные магазины, а не наш отгрузочный запас. Имя содержит
    «Евросиб», поэтому общее правило слило бы его со «Складом Евросиб» и раздуло остаток,
    доступный к отгрузке на площадки. Правило `/ответхран/` стоит ДО «евросиб» и возвращает имя
    как есть — колонка честно уходит в блок «не посчитано». В файле 07.09 она пустая; не «чини».
    **ПОСЛЕДНЯЯ СТРОКА ФАЙЛА — «Итог», И ОНА ЗАДВАИВАЕТ ВСЁ.** В рабочем своде продавца
    внизу стоит строка «Итог» без кода: сумма по колонке = ровно 2× реального остатка. Скрипт
    её пропускает (кода нет → не в `supSet`), но ЛЮБАЯ ручная проверка «просуммирую колонку»
    даёт удвоенную цифру — 07.09.2026 так и вышло (172 292 вместо 86 146). Сверяй по строкам
    с кодом, а не по колонке целиком.
  · `china` / `order` → `REAL_DATA.inbound.{china|order} = {date, total, bySup:{код:шт}}`.
    Китай — примерно месяц до склада продавца; заказ производству — ~3 мес до склада и ~4 мес до площадки.
  Ни один из этих слоёв в покрытие площадок НЕ входит: склад — только в подсорт, `inbound` — справочно
  (колонка «Приедет», чтобы решать «ждать или заказывать ещё»).
- **`REAL_DATA.shipments` — журнал отгрузок НА ВБ** `[{id,mp:'wb',sup,sku,qty,left,date,arrived[]}]`.
  Зачем: Озон показывает поставку официально (заявка → в пути → остаток), а **ВБ путь от отправки
  до приёмки не показывает вообще**. Поэтому продавец сообщает об отгрузке сам —
  **`wb-shipment.cjs add "512190=300, 487175=1200" [дата]`** (ещё `list` / `close <id> [кол-во]` /
  `drop <id>`), и она считается «в пути на ВБ» в подсорте. **Закрывается АВТОМАТИЧЕСКИ в
  `update-wb-stock.cjs`: если остаток по этому коду 1С вырос — значит поставка принята** (продажи
  остаток только уменьшают, поэтому рост = приход). Закрываются самые старые отгрузки первыми,
  не больше величины роста; скрипт печатает, что закрыл, — продавцу на сверку.
- **Вид «По месяцам»** на «Оборачиваемости» (сегмент `#turnViewSeg`, состояние `turnView`):
  история заказов *артикул × месяц* (`monthlyOrders` + `renderTurnMonths`). Зачем: фиксированное
  окно 7 дн врёт — у новинок в старых месяцах нули, а ноль у товара в ауте не значит «не покупают».
  Продавец смотрит пики и решает дозаказ сам. Пиковый месяц подсвечен акцентом, заливка — внутри
  товара, «Ср/мес» считается **только по месяцам с продажами** (иначе новинки занижены), неполные
  месяцы помечены `*`. **Выкуп здесь фиксированный `TURN_PEAK_BUYOUT=0.85`** (по просьбе продавца;
  свой % выкупа на истории с нулями не показателен) — из него колонка «Продаж/дн в пик».
  Суммы ₽ в этом виде НЕ показываем: деньги в `orderSeries.money` есть только с середины июля,
  на истории они не сходятся. Экспорт CSV переключается вместе с видом (`exportTurnMonthsCSV`).
- **Виды «Закуп: по пику» / «Закуп: по среднему»** (`purchaseRows(base)` + `renderTurnPurchase`):
  расчёт закупа от истории, а не от последних 7 дней. **Декабрь исключён** из обеих баз (распродажный
  месяц, не показателен). Месяцы сравниваются по **среднедневному темпу**, а не по сумме за месяц —
  иначе неполный текущий месяц заведомо проигрывает полным и никогда не станет пиком. «Среднее» —
  по месяцам С продажами (новинки иначе занижены). Формула:
  `продаж/дн = темп базы × 0.85` → `нужно = ceil(продаж/дн × 180)` → `к закупу = max(0, нужно − «есть»)`,
  округление ВВЕРХ до контейнера. **«Есть сейчас» в закупе = ВСЯ ТРУБА**: остаток на площадке +
  в пути на неё + свой склад (`warehouse`) + едет из Китая (`inbound.china`) + уже заказано
  производству (`inbound.order`). Иначе заказ на фабрику двоил бы то, что уже едет (было 160 912 шт
  такого задвоения). В «Дозаказе» по-прежнему только покрытие площадки — там другой вопрос.
  **Склад/Китай/заказано — ОБЩИЕ для ВБ и Озона** (ключ — код 1С), в каждой выгрузке вычтены целиком,
  поэтому складывать файлы ВБ и Озона нельзя — об этом написано на листе «Как считается».
  Свои CSV (`exportPurchaseCSV`), переключаются вместе с видом.
- **Вид «Разбор товара»** (`turnView="item"`, `itemAnalysis` + `renderTurnItem` + `itemDayChart`,
  выгрузка `exportItemXLSX`): ответ на вопрос «руководитель прислал проформу, в ней кода X — 4650 шт,
  хватит на полгода?». Экран одного кода 1С сразу по ОБЕИМ площадкам (товар физически один, витрины
  две), поэтому **на переключатель ВБ/Озон не реагирует** и рисуется в `renderTurnoverTab` ДО всех
  проверок площадки; таблица (`#turnTableCard`) и её фильтры на это время прячутся. Ввод — три поля
  (`#itemQ` код/артикул/название, `#itemQty` количество из проформы, `#itemMonths` горизонт), при
  нескольких совпадениях показываются чипы выбора. Ничего своего не изобретает: заказы — из
  `orderSeries`/`ozon.orderSeries`, выкуп — из общего `buyoutOf`, остатки — те же поля, что в подсорте.
  · **ДЕТЕКТ АУТОВ — главное, чего нет в остальных видах.** Средний темп за 14/28 дней врёт, если
    товар часть периода стоял без остатка: дни аута идут нулями и занижают партию. Дни с нулём заказов
    при живом предыдущем темпе собираются в эпизоды (`itemOutEpisodes`), и рядом считается темп ДО
    провала и ПОСЛЕ (`before`/`after`/`recovery`). Если после возврата темп не вернулся — карточка
    потеряла позиции, и планировать надо по доаутному темпу (`preRate`), а не по текущему. Показы из
    воронки печатаются рядом как подтверждение причины (выпал из выдачи, а не разонравился).
    Реальный случай 501432: 19–24.08.2026 показы 22 384 → 97, заказы 29,7/дн → 16,1/дн (59% от прежнего).
  · **ТРИ ПОРОГА, БЕЗ КОТОРЫХ БЛОК КРИЧИТ ВОЛКАМИ.** Первая версия находила «провалы» у 99 товаров
    из 120. Сейчас: эпизод открывается только при темпе от `ITEM_OUT_MIN_RATE=2`/дн; **значимым**
    (`e.material`, только такие показываются) считается свежий — не старше `ITEM_OUT_RECENT=45` дней,
    с потерей от `ITEM_OUT_MIN_LOST=8` заказов и прежним темпом от 3/дн; `preRate` берётся только по
    провалу не старше 30 дней. Осталось 107 кодов из 407 — это реальные августовские остановки.
  · **«ПРОВАЛ» И «ТОВАР НЕ ПРОДАЁТСЯ» — РАЗНЫЕ СОСТОЯНИЯ** (`ITEM_OUT_MAX_DAYS=30`). Нули длиной
    больше месяца — это не аут, а вывод из продажи: у такого кода прежний темп ничего не значит, и на
    нём выходило «потеряно 4 429 заказов» по позиции, которая стоит с декабря. Такие эпизоды из
    провалов исключены, вместо них печатается «Продаж нет уже N дней» (`silentDays`/`lastSale`).
  · **РАЗРЫВ ДО ПАРТИИ.** «Хватит до» сравнивается с `etaSale` ближайшей партии (выход В ПРОДАЖУ, не
    приход на склад) — правильный объём не спасает, если он встаёт в продажу позже, чем кончается
    остаток. У 501432 разрыв 62 дня, ровно на предновогодний пик.
  · **ФИЛЬТР «НА ВЫВОД» СЮДА НЕ ДЕЙСТВУЕТ** — см. `showOut`: в «Разбор товара» приходят
    с конкретным кодом в руках, прятать введённый руками товар бессмысленно.
  · **ПАНЕЛЬ ВВОДА `#turnItemCtl` ПРЯЧЕТСЯ ЧЕРЕЗ `hidden`, И ЭТОГО МАЛО.** У класса
    `.itm-ctl{display:flex}` приоритет ВЫШЕ браузерного `[hidden]{display:none}`, поэтому
    строка ввода висела во всех видах «Оборачиваемости», хотя атрибут проставлялся правильно.
    Держи правило `.itm-ctl[hidden]{display:none}` — без него баг вернётся. Показ/скрытие
    делается в `renderTurnoverTab` ДО проверки `if(!RD)`, иначе панель осталась бы на экране
    «данных нет». Сама панель — не голые `<input>`, а поля `.itm-f`/`.itm-in` с подписями:
    в `.toolbar` стили привязаны к самому классу `.toolbar`, и вне его инпуты рисуются
    браузерными дефолтами (белые прямоугольники на тёмной теме).
  · **ПАЛЛЕТА И КОНТЕЙНЕР — РАЗНЫЕ ЧИСЛА, НЕ ПУТАЙ** (перепутал 02.09.2026, продавец решил, что
    вместимость паллеты потерялась из снимка, хотя она стояла верная). `pallets.bySup[код]`
    = `{wb,oz}` — сколько штук на ПАЛЛЕТЕ, у площадок по-разному (у 501432 ВБ 16 / Озон 20);
    `catalog[].containerQty` — размер КОНТЕЙНЕРА, кратность заказа на фабрику (980–2 900 шт).
    Совпадений между ними нет ни у одного из 52 товаров с обоими полями. В шапке блока
    подписывался `containerQty` как «паллета» — теперь печатаются оба поля своими именами,
    а при введённом количестве считается объём партии в паллетах и машинах (4 650 шт по
    16–20 шт = 233–291 паллета = 8–9 машин): для проформы это не мелочь.
  · **«Есть сейчас» = площадки + свой склад целиком**; `ozIncoming`/Китай/заказанное показаны
    отдельным нижним блоком и в «хватит» НЕ входят — до площадки они доедут через месяцы.
  · Инварианты (проверены прогоном по всем 407 кодам): «Есть сейчас» = сумма строк трубы =
    `onSite+atSeller`; `хватит дней × продаж/дн = запас`; `продаж/дн = зак.ВБ×выкуп.ВБ + зак.Озон×выкуп.Озон`;
    внутри каждого эпизода заказов ровно 0.
- **Фильтры/сортировка работают во ВСЕХ видах «Оборачиваемости»** (дозаказ · по месяцам · оба закупа ·
  подсорт). «Разбор товара» сюда НЕ входит — там нет таблицы, и `renderTurnoverSimple`
  отдаёт его в `renderTurnItem()` первой же строкой. Общая механика: `turnBaseRows()` отдаёт строки текущего вида, `turnColDefs()` —
  описания колонок ЭТОГО вида (у подсорта ещё и под площадку), `turnFilteredRows()` применяет
  поиск + категории + список товаров + все колоночные фильтры. Селекты категорий/товаров
  заполняются в `renderTurnoverTab` ДО любых ранних выходов — иначе на подсорте они оставались пустыми.
  `renderTurnoverSimple()` в начале делегирует подсорт в `renderTurnResupply()`: панель фильтра
  вызывает именно её, и без этого фильтр на подсорте рисовал таблицу дозаказа.
- **Фильтры по колонкам (Excel-стиль)** — у каждой числовой/статусной колонки в шапке кнопка-воронка
  (`.col-filter`, `thSortF()`): клик открывает панель `.colfilter-panel` (переиспользует стили `.uisel-panel`)
  со списком **значений этой колонки** (с подсчётом строк и поиском при >12 значений). Отметил — таблица
  показывает только эти значения; **фильтры комбинируются логическим И** и стакаются по колонкам (выбрал статус
  «Пора дозаказать», затем в «Дозаказать» — конкретные партии). Состояние — `turnColFilters{ключ:Set значений}`
  (пусто = без фильтра), логика — `turnColDefs()` (`val(r)`=каноничное значение колонки, `sortVal(v)`=для сортировки
  списка), отбор — `turnFilteredRows(all,exceptKey)` (панель + все колоночные фильтры; `exceptKey` даёт «контекстные»
  списки как в Excel). Панель = отдельный `position:fixed` div в `<body>` (не режется `overflow` таблицы),
  один за раз (`_colFilterKey`), закрывается по клику вне/Esc/скроллу. Клик по воронке не сортирует
  (`wireSortable` пропускает `.col-filter`). **Старый тулбар-селект статусов удалён** — статус теперь колоночный
  фильтр (мультивыбор, лучше). Работает одинаково для ВБ и Озона; при смене площадки `applyMpData` сбрасывает
  `turnColFilters`. **Экспорт CSV (`exportTurnoverCSV`) идёт строго по текущим фильтрам+сортировке** (что видишь —
  то и качаешь), все колонки + ИТОГО по дозаказу; кнопка «Скачать таблицу CSV».

## Вкладка «Реклама» (`ads`) — какие товары брать в рекламу (пока только ВБ)

Повторяет ручной отбор продавца (согласован 31.08.2026). `adRows()` + `renderAdsTab()` +
`renderAdHowTo()` + `exportAdsXLSX()`; константа `AD_MIN_MSK_DAYS=21`.

- **Реклама ведётся по СКЛЕЙКАМ, а не по карточкам.** Склейка — карточки, слитые в одну:
  в выдаче они конкурируют как одна позиция. Разбивка приходит из выгрузки XWAY
  (`scripts/update-ad-tags.cjs` → `catalog[].adGroup` / `catalog[].adTag`, дата в
  `meta.adTagsDate`). «Группа» крупнее ВБ-категории (Пушкары · Рули · Песок · Светофоры ·
  Набор для бокса), «тег» = склейка внутри группы. Пустой тег → вся группа одна склейка.
- **Парето ВНУТРИ КАЖДОЙ СКЛЕЙКИ по ВЫРУЧКЕ** (заказано ₽ за период): накопленная доля
  ≤80% — A, ≤95% — B, дальше C. Оба выбора — решение продавца:
  · по выручке, а не по штукам, потому что реклама окупается с денег (на реальных данных
    базы расходятся всего в двух позициях);
  · внутри склейки, а не по всей группе, чтобы лидер нашёлся в КАЖДОЙ склейке — по группе
    кандидатов вдвое меньше и слабые склейки выпадают целиком.
- **ЗАПАС ДЛЯ РЕКЛАМЫ = FBO + СВОЙ МОСКОВСКИЙ FBS** (`cover`/`coverDays`, `AD_MIN_STOCK_DAYS=21`).
  **ПРАВКА ПРОДАВЦА 10.09.2026 — ГЛАВНОЕ ЗДЕСЬ.** Раньше считался ТОЛЬКО московский FBS, и
  рекомендация требовала выключить рекламу по светофорам, у которых на складах ВБ лежало
  798 / 2 513 / 3 880 / 6 894 шт: «товар есть в большом кол-ве на FBO … Fbo — это ещё лучше
  вариант чем fbs». Он прав: FBO — товар уже на складах Wildberries, доставка оттуда быстрее
  собственного FBS. Склейка «Светофоры» за 09.09 отработала с ДРР 4,8% в заказах, то есть
  правило кричало на здоровые кампании. После правки те же коды дают 312–427 дней запаса,
  и кампаний «без запаса» стало 0 вместо 11 (кандидатов 20 → 31).
  · **Нск-1 в покрытие НЕ входит** — он снабжает Ozon и сибирский FBS, засчитывать его рекламе
    значит считать один товар дважды. Евросиб тем более (по FBS с него не торгуют).
    **ПОДТВЕРЖДЕНО ПРОДАВЦОМ 11.09.2026, ВОПРОС ЗАКРЫТ.** Я спросил прямо, не добавить ли
    новосибирский FBS (склад так и называется — «Склад FBS» — и тоже торгует по FBS на ВБ).
    Ответ: «у меня есть опыт и он говорит о том, что товара которого нету на Fbo либо на Fbs Мск,
    то в него нет смысла вливать деньги на рекламу, но я периодически пробую и смотрю как оно
    будет». То есть покрытие = FBO + Москва, и это НЕ недосмотр — из Новосибирска до основного
    покупателя долго, реклама не отбивается. Не предлагай добавить Нск-1 снова.
    **Кампании, которые правило помечает, часто и есть его «периодически пробую»**: замер 11.09 —
    три кампании без FBO и Москвы (арт. ВБ 833127031, 1158274549, 971138374 на 7 876 ₽/день),
    и ВСЕ ТРИ со статусом «Новинка», товар лежит на Нск-1 (2 620 / 265 / 2 348 шт). Пометка тут
    информирует, а не требует выключить: решение за продавцом.
  · **АГРЕГАЦИЯ РАЗНАЯ У ДВУХ СЛАГАЕМЫХ, НЕ СЛОМАЙ**: московский FBS лежит по КОДУ 1С и на
    уровне склейки суммируется через `Set` (у одного кода несколько артикулов ВБ — обычная
    сумма задвоила бы склад), а FBO — СВОЙ у каждой карточки и складывается построчно.
  · Порог в ДНЯХ, а не «больше нуля»: реклама разгоняет продажи, и позиция с запасом на пару
    дней встанет посреди кампании. Отсеянные показаны отдельным блоком внизу таблицы,
    а не молча спрятаны; в колонке «Запас ВБ» печатается состав — «N дн · FBO X шт + Мск Y шт».
  · Переименования при правке: `AD_MIN_MSK_DAYS`→`AD_MIN_STOCK_DAYS`, `mskDays`→`coverDays`,
    `adMskBucket`→`adStockBucket`, ключ фильтра `mskSt`→`stockSt`. Поля `msk` (Москва FBS)
    и `fbo` остались раздельными — они нужны для показа состава.
- Остальные товары склейки едут на **ассоциативных конверсиях** — реклама на них не ставится.
- Продажи/дн = заказы ÷ дней периода × общий % выкупа (`meta.buyoutWin.all`). Показы, CTR и
  конверсия — из `FUNNEL_CACHE` за то же окно, чтобы видеть, есть ли куда расти.
- Виды: «Кандидаты» (плоский список по убыванию выручки + блок отсеянных) и «По склейкам»
  (сгруппировано, в шапке склейки — выручка, остаток в Москве, число кандидатов).
  Группы в селекте отсортированы по числу карточек, поэтому первой открывается самая крупная.
- **Вкладка считает только Wildberries** и в `MP_TABS` не входит. Озон — отдельно, позже.
- `scripts/ad-candidates.cjs "<группа>" [дней]` — тот же расчёт в консоли (для отладки
  и обсуждения правил, в снимок ничего не пишет).

### Вид «Отдача» — разбор действующих кампаний (`renderAdsPerf`)
- Данные: `scripts/update-ads-perf.cjs <выгрузка XWAY>` → `REAL_DATA.adPerf = {from,to,bySku}`.
  Период берётся из имени файла; **даты приходят в ДВУХ форматах** — XWAY пишет ГГГГММДД
  («20260826__20260830»), выгрузки аналитики ВБ — ДДММГГГГ («27082026»); `isoFrom8()`
  различает их по первым двум цифрам.
- **БАЗА ДРР — «ОБЩАЯ СУММА ЗАКАЗОВ» (вся выручка товара), А НЕ «ЗАКАЗЫ, РУБ.» (заказы
  с рекламы).** Ошибка первой версии, продавец поймал её 31.08.2026: ДРР — это доля рекламных
  расходов В ВЫРУЧКЕ, а не окупаемость рекламных заказов. По рекламным заказам выходило 30.6%,
  по общей выручке — 7.7%, ровно как в кабинете XWAY (колонка «Общий ДРР»). Сверка на
  «Пушкарах» 26–30.08 сошлась в цифру: расход 355 470 ₽, общая сумма заказов 4 608 116 ₽,
  1 311 заказов. **Не возвращай `rub` в знаменатель ДРР** — там должен быть `totRub`.
- **ДРР СЧИТАЕТСЯ В ВЫКУПЕ, ЦЕЛЬ ≤7%** (`AD_DRR_GOAL=7`, хорошо `AD_DRR_GREAT=6`, ниже — идеально).
  В выгрузке выручка стоит в ЗАКАЗАХ, поэтому умножаем её на `meta.buyoutWin.all` (75% —
  «выкуплено ÷ ВСЕ заказы»). Из-за этого цифра на вкладке ВЫШЕ, чем в кабинете XWAY, — так
  и задумано: продавец ориентируется именно на выкуп.
- **РЕШЕНИЕ — ПО ДРР С АССОЦИАТИВНЫМИ ЗАКАЗАМИ, а не по личному.** Рекламируемый товар —
  «лошадка», источник трафика: реклама одного приносит заказы соседним. Если у неё самой ДРР
  плохой, но с учётом всех пришедших продаж экономика сходится, выключать её незачем.
  `drrA` = расход ÷ ((своя выручка + «Заказы др. артикулов, руб.») × выкуп); `drrD` — личный,
  показан справочно. Замер 26–30.08: прямой ДРР 43.8%, с ассоциативными 17.4%.
- **Оценка даётся СКЛЕЙКЕ, а не товару**: строка-заголовок склейки несёт её общий ДРР и
  вердикт «В цели» / «Править склейку». Товар внутри склейки, которая в цели, помечается
  «склейка в цели — тянет трафик», а не «разобрать».
- Кампании с расходом < `AD_MIN_SPEND=500 ₽` помечаются «мелкий расход» и не судятся:
  у ВБ по многим карточкам списывается рубль-два перелива, без порога они висели бы
  «разобрать» при нулевых продажах.
- **ПОТОЛОК CPO ВЫВОДИТСЯ ИЗ ЦЕЛИ ПО ДРР, а не подбирается** (`adMaxCpo`):
  `ДРР = расход ÷ выручка`, `CPO = расход ÷ заказы`, `выручка = заказы × чек × выкуп`
  ⇒ **`макс CPO = целевой ДРР × средний чек × % выкупа`**. Чек берётся В ЗАКАЗАХ (как в отчёте),
  выкуп домножается отдельно — иначе он учёлся бы дважды.
  Считается НА ДВУХ УРОВНЯХ (просьба продавца «и так и так»):
  · **по товару** — свой чек и свой % выкупа (`buyoutOf`, флаг `boOwn` показывает, свой он
    или общий). Разброс выкупа по товарам 30–100%, и он двигает потолок сильнее всего:
    при 42% потолок вдвое ниже, чем при 83%;
  · **по склейке** — её чек и выкуп, ВЗВЕШЕННЫЕ ПО ЗАКАЗАМ. Продавец не выключает «лошадку»
    с плохим личным ДРР, если склейка бьётся, — значит и судить её надо потолком склейки.
  Вердикт товара при перерасходе — «снизить ставку в N раз» (`cpo/max7`).
  **«Выкуп в моменте» получить нельзя** — по свежим заказам половина ещё едет, любая цифра
  за последнюю неделю занижена. Берём последнее ДОЗРЕВШЕЕ окно; на 31.08 это июль (75.6%),
  а август идёт к 70–72%, то есть потолок сейчас завышен примерно на 6%.
- Календарь периода на этот вид НЕ влияет — он про отбор кандидатов; здесь период тот,
  что был в загруженной выгрузке.
- **Остаток в МОСКВЕ и «хватит дней» есть и здесь** (просьба продавца 31.08.2026: решение
  «оставить / снизить ставку / выключить» он принимает вместе с запасом, и раньше за этим
  надо было уходить в «Кандидаты» или в выгрузку). Продажи/дн считаются ЗА ПЕРИОД САМОЙ
  ВЫГРУЗКИ (тот же период, что и расход в этой таблице), а не за окно кандидатов. В строке
  склейки остаток суммируется **по коду 1С через `Set`**: у одного кода бывает несколько
  артикулов ВБ, и простая сумма по строкам задваивала бы склад.

### ДРР — ЧЕТЫРЕ УРОВНЯ, И ВСЕ В ВЫКУПЕ (правило продавца 01.09.2026)
Продавец смотрит ДРР на четырёх отрезках: **весь ассортимент** (на это смотрит руководство) →
**категория** (руководство и он) → **склейка внутри категории** (его рабочий уровень) →
**товар**. «Чтобы повлиять на ДРР по кабинету, я должен знать ДРР на каждом отрезке».
Блок `#adDrrTree` (`adLevels()` + `renderAdDrrLevels()`) в виде «Отдача»: клик по категории
выбирает её, клик по склейке фильтрует таблицу.

**ОДНА ФОРМУЛА НА ВСЕХ УРОВНЯХ:** `ДРР = расход ÷ Σ(выручка товара в заказах × ЕГО % выкупа)`.
· **Выкуп берётся СВОЙ У КАЖДОГО ТОВАРА** (`buyoutOf`), а не общий по магазину — правка
  продавца 01.09.2026 («у тебя во всех категориях один и тот же процент выкупа?»). Разброс
  двигает цифру заметно: у «Пушкаров» взвешенный выкуп 66.8% против 75.6% по магазину,
  и их ДРР был занижен (10.5% → 12.0%). Общий процент — фолбэк для товаров, у которых
  своих заказов в окне замера мало. В строке уровня печатается применённый выкуп.
· **Порядок уровней — ПО ВЫРУЧКЕ, а не по расходу**: «всегда смотрим сначала туда, где
  больше всего зарабатываем, там и теряем больше всего». Выбор категории по умолчанию — тоже.
· **Знаменатель — `totRub` всех карточек уровня, включая те, где рекламы нет.** Так считает
  кабинет XWAY, и так решил продавец: «лошадка» тянет продажи соседям, они законно в зачёте.
  Раньше в знаменатель шли только рекламируемые карточки — по «Пушкарам» 26–30.08 выходило
  3 143 218 ₽ и 11.3% против 4 608 116 ₽ и 7.7% на его экране. `update-ads-perf.cjs` теперь
  кладёт в снимок и строки с нулевым расходом. Не возвращай их пропуск.
· **`othRub` («заказы других артикулов») в агрегаты НЕ идёт** — соседние карточки уже сидят
  в выручке своего же уровня, и добавление считало бы их дважды. `drrA` остаётся только
  в строке товара, справочно.
· **Цель 6–7% задана В ВЫКУПЕ**, поэтому цифра на вкладке выше кабинетной: 26–30.08 — 7.8%
  в заказах и 10.5% в выкупе. KPI показывает обе: основная в выкупе, рядом «в заказах
  (кабинет XWAY)» для сверки.
· Расход меньше `AD_MIN_SPEND` — вместо ДРР бейдж «мелкий расход»/«нет рекламы»: «0.0%»
  при расходе 5 ₽ читалось как идеальная кампания.
· **Порядок категорий и выбор по умолчанию — ПО РАСХОДУ**, а не по числу карточек: по
  количеству первой открывалась «Машинки» (162 карточки, 4 089 ₽), а разбирать надо
  «Пушкары» (443 154 ₽).

### ГРУППЫ НА ВЫВОД — НЕ СЧИТАЮТСЯ (`meta.adOutGroups`)
Решение продавца 01.09.2026: весь товар в группах **Машинки · Парковки · Распродажа ·
Трансформеры** идёт на вывод, рекламу по ним не разбираем. Список живёт В СНИМКЕ, а не
в коде (`scripts/ad-out-groups.cjs`: `list` / `set` / `add` / `drop` / `resnap`), потому что
он изменится. Такие группы выкидываются из отбора, из пирамиды ДРР и из рекомендаций,
но **не исчезают молча**: под пирамидой печатается строка «Не считается — товар на вывод»
с их расходом и ДРР, чтобы сумма сходилась с кабинетом.
**СИГНАЛ ОБ ОБНОВЛЕНИИ АССОРТИМЕНТА.** Продавец предупредил: «если дальше в этих группах
изменится ассортимент, сигнализируй — возможно группы обновятся новым ассортиментом».
Поэтому `ad-out-groups.cjs` снимает СЛЕПОК состава (`meta.adOutMembers`, дата в `adOutAt`),
а `update-ad-tags.cjs` при каждой заливке склеек сверяет с ним и печатает новые артикулы
с названиями. После разбора слепок обновляется командой `resnap`.

### НАКОПИТЕЛЬ ВЫГРУЗОК РЕКЛАМЫ (`adPerf.parts`)
Выгрузки XWAY приходят кусками (26–30.08, затем 31.08, дальше по дню). Раньше новая
заливка ЗАМЕНЯЛА старую: по одному дню ДРР шумный (расход сегодня — заказы завтра), и,
главное, нельзя было увидеть, что изменилось после наших действий. Теперь каждая выгрузка
ложится частью `parts[]`, а `bySku` — их сумма (периоды не пересекаются, суммы складываются
честно). **Повторная заливка того же периода не двоит** — часть с пересекающимися датами
заменяется целиком, скрипт печатает об этом и о пропусках дней между частями.

**ЧАСТЬ «ТОЛЬКО РАСХОД» (`spendOnly:true`) — решение продавца 09.09.2026.** 09.09 он прислал
выгрузку за 03–25.08: расход **3 241 ₽ за 23 дня** (141 ₽/дн, показов 19 411) при выручке
20 251 351 ₽ — реклама в этот период фактически не крутилась, включилась 26.08. Часть нужна
для ПРИБЫЛИ (расход был), но её выручка в знаменателе ДРР роняет цифру вдвое: ДРР вкладки
3,28% вместо 7,15% в выкупе, по всему накопителю 2,5% вместо 5,6% в заказах. Продавец
принимает решения по ставкам по ДРР, поэтому такие части помечаются `spendOnly`.
· Ставит/снимает **`scripts/ad-part.cjs`** (`list` / `spend-only <от> <до>` / `full <от> <до>` /
  `drop <от> <до>`), он же пересобирает `bySku` и границы `from`/`to`.
· **Фильтр продублирован в `update-ads-perf.cjs`** — иначе ежедневная заливка пересобирала бы
  `bySku` по всем частям и молча возвращала разбавленный ДРР. Правишь одно — правь второе.
· **РАСХОД НА ПРИБЫЛЬ НЕ МЕНЯЕТСЯ**: `adRowsFromPerf` и `recAdsInWindow` в index.html ходят
  по `parts[]` и берут только `spend`, поэтому помеченная часть по-прежнему уменьшает прибыль
  своих дней (проверено: расход за 03.08–08.09 в `ADS_CACHE` = 927 136 ₽, из них 3 241 ₽
  приходится на 03–25.08). `drop` — другое: он удаляет часть совсем, и расход пропадает тоже.
· `adPerf.from`/`to` после пометки описывают АКТИВНЫЙ период (26.08–08.09) — это подпись
  «Период рекламы» в UI и в выгрузке.

### ДРР ВИДЕН ВО ВСЕХ ВИДАХ, НЕ ТОЛЬКО В «ОТДАЧЕ»
Просьба продавца 01.09.2026 («нету отображения ДРР ни по кабинету, ни по склейке, ни по
артикулу»). `adDrrLevelsCache()` считает ДРР по склейкам, категориям и всему кабинету одним
проходом (кеш сбрасывается в начале `renderAdsTab`), дальше:
· **KPI №3** в «Кандидатах»/«По склейкам» — ДРР выбранной категории (или всего кабинета,
  если категория не выбрана); в подсказке — весь кабинет, ДРР в заказах и доля кандидатов.
  Раньше там висела «доля от группы», а ДРР жил только в «Отдаче».
· **строка склейки** в «По склейкам» — расход и ДРР склейки;
· **колонка «Реклама ₽»** — ДРР ПОКАЗЫВАЕТСЯ ВСЕГДА. Раньше у рискованных строк вместо ДРР
  печаталась причина («остатка нет»), и цифры не было там, где она нужнее всего; теперь
  и ДРР, и пометка про запас.
Формула везде одна и та же — см. «ДРР — ЧЕТЫРЕ УРОВНЯ». Сверка: сумма расхода по склейкам
равна расходу по кабинету (471 942 ₽).

### ВЫГРУЗКА КЛАСТЕРОВ XWAY: «ПОЗИЦИЮ В ВЫДАЧЕ» НЕ ИСПОЛЬЗОВАТЬ
Продавец присылает по товару выгрузку «Кластеры кампании» (лист «Страница 1»): по каждому
поисковому кластеру — «Запросов в неделю» (частотность), показы/клики/корзины/заказы, расход,
CPC/CPO/CTR, конверсии, «Общие клики/корзины/заказы», «Ставка CPM», «Целевое место».
**Колонка «Позиция в выдаче» в этой выгрузке НЕПРИГОДНА** (объяснение продавца 03.09.2026):
это фактическая позиция НА МОМЕНТ ВЫГРУЗКИ, а не за период отчёта, и кампании у него крутятся
ПО РАСПИСАНИЮ (по тёплой картине заказов). Если в момент выгрузки реклама была выключена,
в колонке стоит позиция без рекламы — и вывод по ней будет ложным. Подённые позиции есть
только в интерфейсе XWAY (правый блок с колонками по дням), в файл они не попадают.
**Не строй на этой колонке никаких утверждений.** Промах 03.09.2026: по ней был сделан вывод
«97% расхода уходит туда, где мы за 20-м местом» — снят как недостоверный.
**Что в выгрузке ПРИГОДНО и уже даёт вывод без позиций** — цена клика по кластерам: на замере
02.09 по арт. ВБ 49400769 «толокар синий трактор» дал 11 кликов за 28 ₽ (CPC 2,6 ₽), а «детская
машина большая для езды» — те же 11 кликов за 527 ₽ (CPC 47,9 ₽), при одинаковой 1 корзине.
72% бюджета ушло на 6 кластеров с CPC выше 30 ₽ при среднем 26,4 ₽.
**Файл не содержит ни артикула, ни периода** — они были только в имени (`XWAY_49400769__20260902__20260902.xlsx`).
Без такого имени привязать выгрузку к товару нечем: по сумме расхода не опознаётся.

### Период и фильтры на «Рекламе»
- **ПЕРИОД — КАЛЕНДАРЬ, а не пресеты 14/28/56 дней** (31.08.2026): продавец сверяет рекламу
  с конкретными датами выгрузки, и фиксированные окна в них не попадали. Состояние — ISO-даты
  `adFrom`/`adTo` (пусто = последние `AD_DEF_DAYS=28` дней), окно = дни `orderSeries.dates`
  внутри диапазона (`adWin()`), подпись — `adPeriodTxt()`. Перевёрнутый ввод («по» раньше «с»)
  разворачивается сам. Не возвращай `<select id="adPeriod">`.
- **Колоночные фильтры-воронки** — та же механика, что на «Оборачиваемости» и «Запасе и темпе»:
  контекст `CF_CTX.ads`, состояние `adColFilters`, описания `adColDefs()` (набор колонок РАЗНЫЙ
  у «Кандидатов/По склейкам» и «Отдачи», поэтому при смене вида фильтры сбрасываются — ключи
  не совпадают). **Клик по воронке ловится слушателем на `#adTable`** — своя таблица, свой
  слушатель; без него панель просто не открывалась (ровно это и случилось в первой версии).
- **ЧИСЛОВЫЕ КОЛОНКИ ФИЛЬТРУЮТСЯ ГРУППАМИ, а не поштучно.** Список из 75 разных значений
  («9.6%», «9.7%» …) фильтром не является. Пороги взяты те, по которым принимается решение:
  ДРР — `adDrrBucket` (идеально ≤6% · в цели 6–7% · выше цели · нет продаж), CPO — «в потолке /
  дороже потолка», Москва — `adMskBucket` (нет остатка · меньше 21 дн · 21+ дн · остаток есть,
  продаж нет). В «Решении» причина отсева сводится к одной из трёх (иначе «хватит на N дн»
  давало бы у каждого своё значение).
- **База панели = выборка таблицы**: у «Кандидатов» группа отсекается внутри `adRows()`,
  у «Отдачи» — в `adBaseRows()`. Без этого в списке склеек висели 55 кампаний чужих групп,
  которых в таблице нет.
- В «Отдаче» сортировка идёт **внутри склейки** (`adSortPerf`): сами склейки всегда по расходу
  сверху вниз — разбор ведётся от самой дорогой.
- **ДЕЙСТВУЮЩИЕ КАМПАНИИ ВИДНЫ И В ОТБОРЕ** (31.08.2026, «тут я этого не вижу»): отбор и деньги
  жили в разных видах, и главное — что реклама крутится на товар, которого в Москве нет, —
  приходилось искать глазами в «Отдаче». Теперь `adRows()` подмешивает `adSpend`/`adDrr` из
  `adPerf` (колонка «Реклама ₽»: расход + ДРР, красным когда запаса меньше `AD_MIN_MSK_DAYS`),
  KPI №4 стал **«Реклама без запаса в Мск, ₽»** (число отсеянных ушло в подсказку), а в виде
  «Кандидаты» появился **третий блок — «Реклама крутится, а запаса в Москве нет»**: там
  показываются кампании, не попавшие в категорию A, иначе KPI говорил «145 829 ₽ в трубу»,
  а строк в таблице не было. Расход < `AD_MIN_SPEND` помечается «мелкий расход» и в риск НЕ
  идёт (у ВБ по многим карточкам списывается рубль-два перелива — без порога в списке
  «остатка нет» висели 26 позиций вместо 7 реальных).
  **Период расхода ≠ период отбора**: расход берётся за период загруженной выгрузки рекламы,
  а выручка — за календарное окно. Другого источника расхода нет; это подписано и в шапке
  объяснения, и в выгрузке (строка «Период рекламы»). Не «чини» это подгонкой периодов.
- **СКЛЕЙКА В ТАБЛИЦЕ ДОЛЖНА ЧИТАТЬСЯ КАК БЛОК** (просьба продавца 01.09.2026: «очень
  сильно сливаются склейки, когда начинаешь считать — считаю всё подряд»). У `.row-group`
  вообще не было стилей, она отличалась только жирным шрифтом. Теперь: полоса с фоном
  `accent 10%`, рамка сверху 2px и вертикальный акцент слева у строки склейки; товары
  внутри — класс `in-grp` с отступом 22px и бледной направляющей. В видах отбора заголовок
  склейки — ячейка `colspan` с классом `grp-head`: текст прижат влево, а внутренний
  `.grp-in` — `position:sticky;left:12px`, иначе при горизонтальной прокрутке подпись
  уезжала за правый край (ячейки таблицы выровнены вправо).
- **Первая колонка таблиц рекламы — `class="metric"`** (sticky слева). Без неё при
  горизонтальной прокрутке название товара уезжало под левый край, и строку нельзя было
  опознать: в кадре оставались «с прицепо…» и «ручкой La…».

## Вкладка «Рекомендации» (`recs`) — что делать сегодня

**Первая вкладка и точка входа по умолчанию** (`switchTab("recs")` при отсутствии сохранённой).
Продавец заходит и сразу видит, что делать. `recBuild()` + `recChanges()` + `renderRecsTab()`.

- **Ничего не считает сама** — вызывает те же функции, что и остальные вкладки
  (`resupplyRows`, `srRows`, `adRows`, `adPerfRows`). Рекомендация ОБЯЗАНА совпадать с той
  вкладкой, куда отправляет; кнопка «Открыть →» переключает вкладку и нужный вид.
- **`adRows()` вызывается с принудительным `adGroup=null`** (и восстановлением) — иначе
  рекомендации молча сужались до категории, выбранной на «Рекламе».
- **Блок «Что изменилось за неделю и почему»** (`recChanges` + `recDiag` + `recFactors`).
  Первая версия искала причину ПОРОГАМИ («цена изменилась больше чем на 8%?»), и когда ни
  один порог не срабатывал, писала «причина не видна в данных» — при том что данные её
  содержали (пример 49400769: 148 → 121 заказов, каждый признак под порогом). Переделано
  01.09.2026 на три шага, и ответ есть ВСЕГДА:
  1. **ФАКТОРНОЕ РАЗЛОЖЕНИЕ** (`recFactors`). `заказы = показы × CTR × переход→корзина ×
     корзина→заказ`, поэтому изменение раскладывается на вклад каждой ступени В ШТУКАХ,
     и сумма вкладов ТОЧНО равна фактической дельте (проверяется тестом). У 49400769:
     показы +10, CTR −8, переход→корзина −12, корзина→заказ −18 = −27 заказов.
  2. **ПРОВЕРКА НА ШУМ**: `z = |Δ| ÷ √(a+b)`; ниже 1.96 — падение неотличимо от случайного,
     так и пишем («на грани шума, решение можно отложить»). У 49400769 z=1.65.
  3. **КОНТЕКСТ СЕГМЕНТА**: как изменились его склейка и категория — **БЕЗ САМОГО ТОВАРА**
     в знаменателе, иначе крупная карточка тянет свой сегмент вниз и про неё же делается
     вывод «падает весь сегмент, дело не в карточке». У 49400769 склейка +50% при его −18%
     → падает он один, спрос есть.
  **ЕДИНИЦЫ НЕЛЬЗЯ СМЕШИВАТЬ** (правка продавца 01.09.2026: «переход→корзина −6%, где тут
  6%, когда тут 0,6»). У ШТУЧНЫХ величин (показы) изменение — в процентах. У КОНВЕРСИЙ —
  в ПРОЦЕНТНЫХ ПУНКТАХ: `10,63% → 9,99%` это **−0,64 п.п.**, а −6% — относительное изменение,
  совсем другая величина. В карточке п.п. идут основной строкой, относительное — мелким
  в скобках. Десятичные с ЗАПЯТОЙ, как везде в дашборде. Никаких «зак.» — только «заказов»
  через `plural()`. Вклад меньше половины заказа пишется «почти не повлияло», а не «−0 заказов».
  Разложение рисуется **полосками** (`.chg-steps`/`.cs-row`), а не строкой через точку с
  запятой: сплошной текст продавец не читал.
  Плюс **каннибализация**: кто внутри склейки вырос. Сосед берётся из ТОЙ ЖЕ ЦЕНОВОЙ ПОЛКИ
  (±30%) — иначе выбирался просто самый выросший, а каталка за 2 700 ₽ не конкурент
  трактору за 4 700 ₽. У 49400769 это 49400768: тот же трактор, 4 303 ₽ против 4 659 ₽.
  **Каждая строка заканчивается ДЕЙСТВИЕМ** (`diag.act`), выведенным из ведущего фактора:
  остаток → вернуть в наличие; весь сегмент вниз → не трогать карточку; сосед той же полки
  вырос при росте цены → вернуть цену; корзина→заказ → цена и сроки; переход→корзина →
  контент карточки; CTR → главное фото; показы → выдача и рекламный импульс.
- **ТОВАР «НА ВЫВОД» В РЕКОМЕНДАЦИИ НЕ ПОПАДАЕТ** (просьба продавца 01.09.2026). Определяется
  по `productionStatus` каталога ВБ, правило то же, что в подсорте: считаем по ВСЕМ карточкам
  кода 1С, и если хоть одна НЕ «На вывод» — товар живой (`recOutSups`/`recIsOut`/`recAlive`,
  кеш сбрасывается в `hydrateData`). Сейчас на выводе 302 кода из 413, без фильтра половина
  советов была про товар, который и так убирают. Фильтр применён и к «Что изменилось».
- **ФОКУС СНИМАЕТ СОБСТВЕННЫЕ ФИЛЬТРЫ ВКЛАДКИ.** Иначе получается то, на что продавец
  указал: полоса говорит «9 позиций», а в таблице 7 — поверх фокуса продолжал работать
  выбранный на «Рекламе» фильтр по категории. `setFocus` чистит поиск, категории,
  колоночные фильтры и сегмент действия целевой вкладки; в «Рекламе» при активном фокусе
  игнорируется и выбранная группа (`adRows`/`adBaseRows`/`renderAdsPerf`), а знаменатель ДРР
  и KPI считаются ПО ФОКУСНОМУ НАБОРУ — продавец пришёл разбирать эти кампании, и шапка
  обязана описывать их, а не всю категорию. **Ключи содержат и код 1С, и артикул ВБ**:
  подсорт ключуется по коду, закуп — по артикулу, и без обоих «Дозаказать» открывался пустым.
  Число позиций хранится отдельно (`n`), иначе полоса показывала бы удвоенный счёт ключей.
- **ПЕРЕХОД ИЗ РЕКОМЕНДАЦИИ ФИЛЬТРУЕТ ЦЕЛЕВУЮ ТАБЛИЦУ** («я должен не только перемещаться,
  а ещё и список сам должен отфильтровываться»). Каждое правило несёт `keys` — коды 1С или
  артикулы ВБ; кнопка кладёт их в `recFocus`, а `focusApply()` вклинивается в
  `turnFilteredRows` / `srFilteredRows` / `adFilteredRows`. Сверху появляется полоса
  `#focusBar` с числом позиций и кнопкой «показать все» — без неё продавец забудет, что
  таблица урезана. Фокус живёт только на своей вкладке: `switchTab` его снимает.
- **CPO И ДРР — ОДНА ВЕЛИЧИНА В ДВУХ ЕДИНИЦАХ, И ЭТО НУЖНО ДЕРЖАТЬ В ГОЛОВЕ** (разбор
  с продавцом 01.09.2026). Потолок выводится как `цель × чек × выкуп`, ДРР считается как
  `расход ÷ (заказы × чек × выкуп)`, поэтому **`CPO/потолок = ДРР/цель` ТОЖДЕСТВЕННО**.
  Вердикт вида «ДРР мимо цели, но CPO в потолке» невозможен — он означает, что разъехались
  знаменатели. Так и было: ДРР считался по своим заказам, а CPO — по своим ПЛЮС
  ассоциативным (на «Пушкарах» 26–31.08: 191 ₽ вместо 283 ₽ при потолке 188 ₽). Исправлено,
  проверка тождества входит в тест.
  **Выкуп в потолке взвешивается ПО ВЫРУЧКЕ** (`rev/ordRub`), а не по заказам: только тогда
  `заказы × чек × выкуп` в точности равно выручке в выкупе (на order-взвешенном тождество
  расходилось на 1–2%).
  **ПАРА «С АССОЦИАТИВНЫМИ» — НЕ ОТМЕНЕНА, А ПОСТРОЕНА ЧЕСТНО** (уточнение продавца:
  «не исключай CPO с ассоциативными, но не так, как ты сделал изначально»). Если заказы
  соседей идут в числитель, их выручка обязана идти и в знаменатель ДРР, и в чек для потолка:
  `aovA`/`maxA`/`drrA`/`cpoAll`. Тогда работает вторая пара тождеств
  `CPO_асс/потолок_асс = ДРР_асс/цель`. **Имеет смысл ТОЛЬКО НА УРОВНЕ ТОВАРА** — на склейке
  и категории выручка соседей уже внутри уровня, и в строке склейки колонка «ДРР общий»
  показывает «—», а не двойной счёт.
- **ПО CPO НЕЛЬЗЯ СУДИТЬ О КЛИКАХ.** `CPO = CPC ÷ конверсия(клик→заказ)`: высокий CPO бывает
  и от дорогого клика, и от плохой конверсии. Формулировка «клики не дорогие, а мало
  конвертят», выведенная из CPO, была подменой. Диагностика теперь раскладывает ДРР на рычаги:
  `ДРР = CPC ÷ (конверсия клик→заказ × чек × выкуп)` — и каждый рычаг сравнивается с уровнем
  выше (товар со склейкой, склейка с категорией, категория с кабинетом). Ведёт рычаг с самым
  большим отклонением. Плюс `k = ДРР ÷ цель` переводится в две конкретные цифры: «клик должен
  стоить X ₽ вместо Y» и «конверсия должна быть Z% вместо W%». Если ни один рычаг не выбивается —
  так и пишем: дело в объёме, денег льётся больше, чем позиция отбивает своей выручкой.
- **ДЕРЕВО ГИПОТЕЗ «КАТЕГОРИЯ → СКЛЕЙКА → ТОВАР»** (`recAdTree` + `adHypo` + `adNodeAgg`,
  блок `#recAdTree`). На каждом уровне один и тот же разбор, меняется только набор карточек:
  ДРР в цели + запас есть → «можно добавлять бюджет»; в цели, но запаса мало → «сначала
  завезти в Москву»; мимо цели и CPO выше потолка → «снизить ставку в N раз»; мимо цели,
  но CPO в потолке → «дело не в ставке, смотреть карточку» (CTR/конверсия); расход без
  продаж → «выключить»; товар мимо цели внутри склейки, которая в цели → «оставить, тянет
  трафик». **Потолок CPO считается по данным ЭТОГО уровня** — у склейки свой чек и свой
  выкуп, сравнивать товар с чужим потолком нельзя. К узлу добавляется «Главный вклад» —
  дети с наибольшим перерасходом против цели, иначе вердикт «ДРР 10.5%» не говорит, что чинить.
  Карточки с расходом меньше `AD_MIN_SPEND` сворачиваются в сноску.
- **РАЗБОР ИЗМЕНЕНИЙ — ДЕРЕВОМ «КАТЕГОРИЯ → СКЛЕЙКА → ТОВАР»** (`recTree` + `recNode` +
  `recSum`, просьба продавца 01.09.2026: «проваливаюсь в склейки, дальше в товары»).
  На каждом уровне ТОТ ЖЕ разбор, что и у товара: факторное разложение, проверка на шум,
  действие. Складывать воронку можно смело — показы/переходы/корзины/заказы аддитивны;
  **конверсии пересчитываются ИЗ СУММ, а не усредняются** (среднее из процентов при разных
  объёмах даёт неверный результат). Проверка вложенности входит в тест: заказы категорий =
  заказы склеек = заказы товаров. Сегмент «Просело / Выросло / Всё» переключает выборку.
- **ПЛАН ПО ЧИСТОЙ ПРИБЫЛИ — КАРТОЧКА `#recPlanCard`** (`recPlanCalc` + `renderRecPlan`,
  решение продавца 10.09.2026: «давай поставим план по чистой прибыли для каждого мп и будем
  идти к этой сумме, выполняя рекомендации»). План живёт В СНИМКЕ — `meta.plan[<ГГГГ-ММ>] =
  {wb, oz, setAt}`, ставит **`scripts/set-plan.cjs`** (`list` / `<месяц> --wb N --oz N` /
  `drop <месяц>`): это решение человека, оно меняется каждый месяц, в коде ему не место.
  Первый план — сентябрь 2026: ВБ 6 280 000 ₽, Ozon 5 500 000 ₽.
  · **МЕСЯЦ КАЛЕНДАРНЫЙ НА ОБЕИХ ПЛОЩАДКАХ**, хотя у ВБ «свой» месяц идёт целыми неделями
    (сентябрь 31.08–27.09). План — управленческая цель, её проще держать по календарю, и иначе
    Озон с ВБ не складывается. Для сверки с закрывающими документами ВБ остаётся блок недель
    на «Главной»; в подписи карточки это оговорено.
  · **ФАКТ — ДО ПОСЛЕДНЕГО ДНЯ ДАННЫХ КАЖДОЙ ПЛОЩАДКИ ОТДЕЛЬНО** (`wbLast`/`ozLast`): выгрузки
    приходят в разные дни, и общий «последний день» занижал бы ту площадку, что свежее.
  · Прогноз ЛИНЕЙНЫЙ по темпу с начала месяца (`fact/days × left`). Под таблицей — перевод
    разрыва в ежедневную цифру («поднять с X до Y ₽/день, ×N»), чтобы рекомендации ниже стали
    измеримыми. Считается по ЖИВОМУ ассортименту (как вся «Главная»), поэтому Озон в карточке
    чуть ниже, чем в `ozon-finance.cjs`, — тот считает по всем товарам.
  · Прибыль ВБ с 03.08 — ОЦЕНКА, прибыль Озона — по «Предельной цене»; обе оговорки напечатаны
    в подписи карточки, не убирай их.
- **РЕКОМЕНДАЦИИ ПО РЕКЛАМЕ — ПО ПОСЛЕДНЕЙ ВЫГРУЗКЕ, А НЕ ПО НАКОПИТЕЛЮ** (`adPerfNow`,
  `AD_NOW_MIN_DAY=100` ₽/день, правка 10.09.2026). Продавец: «я уже давно отключил рекламу на
  товар которого в мск нету, а в дашборде информация не обновилась». Причина: `adPerf.bySku` —
  СУММА всех частей (сейчас с 26.08), и выключенная неделю назад кампания честно продолжала
  висеть в правиле со своим историческим расходом. Для ДРР суммирование правильное и там ничего
  не менялось; правила `adRisk` и `idle` переведены на `adSpendNow` — расход по ПОСЛЕДНЕЙ части,
  нормированный на день. Замер 10.09: было 11 кампаний на 220 347 ₽ по накопителю, стало
  **4 кампании на 3 098 ₽ за 09.09** (92 942 ₽ в пересчёте на месяц) — ровно те, что реально
  крутятся. `adSpendPrev` даёт сравнение с предыдущей выгрузкой (`recWasTxt`), а если риск
  исчез совсем — правило `adRiskOk` печатает, сколько кампаний вы выключили.
  · **ПРОЦЕНТ ОТ ПОЧТИ НУЛЯ — НЕ ПРОЦЕНТ.** Первая версия `recWasTxt` печатала «+2 016 667%»
    (вчера 6 копеек перелива, сегодня 1 210 ₽). Ниже порога `AD_NOW_MIN_DAY` пишем словами:
    «включена в эту выгрузку» / «сейчас выключена» / «раньше не крутилась»; рост больше чем
    вдвое — «в N раз», а не проценты.
  · Сравнение ВСЕГДА в ₽/ДЕНЬ: части накопителя разной длины (26–30.08 это 5 дней, дальше по
    одному), сравнение итогов в лоб дало бы пятикратный «рост» на ровном месте.
- Правила действий (`rank` = порядок): нечем закрыть · реклама без запаса в Мск ·
  отгрузить на Ozon · перемещение Евросиб→Нск-1 · склейки выше цели ДРР · цена и темп ·
  кандидаты без рекламы · дозаказ на фабрике · чего не хватает в данных.
- KPI: срочных задач · денег на кону (сумма `money` по правилам) · заказы неделя к неделе ·
  сколько товаров просело.

## Вкладка «Запас и темп» (`stockrate`) — сколько есть и на сколько хватит

Отвечает на вопрос продавца: какой темп продаж выставить в репрайсере, чтобы товара хватило
И на ВБ, И на Озоне ровно до прихода новой партии. `srRows()` + `srDecorate()` + `renderStockRateTab()`.

- **Ключ строки — код 1С** (на Озоне он же артикул): товар физически один, поэтому площадки
  считаются ОБЩИМ запасом, а не по отдельности.
- **Запас** = остаток ВБ (FBO) + остаток Озона + `ozTransit` + все три своих склада. Свой склад
  входит РОВНО ОДИН РАЗ, хотя он одновременно продаётся на ВБ по FBS и может уехать на Озон
  (подтверждено продавцом 25.08: файл «мои склады» включает FBS). **Wildberries считается целиком —
  и FBO, и FBS**: продавцу нужно знать, сколько продавать в день, чтобы товара хватило ровно до
  прихода партии и при этом не осталось лишнего (27.08).
- **`ozReach` — Озон вычитается только из того, до чего дотягивается** (27.08): `остаток Озона +
  Нск-1 + Евросиб`. Москва на Ozon не отгружает, поэтому её остаток Озону недоступен и целиком
  достаётся ВБ; без этой оговорки спрос Озона съедал бы товар, до которого он физически не дойдёт
  (арт. 487176: 4 995 шт в Москве, Озон дотягивается только до 711).
- **ЦЕНОЙ УПРАВЛЯЕМ ТОЛЬКО НА ВБ** (условие удержания индекса цен: ВБ ведущий, Озон следует
  за его ценой; в репрайсере темп задаётся именно для ВБ). Поэтому спрос Озона — ЗАДАННАЯ
  величина: `ozTake = spdOz × горизонт` вычитается из запаса, и цель считается для остатка:
  **`цель ВБ = (запас − ozTake) ÷ горизонт`**. Не возвращай «цель = весь запас ÷ горизонт» —
  так цель была завышена на объём Озона.
- **ЦЕЛЬ ДАЁТСЯ В ЗАКАЗАХ, А НЕ В ВЫКУПАХ** (`targetOrders = target ÷ boWb`): продавец вводит
  в репрайсер число ЗАКАЗОВ в день, к которому тот стремится (не добирает — снижает цену до
  заданного минимума, перебирает — поднимает). `boWb` — фактический % выкупа товара на ВБ
  (`spdWb/ordWb` за то же окно 7 дней), при отсутствии продаж — общий по магазину.
  Заказы по коду считает `srOrdersPerDay(mp)`. Не показывай цель в выкупах — её нельзя ввести.
- **Горизонт для кода БЕЗ своей партии** — по общей дате `inbound.order.etaSale` (решение
  продавца 25.08: «считай их так же до 10.12»), строка помечается `noOwnBatch`, «Партия» пустая.
  Без этого 139 из 194 позиций оставались без цели.
- **Вердикт** (`SR_BAND=0.15`) — только про ВБ: темп выше цели на 15% → «Поднять цену»,
  ниже на 15% → «Разогнать», между → «Держать». Отдельный **`short`** — «Не хватит и Озону»
  (`ozTake >= запас`): даже при нуле продаж на ВБ товар кончится, подъём цены поможет лишь
  частично (он притормозит и Озон, раз цена общая). Сегмент `#srActionSeg` фильтрует.
- **По Озону рычаг один — КОГДА ОТГРУЖАТЬ**: `отгрузить через = хватит дней на площадке −
  OZ_LEAD_DAYS(30)`; `≤0` → «срочно». Месяц — срок продавца на поставку до продажи на Ozon. Рядом показывается остаток своего склада: «срочно»
  при пустом складе — это уже дефицит, а не задача логистики.
- Выгрузка `exportStockRateXLSX` — лист «Как считается» + цифры.
- **Колоночные фильтры (воронки) — та же механика, что на «Оборачиваемости»**, но она больше
  не привязана к одной таблице: контекст описан в `CF_CTX` (`turn` / `sr`) — откуда брать набор
  фильтров, описания колонок, базовые строки и что перерисовывать. `thSortF(...,ctxName)` кладёт
  `data-fctx` в кнопку, `openColFilter(anchor,key,ctx)` его читает. На вкладке фильтруются
  «Категория», «Артикул» и «Что делать на ВБ» (`srColDefs`/`srColFilters`/`srFilteredRows`).
  **`closeColFilterOnScroll` даёт 400 мс форы после открытия**: широкая таблица при клике по
  шапке подскроливается, браузер шлёт `scroll`, и панель закрывалась в тот же миг, в который
  открылась — воронка выглядела нерабочей. Не заменяй обратно на `closeColFilter` в scroll/resize.

**У ПАРТИИ ДВЕ ДАТЫ:** `inbound.<china|order>.eta` — приход НА СКЛАД продавца,
`etaSale` — ВЫХОД В ПРОДАЖУ на площадке (между ними ~месяц на приёмку и отгрузку;
на 25.08 продавец назвал 2,5 и 3,5 месяца). **«Запас и темп» считает по `etaSale`** —
пока партия лежит непринятой, продажи она не закрывает; без неё берётся `eta`.
Ставятся `scripts/set-eta.cjs order 2026-11-09` и `... order --sale 2026-12-10`
(плюс `etaBySup`/`etaSaleBySup` для отдельных кодов), `set-eta.cjs list` — показать,
`set-eta.cjs drop china` — убрать партию целиком (её залили по ошибке).
Раньше хранилась только дата ЗАГРУЗКИ файла и подпись «приедет примерно через 3 месяца» —
дата не менялась, поэтому надпись висела вечно одинаковой. Теперь дни до прихода считаются
от СЕГОДНЯ и пересчитываются сами; прошедшая дата помечается «просрочена», а не считается
молча приехавшей. Без `eta` целевой темп не считается — в колонке стоит «дата партии не задана».

## UI-паттерны, которые уже есть — не изобретай заново

- **Кастомный `<select>`**: все выпадающие списки — не нативные `<select>`
  (те непредсказуемо открываются вверх/вниз и обрезаются `overflow:hidden`
  родителя). Компонент `enhanceSelect()` в конце `index.html` рендерит панель
  `position:fixed` под кнопкой; поддерживает мультивыбор (галочки) через
  `sel.multiple`. Категорийные фильтры — мультивыбор (`selCats(id)` +
  `catPred(cat)` — предикат, принимает строку или массив, `null` = без фильтра).
- **Сортировка кликом по шапке** (`wireSortable`): первый клик по колонке — «от большего к меньшему»
  (текстовые `name`/`category` — по возрастанию), повторный — наоборот. **Объект состояния сортировки
  НЕЛЬЗЯ пересоздавать** (`turnSortState = {...}`): `wireSortable` держит ссылку с момента загрузки,
  и после присваивания нового объекта клики уходят в «осиротевший» стейт — сортировка молча умирает.
  Меняй поля: `turnSortState.key=…; turnSortState.dir=…` (см. обработчик `#turnViewSeg`).
- **Sticky-шапка таблиц**: `.table-scroll{overflow:auto;max-height:74vh}` +
  `thead th{position:sticky;top:0}` — так и должно быть, не убирай max-height,
  иначе sticky снова перестанет работать (sticky требует свой scroll-контейнер).
- **«ВЫКУПЛЕНО НА СУММУ» — ВСЕГДА ОЦЕНКА, СЫРУЮ ЦИФРУ НЕ ПОКАЗЫВАЕМ** (08.09.2026, продавец
  заметил: «заказано не бьётся с выкуплено»). Элемент `money[дата][sku][1]` **заморожен на
  момент выгрузки и задним числом НЕ обновляется** — ни у ВБ (столбец «Выкупили на сумму»
  подённой аналитики), ни у Озона (заказы со статусом «Доставлен»; дедуп по «отправление+SKU»
  не даёт обновить статус при повторной заливке). Продавец выгружает день в тот же день, когда
  почти ничего не доехало, поэтому цифра остаётся мёртвой навсегда. Замер: ВБ — июль 26,8%,
  август 20,9%, сентябрь 1,8%; Озон — май 78,8% (старая заливка успела дозреть), июль 37,8%,
  сентябрь 4,2%; по дням скачет 0% → 21% → 0%. **Поэтому KPI, график и колонка таблицы считают
  `заказано × % выкупа`** — тем же способом, что выручка на «Главной», и подписаны «(оценка)».
  Хелперы: `wbMoneyBuyoutFor(d)` (денежный выкуп ВБ из `meta.buyoutWin.history`),
  `mpBuyoutRate(d)` (площадка активная), у Озона — `ozBuyoutFor(d)`. Проверка после правок:
  доля оценки от заказанного должна равняться проценту выкупа площадки (замер 01–07.09:
  Озон 80,0%, ВБ 79,5%, «Всего» 79,8% и равно сумме двух). **Не возвращай сырое `[1]` в показ.**
- **Подсказки на KPI «Динамики»** (`wireKpiTips("dynKpis")`): «В среднем в день» — лучший/слабейший
  день, ₽ в день, темп прошлого периода; «Активных товаров» — сколько с заказами и без, разбивка
  по категориям «с заказами / всего»; «Топ-категория» — весь расклад категорий с долями;
  деньги — средний чек и % выкупа от заказанного. Работают на всех трёх площадках.
- **«Динамика заказов» на «Всего» — разрез по площадкам.** KPI: две плитки «Из них Wildberries /
  Из них Ozon» (штуки + % заказов), деньги суммируются, в подсказках — разложение ВБ/Озон по штукам,
  «заказано ₽» и «выкуплено ₽». Таблица «Свод» на «Всего» своя: `ВБ, шт · Ozon, шт · Всего, шт ·
  Доля Ozon · ВБ, ₽ · Ozon, ₽ · Всего, ₽` (колонка категории убрана во вторую строку названия —
  иначе 9 колонок не влезают). Штуки по площадкам даёт `mpQtySplitByCode(fromIso,toIso)` (идёт
  напрямую в `WBDATA`/`OZDATA`, ключ — код 1С), деньги — `combinedMoney()`/`mpMoneySplit()`.
  Инвариант: по каждому коду ВБ + Озон = Всего и в штуках, и в рублях.
- **«Динамика заказов» → таблица «Товары за период»** имеет два вида (сегмент `#dynViewSeg`, состояние
  `dynView`): **«По дням»** (по умолчанию) — сводка *товар × дни* (`renderDynPivot`), как в рабочем файле
  продавца: колонка на каждый день периода, заливка `heatBg` показывает динамику **внутри товара**
  (относительно его же лучшего дня), закреплённая строка «Всего по дням» даёт итог по магазину за каждый
  день, «Итого» залипает справа (`position:sticky;right:0`), понедельники помечены пунктиром (`.wk`).
  Горизонтальная прокрутка тут НОРМАЛЬНА (в отличие от «Оборачиваемости») — колонок столько, сколько дней;
  товар и «Итого» залипают, поэтому не теряются. **«Свод»** — прежняя таблица (товар/категория/шт/доля/₽).
  Денежные колонки показываются, когда для площадки есть `orderSeries.money` (у «Всего» — скрыты).
  **ВЫГРУЗКА В EXCEL** (`exportDynamicsXLSX`, кнопка `#dynExport`, просьба продавца 10.09.2026:
  «чтобы была возможность поработать с файлом»). Правило то же, что на «Оборачиваемости»:
  **что видишь — то и качаешь**. Экспортёр НЕ пересчитывает данные, а берёт `_dynLast` —
  снимок того, что положил в таблицу `renderDynamicsTab` (строки, период, сортировка, площадка,
  флаги `hasMoneyData`/`isAll`). Иначе фильтры и период разъедутся с экраном при первой же
  правке рендера. Два листа: «Как считается» (площадка, период, вид, активные фильтры,
  сортировка, расшифровка колонок) + цифры. Вид «По дням» отдаёт сводку товар × дни вместе
  со строкой «ВСЕГО ПО ДНЯМ», вид «Свод» — итог за период; на «Всего» у свода свои колонки
  (ВБ/Ozon/Всего в штуках и ₽). На Озоне колонка «Код 1С» пустая — там артикул И ЕСТЬ код 1С,
  дублировать его не нужно. Проверка после правок: скачать все четыре сочетания
  (ВБ/Озон/Всего × по дням/свод) через Playwright — число строк и сумма штук обязаны совпасть
  с `_dynLast` на экране.
- **КНОПКИ ЗАКРЫТЫХ МЕСЯЦЕВ В ПЕРЕКЛЮЧАТЕЛЕ ПЕРИОДА** (просьба продавца 04.09.2026: «сделай
  помесячно там, где месяца уже закончились, чтобы было понятно, какой это месяц»). На «Главной»
  между неделями и «Весь период» стоят кнопки вида «авг 26», «июл 26». Состояние — `homeMonth`
  (ISO-ключ месяца), выбор месяца снимает выбор окна и наоборот. Списки строят
  `wbClosedMonths` / `ozClosedMonths` / `allClosedMonths`.
  · **ЗАКРЫТЫЙ = ЕСТЬ ЦЕЛИКОМ.** У Озона это все календарные дни месяца, у ВБ — все недели,
    попадающие в месяц по правилу четверга (`wbWeeksInMonth`). Незакрытый месяц кнопкой не
    показываем: продавец нажал бы «сен 2026» и получил три дня, приняв их за месяц. Поэтому
    у Озона нет мая (история начинается 03.05), а у ВБ нет июня и сентября.
  · **НА «ВСЕГО» МЕСЯЦ — ДВА РАЗНЫХ НАБОРА ДАТ** (у ВБ по неделям, у Озона календарный),
    поэтому общий список дат там не используется: каждая площадка берёт свои дни, а в подсказке
    кнопки написаны оба отрезка. Месяц показывается, только если закрыт на ОБЕИХ площадках.
  · **Относительные «2 мес / 3 мес» скрываются, когда есть именованные месяцы** — «3 мес» это
    скользящие 90 дней, рядом с «авг 26» читалось бы как её синоним.
  · **НЕДЕЛЬНЫЕ КНОПКИ СЧИТАЮТСЯ ТОЛЬКО ПО ОТКРЫТОМУ ХВОСТУ** (`openTailDays`) — дням после
    последнего закрытого месяца. Продавец 04.09.2026: «зачем ты оставил 8 недель, если у тебя
    только первая неделя сентября идёт», «август ты отдельно вынес, а если выбрать 8 недель,
    толку от них». Скользящее окно «8 нед» залезает в уже вынесенный отдельной кнопкой месяц
    и только путает. На 04.09 хвост 3–4 дня, поэтому недельных кнопок нет вовсе.
  · **ИДУЩИЙ МЕСЯЦ — ТОЖЕ КНОПКА** (`openMonthEntry`, подпись «сен 26 · идёт»). Без неё, когда
    хвост короче недели, посмотреть текущий месяц становится нечем: недельные окна не строятся.
    Спутать с закрытым нельзя — в подписи «идёт», в подсказке число дней. На «Всего» подсказка
    пересобирается и показывает ОБА отрезка: у ВБ и Озона месяц кончается в разные дни.
  · `renderPeriodSeg(segId,curN,daysAvail,months,curMonth)` — месяцы необязательны, «Финансы»
    и «Аналитика» их не передают и работают как раньше (проверено: набор кнопок и цифры те же).
  · Сверка при внедрении: каждая кнопка месяца на всех трёх площадках дала ровно тот же итог,
    что прямой расчёт по датам месяца (ВБ авг 3 078 374 ₽/28 дн, июл 7 076 756 ₽/35 дн;
    Ozon авг 4 933 399 ₽/31 дн; Всего авг 8 011 773 ₽ = ВБ 28 дн + Ozon 31 дн).
- **Периоды СТРОЯТСЯ ПО ФАКТУ ЗАГРУЖЕННОГО** (`periodOptions` + `renderPeriodSeg`, вызывается
  в начале рендера Главной/Финансов/Аналитики). Кнопки: «1 нед … N нед» пока недели помещаются
  в историю (максимум 8), затем месяцы, которые строго меньше истории, и всегда «Весь период»
  (`n=0`). Раньше сетка была фиксированной (7/14/21/28 дн, 2–6 мес), и когда пришла 5-я неделя,
  «28 дн» перестало быть «всеми данными» — увидеть всё можно было только через «2 мес», которых
  в снимке нет. **Не возвращай статические кнопки.** Дефолт везде `n=0` («Весь период»).
  В строке свежести Главной печатается «загружено N недель».
- **МЕСЯЦ ПО-ВБ в блоке недель** (`wbMonthOfWeek`/`groupWeeksByWbMonth`/`toggleHomeMonth`): WB закрывает
  месяц не календарём, а ЦЕЛЫМИ неделями — у продавца июль закрылся 02.08, август идёт 03.08–30.08.
  Правило, которое это воспроизводит: **неделя относится к месяцу, где лежит её ЧЕТВЕРГ** (он же —
  месяц, в котором не меньше 4 дней недели). Проверка: 27.07–02.08 → чт 30.07 → июль; 03–09.08 →
  06.08 → август; 31.08–06.09 → 03.09 → сентябрь (потому август и кончается 30.08).
  **Не заменяй на «месяц первого дня недели»** — тогда 31.08 уехало бы в август и период разъехался
  бы с закрывающими документами ВБ. По умолчанию месяцы СВЁРНУТЫ (строка-итог), клик раскрывает
  недели; `wbWeeksInMonth` считает, сколько целых недель в месяце, и неполный месяц помечается
  «неполный» — иначе продавец сравнивал бы 3 недели августа с полным июлем.
- **Блок «Каждая неделя отдельно»** на Главной (`weeklyBreakdown` + `renderHomeWeeks`,
  `#homeWeeksCard`): кнопки периода дают накопительный итог, и по ним не понять, сколько
  принесла отдельная неделя. Таблица режет ВСЮ историю на куски по 7 дней С КОНЦА (у WB
  отчёты пн–вс, куски совпадают с календарными неделями) и считает по каждой: продано,
  выручка, удержания WB, себестоимость, реклама, прибыль, маржа. **Не зависит от выбранного
  периода**, но слушает фильтры категории/поиска. Лучшая и худшая недели помечены.
  Δ к прошлой неделе: у выручки — %, у прибыли — В РУБЛЯХ (база бывает отрицательной,
  и процент превращался в «−823%»). Инвариант: сумма недель = «Весь период» до рубля.
  **Недели КАЛЕНДАРНЫЕ, от понедельника** (`mondayOf`). Раньше история резалась кусками по
  7 дней С КОНЦА — это совпадало с пн–вс только пока последний день был воскресеньем. Пришёл
  понедельник 24.08 — и все границы уехали на день (недели стали 04–10.08, 11–17.08), а в
  начале истории появился обрубок в 1 день. Не возвращай нарезку от конца массива.
  **ТО ЖЕ ПРАВИЛО ДЕЙСТВУЕТ И В ОТВЕТАХ ПРОДАВЦУ, А НЕ ТОЛЬКО В КОДЕ** (его правка 15.09.2026:
  «не нравится как ты периоды сравниваешь, ты получается пропустил 7.09 бери ровные периоды»).
  Промах был такой: пришли данные за понедельник 14.09, и я сдвинул окно на 08–14.09, чтобы
  свежий день попал в таблицу, — при этом сравнил его с КАЛЕНДАРНОЙ неделей 31.08–06.09.
  В итоге 07.09 не попало никуда, а два сравниваемых отрезка были разной природы.
  · Неделя — ВСЕГДА пн–вс. Последняя ЗАКРЫТАЯ неделя сравнивается с предыдущей ЗАКРЫТОЙ.
  · Идущая неделя показывается ОТДЕЛЬНОЙ строкой с пометкой «неполная, N дн» и в сравнение
    не идёт — иначе 1–2 дня читаются как провал против семи.
  · Не двигай границы окна ради того, чтобы захватить свежий день. Свежий день живёт
    в подённой таблице; неделя ждёт воскресенья.
- **«к пред.» — только окно ТАКОЙ ЖЕ длины** (`dateRangeFromCache`: `prev` пустой, если истории
  меньше окна). Раньше брался остаток любой длины, и 28 дней сравнивались с 7 предыдущими —
  выручка «росла» на 274%. Нет полного предыдущего окна — сравнения нет.
  На «Динамике заказов» вместо этого свободный календарь (`dynFrom`/`dynTo`) —
  там есть подённая история заказов, и это удобнее пресетов.
- **Тепловая заливка / мини-бары в ячейках** (`heatBg()`, `.share-fill`) —
  премиум-визуал таблиц; не заменяй на 3D/декоративный фон — обсуждали с
  пользователем и сознательно выбрали «данные как визуал» вместо буквального 3D.
- **Аврора-фон** (`body::before`, `@keyframes auroraDrift`) — фон-атмосфера,
  layering через `html{background:var(--page)}` + `body{background:transparent}`
  + `z-index:-1`. Если меняешь фон страницы — не потеряй это слоение.
- **Отказы загрузки НЕ должны выглядеть как «данных нет»** (инцидент 24.08: продавец увидел
  пустую «Главную» с надписью «пришлите первый отчёт», хотя снимок лежал рядом). Три места:
  · `fdbOpen()` — IndexedDB тут нужна ТОЛЬКО старым браузерным загрузкам, поэтому её отказ
    больше не фатален: есть `onblocked` + таймаут 8 с (без них `open` мог висеть вечно, если база
    открыта в другой вкладке со старой версией), а `fdbGetAll`/`fdbGetAllKeys` идут через
    `fdbReadSafe` и **возвращают `[]` вместо reject**. Раньше любая ошибка базы уходила в
    `.catch(()=>{})` внутри `bootApp` и обнуляла ВСЕ три кеша — снимок становился невидим.
    Флаг `IDB_FAILED` показывает, что локальные загрузки прочитать не удалось.
  · `loadSnapshot()` — **две повторные попытки** (`&retry=N`, паузы 1,5 и 3 с), и только потом
    `snapshotFailed()`: честный экран «Данные не загрузились» с кнопкой «Повторить».
    `initGate()` при `typeof WB_SECURE==="undefined"` теперь тоже зовёт `snapshotFailed()`,
    а НЕ грузит пустой дашборд — не возвращай прежнее «работаем как раньше».
  · `renderHomeTab()` — если `FINANCE_CACHE` пуст, но `BAKED_FINANCE` есть, в строке свежести
    пишется, что данные в снимке есть, а показать их не вышло (+ ссылка на сброс локальных данных).
  `UNLOCK_FATAL` — причина, по которой снимок не открылся, когда дело НЕ в коде доступа
  (например, старый браузер без `DecompressionStream`): иначе выводилось «Неверный код».
- **Экран кода доступа** (`#lockScreen`, `initGate()`) — гейт перед рендером
  приложения. `bootApp()` вызывается только после успешной расшифровки.
  `hydrateData()` пересобирает производные каталога (`catalogCatBySku` и т.п.)
  после расшифровки — если добавляешь новую производную структуру от `RCAT`,
  регистрируй её пересборку там же, иначе после первой загрузки она не обновится.

## Публикация на GitHub Pages — ПРОВЕРЯЙ, ЧТО ДОЕХАЛО

Сайт публикует workflow «pages build and deployment» (ветка `claude/wb-dashboard-html-0kxavb`).
Две вещи, из-за которых продавец пишет «не вижу изменений», хотя код запушен:

1. **Деплой идёт ~5 минут.** Сразу после push сайт ещё отдаёт прошлую версию.
2. **Новый push ОТМЕНЯЕТ незавершённый деплой предыдущего.** Если запушить три коммита
   подряд, первые два получают `cancelled`, и живой остаётся версия, которая успела
   собраться ДО них — сайт залипает на 10–20 минут. Так и вышло 06.08: продавец видел
   build от коммита `691a97e`, пока деплои `f095fa5` (отменён) и `dd8b8e0` (в процессе)
   ещё не доехали.

Отсюда правила:
- **Копи правки и пушь одним коммитом**, а не тремя подряд.
- **После push проверяй статус деплоя** и только потом говори «готово»:
  `mcp__github__actions_list` → `list_workflow_runs`, смотри верхний прогон по ветке —
  нужен `completed / success` с нужным `head_sha`. `cancelled` = версия НЕ опубликована.
- Отпечаток в адресе (`?b=…`) у продавца показывает, какую версию реально отдаёт Pages —
  быстрый способ понять, доехало или нет.

## Тестирование (нет CI, тестируй руками через Playwright)

Песочница блокирует CDN, поэтому SheetJS с jsdelivr не грузится в headless-тестах
самого сайта — при необходимости используй `npm install xlsx` локально и
`page.route()` для подмены CDN-URL на локальный файл, либо тестируй те функции,
что не требуют XLSX-воркера (большинство логики).

Типовой прогон:
```
node http-server-эквивалент index.html-папку -p PORT
# в Playwright: page.goto → fill('#lockInput', код) → click('.lock-btn')
# → waitForFunction(() => FINANCE_CACHE.length > 0)
```
После правок в парсинге/агрегации — обязательно сверяй контрольные суммы
(выручка/к оплате/реклама) с тем, что видно в реальном личном кабинете WB —
пользователь их присылает или показывает на скриншотах.

## ПРАВИЛО: еженедельное закрытие недели (продавец грузит отчёт по понедельникам)

Продавец просил: **каждый понедельник после его загрузки — цифры за прошедшую неделю.**
Финансовых отчётов у него больше нет, поэтому неделя закрывается ОЦЕНКОЙ:

```
node scripts/wb-extend-orders.cjs <подённые файлы...>   # заказы (штуки + ₽)
node scripts/bake-funnel.cjs <те же файлы...>           # воронка
node scripts/wb-buyout-window.cjs <сводный отчёт.xlsx>  # % выкупа, если прислал сводный
node scripts/model-finance.cjs --ads <реклама за период>  # оценка финансов (по умолчанию 0)
node scripts/encrypt.cjs <код> && git commit && git push
```

В ответе продавцу за неделю давать: **выручка · итого к оплате · себестоимость · реклама ·
прибыль · маржа**, и обязательно помечать, что это оценка, а не отчёт. Раз в 3 недели
имеет смысл просить сводный отчёт аналитики за уже дозревший период — он и уточняет
`% выкупа` (`wb-buyout-window.cjs`), и служит проверкой модели.

## ПРАВИЛО: после любого обновления остатков — пересчитать подсорт

Продавец попросил явно: **после КАЖДОЙ заливки остатков — ВБ, Озона или своего склада —
всегда пересчитывать необходимость подтоварки и сообщать результат.** Не ждать вопроса.

```
node scripts/resupply-report.cjs [дней=30] [строк=15]
```

Скрипт повторяет логику вида «Подсорт со склада» (те же покрытия, тот же приоритет Озону)
и печатает: сколько позиций требуют подсорта, сколько срочных, сколько штук отгрузить на
каждую площадку, чего нечем закрыть и что останется на складе. Его цифры обязаны совпадать
с дашбордом — это заодно проверка, что заливка не сломала расчёт.

В ответе продавцу после обновления остатков давать минимум: **к отгрузке всего / на Озон /
на ВБ · сколько позиций нечем закрыть · что срочное**.

## Стиль работы с пользователем

- Пользователь — не разработчик, объясняй результат человеческим языком,
  без жаргона, коротко «что сделал / что проверил / что дальше».
- **ВСЕГДА НАЗЫВАЙ ПЛОЩАДКУ — В ЗАГОЛОВКЕ, В ТАБЛИЦЕ И РЯДОМ С КАЖДОЙ ЦИФРОЙ** (просьба
  продавца 03.09.2026: «про какой из мп ты начинаешь говорить? если по вб, так и пиши»).
  У продавца два кабинета с разными цифрами и разными артикулами, и «заказы упали на 31%»
  без подписи площадки — бесполезная фраза: он не знает, где смотреть.
  · Заголовок раздела несёт площадку («Ozon: что произошло»), таблицы тоже; в перечислениях
    товаров пиши «арт. Ozon 487175», а не голый код.
  · **НЕ МЕШАЙ ПЛОЩАДКИ В ОДНОМ РАЗДЕЛЕ.** Промах 03.09.2026: в разборе Озона привёл пример
    аута 501432 на ВБ — читалось так, будто весь разбор про ВБ. Пример с другой площадки —
    либо отдельным блоком с подписью, либо не приводить.
  · В общих списках (что прислать, чего не хватает) у каждого пункта пометь, к какой площадке
    он относится: ВБ, Ozon или к обеим.
- **ЛЮБОЕ УТВЕРЖДЕНИЕ В СТЕПЕНИ — ТОЛЬКО ПОСЛЕ ПРОГОНА ПО ВСЕМУ РЯДУ** (просьба продавца
  03.09.2026: «прежде чем что-то писать, проверь лучше несколько раз, я тебе доверяю
  и не хочу подставляться»). Он пересказывает мои цифры руководителю, поэтому ошибка
  в ответе — это его репутация, а не моя.
  · Слова «рекорд», «лучший/худший», «максимум», «впервые», «никогда», «всегда»,
    «за всю историю» требуют СРАВНЕНИЯ СО ВСЕМ РЯДОМ, а не с соседним днём. Реальный
    промах 03.09.2026: назвал 02.09 «лучшим днём Озона за всю историю» (354 шт /
    1 280 763 ₽), сравнив только с 01.09, который заливал накануне. На деле это 15-е
    место из 121 по деньгам и 21-е по штукам; рекорд — 12.08 (1 714 699 ₽ / 559 шт).
    Данные лежали в том же `orderSeries`, достаточно было отсортировать.
  · Числа в ответе продавцу берутся ИЗ ПРОГОНА ПО ДАННЫМ, а не из головы и не из своих
    прошлых сообщений в этой же сессии. Цифра из вчерашнего ответа могла устареть после
    новой заливки.
  · Если утверждение проверить нечем (данных за период нет, источник неполный) — так
    и писать, а не заменять его правдоподобной формулировкой.
- Перед тем как заливать изменения в данные — прогоняй контрольные суммы и
  проговаривай их пользователю (он сверяет с личным кабинетом WB).
- Не удаляй/не переписывай историю git без явной просьбы (репозиторий публичный,
  но пользователь однажды отказался от чистки истории — уважай это).
- Мелкие правки (баги, визуал) можно и нужно делать в текущей сессии не спрашивая
  лишний раз; смену кода доступа, чистку git-истории, необратимые действия —
  подтверждать явно.- **КОЛОНКА ЦЕНЫ В ВЫГРУЗКЕ «ЗАКАЗЫ» ПЕРЕИМЕНОВЫВАЕТСЯ** (08.09.2026): была «Ваша цена»,
  стала «Предельная цена» (значения совпадают, сверено с «Суммой отправления»). Парсер искал
  одно имя и при промахе писал НОЛЬ — штуки заливались, деньги нет, и день выглядел как продажи
  без выручки (07.09: 339 шт при 0 ₽). Теперь имя ищется по списку `PRICE_COLS`, а при промахе
  скрипт ПАДАЕТ с перечислением колонок файла: молчаливый ноль опаснее падения. Новое имя
  колонки добавляй в `PRICE_COLS`, не заменяй старое.
  · **Чтобы перезалить уже испорченный день**, недостаточно повторить заливку — дедуп по
    «Номер отправления + SKU» её пропустит. Надо удалить день из `byDateArt*` и убрать его
    ключи из `M.keys` (ключи даты не содержат — вычисляются из самого файла).

